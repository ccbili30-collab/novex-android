# 边缘组件重排 + 沉浸收起 + 状态数据修复

日期：2026-09-15。状态：已对齐开工（用户"开工"指令 + /code-review 协议，重级三代理）。

## 已确认决策（用户逐条确认）

1. **状态合并修复（bug）**：新分支快照先继承路径上最近祖先的全部值，再套用本轮更新
   （"更新旧数据、添加新数据"）。根因：`NovexConversationConfiguration.kt:319` 的
   SetPlaythroughValue 在分支无快照时从空新建，面板取最近快照 → 旧值全被盖住。
2. **收起系统**：范围 = 顶栏（上滑消失）+ 主线页侧边组件（左列/右把手滑出留 6dp 微边）
   + 右下浮动按钮（隐藏）。触发：点空白处切换 + 空闲 6 秒自动收起。生成中不豁免。
   侧边对话页不收起（顶栏、书签常驻）。微边保留状态徽点。返回顺序：侧边→主线→列表。
3. **状态把手**：改回淡半圆，右缘固定，箭头朝内；按下即拖、只沿右缘上下；按下放大
   + 触感反馈 + 系统手势排除。
4. **面板**：L 形拐角缩放手柄（把手固定右缘 → L 恒在面板左下角）；最小 240×220dp；
   未缩放高度自适应，缩放过后按会话记住；文字大小不变、窄则换行竖向折叠；
   数值带进度条（AI 更新时给 max 自动成条）；小节可折叠；不做标签页。
5. **书签**：细长条（常规 22×96dp / 当前 28×120dp），首字竖排上端，底端小尖角，
   色板色；左缘固定成列；跨主线↔侧边页常驻；进入对应侧边时变粗变长，主线页全部
   常规收缩态；新书签排最下；拖动仅保留排序（按下即拖重排）；取消长按。
6. **回传措辞**：指令模板补"状态相关变化写明字段与数值（如 hp 83/100）"。
7. **常驻血条**（收起时也可见的边缘小条）：本批不做，下版再议。
8. **状态不进回传**：维持自然语言简报（决策 11 原案）；结构化补丁留作后手。

## 实现顺序

领域（合并+max 字段+编解码+工具 schema）→ 把手半圆+面板（缩放/条/折叠）→
书签轨（跨页/排序）→ 收起系统（ChatScreen）→ 测试 → 机械检查 → 三代理审查 → CI。

## 完成后

验收结论并入本文，用户真机确认后并入 `docs/PRODUCT.md` 并删除。
