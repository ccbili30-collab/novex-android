# 身份、卡片引用与会话草稿更新

## 确认范围

本计划继承用户最近确认，不替代旧修复清单，也不宣布旧问题已通过手机复验。
只改预览工作树的 next（下一版）分支；不碰正式分支和稳定工作树。检查点 0 不改版本号、不发布安装包。
人工智能不操作手机或模拟器；实际点击由用户完成。每个检查点独立测试、提交。

## 检查点 0：领域与接口基线

- 回答身份支持默认 Nova、独立人格预设、具体角色版本；每对话仅一个主身份。
- 背景设定、管理对象和启动文游是不同关系，不能相互隐式授权或激活。
- 身份使用会话快照；保存、恢复和上下文引用记录采用同一身份含义。
- 第一小步：扩展人格快照持久化，验证挂载背景或管理角色不改变人格；兼容旧 Nova 和角色数据。
- 该小步不是“人格选择、文游启动及提示词运行时全部完成”。后续必须接通实际请求路径。

## 后续连续检查点

1. 身份运行时与产品教学：Nova 负责一般交流、整理与共创；启动文游采用指定身份或默认游戏主持人；仅挂载文游不启动。明确创建卡片的请求直接走原生工具流程，模型自行选择技术格式与模块结构，不反复问要文本还是内置卡；真实冲突、成本和受保护写入仍需确认。
2. 私有空卡与工作空间：新对话提供三个私有空草稿及一个工作空间；存在草稿不代表启用身份、背景或文游。成功写入立即持久化；返回对话列表时清理无引用的空草稿，保留非空作品和文件；进行中的任务与待确认写入保护其目标，不清共享卡片或媒体。
3. 卡片带用途引用：文游可引用多个世界及角色版本，按背景、回答身份、玩家身份、规则、管理区分用途；稳定编号、局部定位、反向索引、循环去重、缺失引用、删除影响与导入导出一起测试。引用不扩大管理权限，不静默改动本局快照。
4. 玩家身份隔离：角色用于扮演时才启用配套身份建议；背景读取、搜索和摘要不能泄露该模块。用户现有选择和文游身份约束发生冲突时明确询问。
5. 角色阶段与关系管理：区分人生阶段、平行分身和编辑修订，不自动同步记忆；先建立可查可编辑的关系列表，再做围绕当前角色展开的局部关系图，不做全库图谱大工程。
6. 集成与交付：实际请求装配、权限过滤、草稿恢复、引用导出和工具行为通过自动测试后，走日常快速通道生成一批候选；用户验证真实模型理解与手机交互。

## 明确延期

模块编辑继续复用现有 JSON（结构化数据）与 Markdown（标记文本）能力，不新增通用富文本组件搭建器。
时间线、地图等特殊模块的专门渲染是后续补全，不挡上述检查点。保留现有图片、模块和交换格式，不重新生成素材。
本轮不实现多角色独立发言或子智能体运行时。

## 测试边界

沿用已确认的领域命令、内容读写、渲染投影、文档与运行时接口，测试可观察结果，不测试私有实现。
每次一条失败行为测试，再实现最小修改并回归；不把提示词字符串检查冒充真实模型行为通过。
现有正式卡片和会话迁移不可丢失未知模块、共享媒体引用、分支数据或旧身份配置。

## 已确认的结束文游规则

用户已确认：明确结束文游时恢复启动前的回答身份与玩家身份，保留消息、本局状态和存档。返回对话列表不结束文游、不切换身份，只执行私有草稿清理与作品归档。
恢复点随对话持久保存；重复结束不重复切换身份。旧文游缺少启动前记录时不得猜测原身份。

## 本轮实施记录

- 接续提交 `3c8b8c5`，先实现身份请求接入与文游启动／结束，再处理私有空卡、带用途引用和访问隔离。
- 沿用已确认测试接口：对话领域命令与持久化、上下文加载与请求装配、内容工作空间命令、编辑草稿与渲染投影。先失败行为测试，再最小实现。
- 酒馆双向通道按用户确认的研究建议纳入结构与来源保留；完整第三方脚本运行不阻塞当前主线。

### 检查点 1：身份请求与文游生命周期（已通过自动回归）

- 默认助手、独立人格与所选角色通过同一上下文入口进入真实请求；管理挂载不成为背景。保留旧会话的角色快照，但只在选中对应角色时启用其扮演要求。
- 身份核心优先分配上下文预算；会话补充提示词与图片风格继续独立保存。工具授权沿用现有软件规则。
- 会话编辑可选择主持人或创建独立人格、修改职责与玩家身份。文游玩家身份冲突需明确替换；结束恢复启动前身份。
- 每次开局有独立局次编号，结束工具核对该编号。历史局次保存原文游快照、分支状态和注册操作，可以从对话编辑查看；同卡重开也不会被旧调用结束。
- 有效配置在首条消息前持久保存；返回列表不清理已开局或已结束但仍有历史的会话。会话配置写入成功后才更新运行状态，并检查编辑期间的新状态冲突。
- 小范围运行时检查 44 项通过；最终预览编译、领域集成回归和会话编辑规则检查均通过，涵盖首条消息前保留与旧身份兼容。未进行手机点击或真实模型对话验收。
- 文游指定回答身份已经有会话快照字段与行为测试；原生文游编辑器中的来源关系仍在后续“带用途引用”检查点接入，不能据此声称完整互引已完成。

### 检查点 2 的设计基线

三张空卡将通过现有内容工作空间的事务一起创建，并保存各自来源对话及私有状态；全局列表按私有状态过滤。退出清理在同一个事务中检查正文、模块、图片、引用与任务保护，非空作品转入全局库并保留来源，不依赖名称判断归属。对话工作空间继续使用现有稳定对话编号与文件读写接口，不再建立另一套文件目录规则。

最终远程检查报告：`tmp/novex-fast-check/3c8b8c5a3bf7-dirty-20260906T161013Z-82935-novex-domain.log`（预览编译、领域与会话编辑检查通过）。未修改版本号，未发布候选安装包。

### 检查点 2：对话私有草稿、原生创建与成果归属（已通过自动回归）

- 新对话先取得稳定编号，再通过现有数据库事务建立私有世界、角色本体和文游空卡。空卡不挂载背景、不切换身份、不启动文游；全局列表和其他卡片的关系选择器隐藏私有内容。
- 所有正文、模块和草稿来源即时持久化。返回列表时，非空作品归库并保留来源；没有正文、模块、图片、关系引用、对话引用或待执行计划保护的空卡才清理。重新进入时补齐已清理的类型，不替换保留下来的作品。
- 原生创建请求优先使用本对话尚未填写且未被计划占用的目标。继续创建同类作品时分配新的私有卡；写入前再次检查归属和空白状态，防止覆盖后来填写的内容。明确的私有创建可直接执行，共享修改、删除与跨项目变更仍沿用实际确认规则。
- 待执行计划、完整模块正文、预分配模块编号和目标保护一起保存；重启可恢复，执行成功解除保护。对话编辑可查看并取消待执行计划。两个待执行创建计划不会复用同一空卡。
- 私有角色通过稳定版本编号读取，不依赖被过滤的全局列表。上下文只获得创作目标目录；卡片正文仍按背景、回答身份或管理权限分别读取。
- 真正返回对话列表才触发归档。进入设置、旋转等页面卸载不再清理。运行中任务等待结束后处理；可恢复任务和待执行计划保留目标；返回列表不结束活动文游。
- 工作空间文件继续采用现有对话与分支目录。成功写入、编辑和产生文件的计算操作登记到创作库，保存来源；重启或退出时按文件索引补登记，同一内容不增加重复修订。
- 修正数据库事务与工作空间命令锁的取得顺序，避免两段对话相互等待。外层事务尚未提交时暂缓媒体文件的物理删除，防止数据库回滚恢复引用后图片已经消失；此处没有新增完整的孤立媒体回收器。
- 新增的真实数据库与文件集成检查 14 项通过，涵盖迁移、中断、重启、引用保护、原生创建、重复写入拒绝、独立目标、并发与媒体回滚。最终预览编译和整组回归通过，包含同类私有卡与独立目标的追加行为。
- 本检查点没有改版本号、没有发布安装包，也没有操作手机。带用途互引、完整玩家身份访问隔离、角色阶段关系与酒馆交换通道仍属于后续检查点，不能因草稿入口接通就宣布完成。

检查点 2 最终远程检查报告：`tmp/novex-fast-check/79ad8c52b932-dirty-20260906T173246Z-86931-novex-domain.log`，构建与回归通过，用时约 5 分半。

### 检查点 3：带用途引用、快照与原生交换（主体接通，继续补全身份隔离）

- 数据库升级到 27，独立保存卡片引用及双向索引；来源和目标使用稳定编号，可定位模块及集合条目。引用来源模块必须属于来源卡片；同一位置只能有一个回答身份或玩家身份，替换沿用引用编号。
- 普通对话沿背景与规则引用读取资料，其他用途不自动展开；循环去重并限制深度。背景人物不会替换回答身份，管理关系不扩大目标卡片的读写权限。
- 文游在用户实际选择时固定根卡、关联背景、明确回答角色及对话既有背景；原卡修改不悄悄改变这些已采用文本。切换回答身份后停止使用原角色专属指令。借用另一文游的规则不会启动其开局、玩家身份或注册操作。
- 世界、角色版本、文游与模块详情共用引用编辑入口，支持目标、模块和条目选择，查看向外引用和使用来源；删除对话框显示关联影响。删除目标后保留独立使用方及缺失引用；删除来源时清除其向外引用。
- 私有草稿有向外引用时算作内容；有传入引用的空目标受到退出清理保护。
- 原生卡包可包含引用依赖、关系、来源编号与内容修订摘要；每次导入创建独立编号映射，并在同一事务内恢复图关系。原有世界与角色关联只在本次导入范围内明确匹配，缺失外部目标保留原始地址，不查找本地同名对象。依赖包非法时整体回滚。导入预览列出同时导入的依赖，导出入口说明包含依赖。
- 管理工具增加设置／移除带用途引用、向外引用和反向引用目录，并给出用途合法值与示例。只要求来源卡片的编辑权限；删除命令在实际执行时再次核对引用归属，不能通过伪造来源删除另一张卡片的引用。
- 真实数据库与原生压缩卡包往返检查已累计 16 项通过，包含 26 → 27 迁移、重启、私有目标保护、身份选择、循环、精确条目、源卡改写后的本局稳定、缺失依赖、独立版本映射、旧关联恢复、非法导入回滚和管理授权。
- 引用界面及依赖交换的整组预览编译与回归分别通过：`tmp/novex-fast-check/ed502dc85be9-dirty-20260906T183521Z-89635-novex-domain.log`、`tmp/novex-fast-check/ed502dc85be9-dirty-20260906T184617Z-90204-novex-domain.log`。工具接入后的最终整组回归也通过：`tmp/novex-fast-check/ed502dc85be9-dirty-20260906T185354Z-90660-novex-domain.log`，用时约 5 分 42 秒。
- 尚不能宣称本轮完整结束：玩家身份引用的启动选择、配套玩家模块全路径隔离、文游独立人格来源编辑、角色阶段关系、受范围控制的主动读取／搜索和酒馆外部格式适配仍需继续；已有界面暂不提供尚未接通的玩家身份引用新增入口。也未完成快照媒体的完整独立持有、采用后的显式刷新，以及对话背景移除与冻结引用来源之间的细分处理。未操作手机、未更改版本号、未发布安装包。

### 检查点 4：身份用途隔离、玩家选择与主动读取

- 角色新增专属扮演指令和配套玩家身份模块，文游新增独立回答人格模块，继续复用文章编辑、保存、预览及原生交换。资料加载器在新指令模块有正文时优先使用它，原始字段仍保留。
- 请求装配、当前资料目录、搜索和按编号读取共用用途过滤。背景人物不暴露配套身份或扮演指令；当前回答角色仅取得扮演指令，未采用的配套身份不能靠猜编号读取。切换身份时过滤旧格式提示词中已停用的生成块，保留块外提示词和存储原件。
- 管理角色总览只返回公开资料及私有模块编号／类型，不返回私有自定义标题或正文；明确指定模块后才读取对应内容。旧格式专属字段通过单独资料范围读取，未知原始扩展不自动暴露。
- 文游玩家身份、明确玩家用途引用和回答角色配套身份形成独立候选，同一候选去重，不同候选不拼接。未选择的多身份快照不能启动；界面显示正文并允许明确保留当前身份。候选和最终采用内容随本局保存，选择不重新读取被修改的原卡。
- 对话编辑选择角色和从角色卡新建对话接入相同配套规则；冲突时保留已有选择并要求明确处理。仅挂载背景不读取配套身份。结束文游继续恢复启动前的回答／玩家身份。
- 玩家用途引用可定位身份模块，拒绝世界卡及非身份模块；借用另一文游身份不启动其开局、人格或操作。共享引用界面开放玩家用途。
- 文游独立人格以模块名称表示职责名称、正文保存职责指令，启动时固定。独立人格与具体回答角色同时声明时拒绝启动，要求保留一个身份；借用规则或后来切换回答身份不会带入原人格模块。
- 当前资料读取工具返回来源编号、修订摘要、正文长度和下一偏移，继续读取必须携带同一修订。目录预览与搜索片段明确标注不是全文；阅读覆盖持久记录和本轮查看来源仍在下一检查点接入。
- 真实数据库身份检查七项通过，覆盖使用／管理隔离、读取／搜索、玩家冲突、选择重载、结束还原、明确借用及独立人格快照；旧格式身份投影另有一项纯逻辑检查通过。
- 预览编译与整组回归通过：`tmp/novex-fast-check/33c00bea58b1-dirty-20260906T191556Z-91791-novex-domain.log`、`tmp/novex-fast-check/33c00bea58b1-dirty-20260906T192902Z-92867-novex-domain.log`、`tmp/novex-fast-check/33c00bea58b1-dirty-20260906T193638Z-93383-novex-domain.log`。
- 交换边界补上导出与导入一致的文件数量／解压容量限制，五项卡包检查通过。引用中的未知扩展及嵌套来源信息在编号映射时保留，已知编号采用新映射，未知字段不执行；专门往返检查通过。
- 本检查点最终整组预览编译与回归通过：`tmp/novex-fast-check/33c00bea58b1-dirty-20260906T194356Z-93857-novex-domain.log`，用时约 5 分 40 秒。
- 尚需继续：阅读覆盖与本轮来源的持久闭环、人生阶段／平行关系及修订区分、普通角色采用快照与旧格式同身份提示词的一致处理、快照媒体独立持有、背景移除／显式刷新、复制关系、酒馆外部格式适配，以及原生创作后续编辑的执行默认值。未操作手机、未改版本号、未发布安装包。

### 检查点 5：当前卡片资料的阅读覆盖与分支记录

- 当前资料工具成功返回前，按真实来源、修订、字符范围和查看方法保存阅读记录；不以模型声称读过作为证据。目录预览和搜索片段单独记录，不计入全文覆盖，重叠片段不重复计数，不同修订不拼接。
- 阅读记录沿用现有数据库中的上下文记录载荷，不增加一套文件或全局卡片。重启后保留范围及原始预置来源；后续工具回合锚定自己的回复编号，当前请求可以汇总，但另一分支不能使用它后来读取的片段。
- 工具执行器使用本次请求开始时的真实用户消息编号，避免把阅读误记到后来排队的消息。返回的累计覆盖仅涉及当前工具已经获准返回的来源，不能通过历史覆盖查询暴露已停用的私有身份。
- 工具返回累计完整性，同时说明过去读过不代表全文仍在当前上下文中。原文修订改变后，持旧修订继续读取会拒绝，也不增加记录。
- 用户消息下的资料入口同时显示预置来源和查看来源；详情区分请求预置资料、本轮阅读／搜索／预览、各修订覆盖范围及预置时省略的资料。
- 纯逻辑覆盖检查、四项分支账本检查、真实数据库重载检查及真实资料工具联调检查通过；后续修订变化拒读补充检查通过。整组预览编译与回归通过：`tmp/novex-fast-check/2a73e29ba9e6-dirty-20260906T195929Z-94686-novex-domain.log`，用时约 5 分 42 秒。
- 本检查点针对当前采用的卡片资料；文件／文档的学习记录仍沿用已有流程，尚未统一成同一阅读展示。普通角色与背景的采用快照、版本关系、媒体快照和酒馆外部格式适配继续处理，不能因此宣称整轮更新已完成。

### 检查点 6：人物版本关系、复制与原生交换

- 数据库升级到 28，人生阶段和平行关系独立于人物正文及本体标记保存，并提供来源、目标和人物索引。本体仍是默认版本；既有其他版本不被迁移程序猜成人生阶段或平行分身。阶段顺序不能成环，同一对版本不能存在冲突关系。
- 人物详情先提供围绕当前版本的关系列表，可查看另一个版本、修改关系类型、移除关系；缺失目标明确显示。通用新增／删除入口改称版本，另存世界分身会明确建立与来源版本的平行关系，不同步后续知识或记忆。
- 原生角色卡导入导出保留关系及未知关系扩展，严格映射本次人物的版本编号；缺失目标保留来源编号并分配缺失占位，不根据库内重名或外部编号连接。导入角色另存分身后的重复来源编号在导出时独立映射。
- 整个人物复制保留内部阶段关系，并重连跨版本模块引用与带用途引用。删除原人物后，复制品内部引用仍然可用；另存单个世界分身仅建立明确平行来源，不把来源的整个人生阶段关系复制到分身。
- 管理工具支持设置／移除版本关系及查询合法类型，要求来源版本编辑权限；关系目录不返回目标正文，也不授予目标的读取或编辑权限。删除确认展示版本关系影响；读取引用期间取消按钮仍可使用。
- 八项真实数据库行为检查通过，涵盖重启、迁移、删除缺失、复制、原生往返、未知扩展、导入分身编号及工具权限；阶段循环规则检查通过。完整预览编译及回归通过：`tmp/novex-fast-check/05546e5182f1-dirty-20260906T203954Z-96919-novex-domain.log`，用时约 5 分 48 秒。
- 编辑修订记录仍需进一步完善；本检查点没有把编辑动作变成人生阶段。普通对话采用快照、显式刷新、媒体持有、酒馆外部格式、创作后续执行默认值和阅读入口统一继续处理。没有改版本号、发布安装包或操作手机。
