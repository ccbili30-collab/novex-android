# 卡片与对话工作区执行修复

用户已确认修复范围并授权自主执行。基线为预览测试版 35 对应代码；独立任务分支从 2d2a394 创建。保留现有卡片数据库、工作空间、权限与采用快照，第六版候选不启用。本任务不自动发布安装包。

## 已固定的产品规则

- 三张私有空卡是起点，不是上限；可以继续创建多张任意类型卡片，不覆盖已填写对象。
- 回答身份、背景采用和管理权限独立。多个世界和角色可作为背景，只有一个主要回答身份和一个活动文游；管理区可以管理多张文游。
- 对话私有卡片和文件是可操作目录；共享卡片按已授权范围管理，修改原件不会更新本局采用，明确刷新才更新。
- 使用读取保持私有扮演/玩家资料隔离，管理读取按模块范围开放；不得以简化工具为由绕过权限。
- 用户指定原生卡片时，以真实写入和回读核验交付，不能以一段未来计划结束为完成。

## 执行顺序与验证

1. **修正错误反馈与目录**：合法空参数不报截断；本对话私有空卡可读取，其他对话和未授权共享对象不可访问；目录标明归属、用途、权限、创建承接关系。
2. **简化内容操作**：提供直接创建、模块写入、排序和引用的结构化入口，内部复用已有管理提案、授权、事务和持久回执。普通私有创建自动承接空卡，追加创建不受三卡限制；共享写入给出可确认的具体计划。
3. **原文入卡**：从已解析文档的修订和章节/块范围复制正文，模型无需重复生成原文；保存与回读检查覆盖模块内容和顺序。
4. **多卡使用和管理入口**：复核已有两种用途入口，补齐明确的同时使用与管理路径；一个活动文游不限制管理文游数量。复验身份和采用不随管理变化。
5. **真实完成状态与过程折叠**：原生创建请求记录未完成、待确认、已写入/待核验和完成；工具过程整轮折叠，保留原始消息和展开入口，确认、失败及继续操作不可隐藏；不做无限自动重试。
6. **真实案例闭环**：使用用户导出包的原始文本及明确创建请求，运行实际模型并经同一原生操作实现保存/读取验证；记录原始调用及费用。不以提示词模拟或空容器成功替代。补充重开、同号重试、跨对话权限及多卡管理自动检查。

## 验收重点

- 47 章资料完整落入文游模块，不擅自创建世界或角色；明确请求多卡时能继续创建。
- 模块读写、排序与带用途引用正确，部分修改不清空未指定字段。
- 原文、模块顺序和引用可回读，重开后保留；中断与重试不重复创建。
- 回答身份、背景与管理对象分别生效；多张文游可管理但只能有一个活动局次。
- 手机对话保持单一主阅读面，过程默认收起，错误和确认可见，返回/关闭保留草稿与阅读位置。
- 不进行用户已排除的手机探索点击；真实手机布局、键盘、触摸和系统分享列为用户最终验收，不由源码测试冒充通过。

## 当前执行结果

六项已实施。完整云端检查通过（34f7155，运行 34124700491），多张相同卡片的补充领域检查通过（415799d，运行 34125329954）。原始案例已通过实际模型与原生数据库闭环，详情见下文。

- 合法空参数不再误报截断；本对话三张私有卡可直接读取，未授权的其他对话卡仍被拒绝。
- 常用入口为创建并填写卡片、写入模块、移动模块、设置卡片引用，复用原有事务、权限与持久回执。创建成功返回实际编号、模块目录和结构回读核验；初始三卡不是容量限制。
- 原文可按指定文档修订直接复制章节及篇首内容；部分解析不能冒充完整原文，带媒体的范围不能悄悄丢掉图片。
- 对话配置加入“同时用作背景并允许管理”；现有单独背景、管理、身份选择和活动文游入口保留。可管理多张文游，管理与背景改变不自行切换身份或启动游戏。
- 连续成功工具调用及其前置进度默认收起；失败、待确认、交互选项和最终正文留在主阅读面，打开过程详情不改变原始消息。
- 卡片状态来自实际回执，随所在消息保存并在重开后恢复；未完成可继续核对，已创建对象有直接打开入口。纯计划结束且无保存回执时最多补救一次，已有写入先核对。回答如何创建的说明性问题不会被该补救流程强制执行。
- 同一创建请求内的相同内容可以复用已完成回执；明确新建请求与多张相同卡片请求不受单卡限制。

回归运行器仅导出编译后的应用代码和测试依赖。原始压缩包、文档、模型请求与响应保留在本地 QA（质量验证）目录，密钥只经模型接口保险库在内存中注入。测试调用共用实际卡片服务、文档服务与 Room（数据库访问库），不另写模拟保存器；接口采用非流式传输，不把该验证称作手机界面或完整流式网络验收。

## 原始案例验证

使用原压缩包里的《超高自由度真实西幻人生模拟器.txt》解析快照与用户两条既有要求，在隔离测试对话中运行 deepseek-v4-flash（深度求索第四代快速版），关闭思考模式。保留导出时的正式提示词主体，更新本次产品工具指南和隔离对话真实目录；未使用第六版候选。此回归只提供实际实现的卡片、文档和选项工具，采用非流式接口；它验证共用原生操作，不冒充完整手机环境原样重放。

实际过程为五次模型响应、四次工具调用：检查文档、读取文档、查看本对话空白文游卡、创建并填写文游卡。没有空提案、权限拒绝、重复写入或补救调用。读取结果确认 1,154 个块、19,993 个已解析字符完整覆盖，未截断。创建使用来源引用和修订，未让模型重新输出全部正文。

真实数据库得到一张文游卡，含篇首资料和 47 章，共 48 个模块。原生测试逐块比对所有正文，关闭数据库重开后再次读取通过；世界卡与角色卡仍为空，回答身份仍是诺瓦，未启动文游。源码及运行环境来自 34f7155，后续提交只补充测试、说明和详情文字。

模型接口记录为输入 120,692 令牌、输出 1,585 令牌（含多轮重复上下文；原始服务用量保留）。测试编排首次读取完成文件时撞上尚未写完的尾部，原生测试本身已通过；读取端增加等待完整 JSON（结构化数据）的处理后，直接重新核对已完成结果，没有为此重新调用模型。

本地证据目录：`local/qa/card-workspace-execution-20260907/original-case/`，包含原始调用、工具回执、完整阅读范围、数据库、关闭重开验证和结果汇总；用户正文不提交远程仓库。云端检查不包括真实手机触摸、键盘及布局验收。本次不打包发布。
