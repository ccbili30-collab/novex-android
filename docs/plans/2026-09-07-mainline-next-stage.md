# 下一阶段主线实施记录

执行依据：用户提供的 2026-09-07 主线执行交接。目标为预览线，正式提示词保持当前版本，第六版仍未验收；本任务不包含发布、改版本号或合并稳定线。

## 任务 A：基线已执行，能力未通过

源码基线 a046322，测试版 34。复用生产核心解析、文件仓库、学习协调和工具执行，准备 18,015、100,038、1,000,027 字符合成资料；仅最小规模发送模型。真实安卓完整请求、页面与手机没有执行。

最小规模 11 个来源、1 个重复、10 个独立文档；解析正文 17,630 字符，279 个可读块，1 个未解析图片附录。固定当前专用学习提示词，独立答案表 35 个事实、11 个查询不发送模型。

60 次深度求索第四代快速模型请求证明：模型返回后笔记提交前失败会遗漏 358 词元并重复请求；长章节输出达到长度限制仍记为整理完成；笔记新增围巾借出时段并传播到后续答案，总览还出现来源错归。不能用 279/279 的覆盖率宣称整理质量通过。实际费用按提供商用量保守估算约 0.1934 美元，非账单金额。

返回正文前、保存前、保存后界面更新前三处中断都有真实文件恢复记录。实际源文档副本修改后，新文档拒绝旧游标，旧笔记不补齐新内容覆盖；解析修订的旧笔记回查仍有缺口。

原始证据在开发区 local/qa/mainline-next-stage-20260907/ 下，旧运行不覆盖。A_BASELINE_REPORT.md 为完整报告，A_SEMANTIC_REVIEW.json 为逐项核对。

## 任务 B 第一批：模型返回与笔记提交分开保存

- 复用现有学习仓库，为模型响应增加独立文件回执，保留原始正文、提供商结束原因、实际或估算用量和请求关联。笔记主文件提交失败后，重开会计入回执用量；恢复同一请求复用正常结束的响应，不重复请求。计入标记与任务一起保存，避免重复计费。
- 学习状态升级第九版，仍可读第一至第八版。文件先同步写临时文件，再原子替换；移除原子重命名失败后直接覆盖主文件的回退。
- 截断、空正文和缺少正常结束标志的响应不推进笔记或已整理覆盖；保留失败响应、用量及停止原因。用户选择继续才重新处理未完成批次。
- 较大窗口为输出预留最多 4,096 词元；小窗口按比例保留输出空间。预检排除尚不可读的图片附录，保持与实际通读范围一致。
- 现有学习任务和错误入口增加“查看最近返回”，详情说明输出是否正常结束、用量及尚未核验；关闭回到原任务，读取不会重试模型。重开恢复停止原因，切换对话关闭旧详情。

同一小规模离线中断复验通过。真实复验的学习部分共 11 次请求，输入 20,356、输出 10,515 词元与保存值一致；保存前中断后未重复调用已返回批次。查询仍有未完成项，未宣布理解质量通过。新旧真实运行合计 122 次请求，保守费用约 0.4026 美元，仍在 1 美元开发测试上限内。

本地核心测试与共享界面组件边界检查通过；安卓整包编译/测试和手机交互需要各自验证，不能相互替代。当前详情复用已有安全区域、键盘避让和滚动容器，没有修改聊天输入、键盘提交或滚动跟随规则。最终手机实际体验由用户验收。

### 第一批的明确限制

回执解决“已保存模型返回后，笔记提交失败”的恢复。存储整体不可写、提供商已计费但响应未到达本机、跨进程同时写同一任务，以及断电时目录元数据持久化，尚未证明；不能声称任何时点绝不丢用量。正常结束标志也不证明内容完整或语义准确。达到预算而尚有待提交响应时仍需核对现有预算控制入口。

## 后续依赖仍待执行

B：来源解析修订、范围与旧笔记恢复；章节/主题索引与成果入口；换模型新预检及用量继承；十万、百万资料实测。小规模语义错误保留，不删题、不换模型覆盖旧记录。

C：按已知样本明确存档修复范围，保留自动数值，接原始事件与恢复读取、旧格式兼容和详情。

D：正式对照/候选装配追踪、真实管理执行与回读/续接，不直接启用第六版。

E：作品多重分组和三库筛选、对话使用/管理/身份的前端闭环。

F：复制、平行路线、世界与文游修订、依赖交换、酒馆最小世界书触发。

G：依赖完成后做混合对话与扩展评测；预览验收不自动发布。

## 用户追加任务 H：预览版对话包导出

用户原话：“附加任务：做完以后增加一个导出对话包，要求原话原环境导出，就在对话菜单里面。这是预览版的测试功能”。排在既定主线之后，不替换 A—G。

入口放对话菜单，仅预览版开放。按实际保存数据导出原始消息、分支、附件，以及当时回答身份、采用的卡片快照、活动文游/玩家身份/本局状态、工作区与工具配置，保存可追溯编号、版本和缺失项，便于复现与排查。原话不以摘要替代；运行中的任务按导出时点说明完成/待处理状态。模型连接保留非密钥配置，应用保存的密钥不进入包。先核对现有导出与依赖包能力，再复用扩展；实际范围、格式和验证记录在实施时补齐。

## 任务 B 第二批进行中：精确来源修订

第一批提交 e80db00 已通过云端完整安卓单元测试和编译，运行 34083106257；没有构建安装包或发布。

普通阅读覆盖已经按来源修订分组，继续复用。新增保存旧解析正文并按精确修订回查，学习预检固定采用的解析，章节笔记和综合笔记传递来源修订；工具通过可选来源修订参数读取旧正文，继续沿用对话访问权限。来源修订缺失时不能用新解析替代，旧笔记没有修订记录时明确标为未知。

严格测试补正了任务 A 的一个归因：首轮旧游标拒绝来自位置越界，并未证明解析隔离。新测试保留合法偏移，复现第一版游标会跨解析续读。现将游标升级第二版，绑定完整文档校验值和正文解析修订；原记录保留，详细报告追加补正。旧游标不能推定所属解析，用户可按已返回位置重新定位。

第二批尚在工作树验证，未标为整体任务 B 完成。旧任务缺少修订依据的重新确认入口、模型交接、完整笔记/成果前端和大规模验证仍待后续完成。


## 任务 B 第三批：原生续接与历史成果

第二批 fc5b6dc 已通过云端完整安卓测试和编译，运行 34084697042；没有安装包或发布。

在现有资料学习任务中增加“资料与整理计划”入口，复用共享滚动弹层和搜索列表，查看当前章节/总览、历史笔记、来源缺失、历次模型及用量；最近一次原始返回保留为详情内的次级入口。查看本身不调用模型，详情为打开时的保存快照。

暂停后可按当前模型准备续接：保存旧模型记录、笔记、整理覆盖和累计用量；新模型、窗口、剩余范围与累计预算重新预检并由原生确认。旧任务和费用没有重置。原进度、用量或解析在确认前变化时重新出示计划。已返回但未提交的批次在换模型后可能重新请求，此前原始返回与用量仍保留，并在确认中说明。

旧任务缺少解析修订，或需要采用新解析时，提供“重新核对来源”。预检阶段不改原记录，确认后将旧笔记保留在历史成果并重新计算当前解析覆盖。当前与历史笔记也可通过同一个只读工具分别分页查询，历史结果明确不计当前覆盖，继续受资料集访问范围限制。

真实文件测试覆盖：第一模型保存一批后中断，关闭重开，第二模型仅处理其余两批；累计用量准确合并，旧成果与模型记录再次重开可读；未知修订旧笔记重新核对不冒充新覆盖；迟到响应带来的用量变化使旧确认失效。本地核心 120 项（1 项需显式启用的真实模型基线跳过）无失败，共享组件边界无违规。第三批安卓云端编译和真实双模型交接仍待执行，不把受控提供商测试说成真实模型已通过。

仍待：工作区中的原文与成果定位、主题索引、来源局部失效优化、大规模通读和语义验收；最终手机交互由用户验收。本轮不改正式提示词，不启用第六版，不发布。

## 任务 B 第四批：成果定位与真实交接复验

第三批 da46959 已通过云端完整安卓测试和编译，运行 34085326112。两种真实模型交接先遇到截断，随后又在调用前遇到剩余词元预算不足；两个现场分别保存，没有把它们计作通过。补足较小窗口的输出预留，通过原生续接确认增加测试词元预算后，从原保存现场完成剩余资料：输入 30,858、输出 18,215 词元与持久值一致，279 个可读块完成，图片附录仍标为部分失败，没有重复处理已保存范围。此项证明存储和交接，不证明语义质量。小规模全部运行累计 137 次请求，保守费用估算约 0.514 美元，原 1 美元上限未突破。

现在从对话菜单的“资料整理进度”可以重新找到本分支已保存的资料集，包括已结束任务，继续使用已有详情、搜索和滚动组件。“本对话文件”的实际导航已经是创作成果库，沿用此入口，不另造文件页。

学习笔记提交后向既有工作区生成可重建文件投影：原附件、精确解析正文、章节笔记、总览、历史笔记与按原文标题整理的主题索引。生成目录对模型只读，写入跟随原附件消息的分支，不落到全对话根目录；成果库沿用已有登记和修订机制。笔记仓库是实际进度依据，文件投影失败不会丢失已经提交的笔记，打开文件入口可重新登记。原附件被覆盖时保留已有归档原件并说明变化，不用新文件替换旧来源。

重新核对来源进一步缩小到受影响部分：解析未变且版本依据完整的笔记与覆盖继续保留，变化或版本未知的来源及依赖它的综合笔记转为历史成果。真实文件测试覆盖重开读取、重复生成不增加独立文件、旧解析定位、附件覆盖与分支隔离；局部变化测试验证不重新处理无关文档。本地核心 122 项（1 项真实模型入口按默认跳过）无失败，组件边界无违规。第四批安卓编译与手机验收另行记录。

十万字和百万字已完成离线预检：18 批与 105 批通读。用户回答“随便烧”，允许按所提两项合计最多 5 美元执行；独立记账，先十万字，再百万字，固定原事实/查询门槛。原正式提示词不改，第六版仍未启用；大规模真实结果尚未完成。

### 第四批云端结果与十万字现场

4796923 已通过云端完整安卓检查，运行 34087415617；没有构建或发布安装包。十万字首轮在 625 / 1,429 个可读块后遇到回答截断，11 份笔记和所有已返回的用量均保留。续跑一次接口结果不明，传输账本保留预算占用；下一次仍在同一未完成批次截断，未重复已保存范围。原目录和失败原始响应全部保留，没有宣称语义通过。

新增测试驱动的真实现场续跑入口，仍经原生续接预检和确认，保存来源文件校验值、模型窗口变更、前后累计用量及旧笔记保留情况。下一次采用 16,384 词元测试窗口以缩小输入批次；与原 32,768 窗口分开记载，质量口径不变，合计 5 美元预算不变。正式提示词未改。

资料详情同时显示本分支的对话工具阅读字符覆盖与学习整理块覆盖。读取已有上下文凭据，按文档及解析修订区分；其他分支、搜索、目录预览和旧修订不能补齐当前正文阅读。查看详情不请求模型，退出或切换对话清理旧视图。安卓编译和最终手机体验需分别记录。

## 任务 E 页面状态与返回路径草图（实施前）

核对后确认当前作品多对多分组尚不存在；文游项目、世界角色关系、成果库筛选都不能冒充作品归类。作品只收纳稳定内容编号，角色收纳具体版本；同一角色根行在任一所属版本命中时显示，归类不改正文、回答身份或局次。

三库根页始终显示同一个作品筛选摘要：全部 / 未归类 / 某作品。选择摘要打开作品选择面板，新建和管理位于该面板内；管理下钻作品详情，再进入跨三库的批量成员选择。系统返回逐级回到管理、选择、原根页；更名输入与成员勾选在提交前保留，取消不写。共享筛选持久保存，切库与重开不丢，分组删除仅移除成员关系。

空作品页面保留作品名称，并提供清除筛选、加入已有和新建入口，不能显示成全库为空。点击卡片仍到现有详情；成员更改和卡片删除分开。后续“用于对话”列出具体选择，连接现有四组对话配置；使用与管理的反向对话索引保持独立，作品相关对话由这些关系汇总而来。尚未实施的入口不能提前标完成。

布局复用现有选择面板、内容弹层、搜索与滚动容器，不重写键盘或安全区域。手机输入法、返回、横竖屏与长列表触摸仍由用户实际验收。

### E 第一批实现，仍非完整流程验收

新增最小作品分组、成员关系和共享筛选记录；数据库从第 29 版迁移到第 30 版。角色成员精确到本体或分身的具体编号，同名作品也保持独立编号。成员替换采用事务并校验打开时的成员集合，过期页面不能覆盖新归类；新加入的目标已经删除则拒绝。解散分组只级联删除收录关系，选中的分组被解散后恢复全部。

世界、角色、文游根页共用持久筛选；选择面板提供全部、未归类、新建和二级管理。管理支持更名和跨三库批量勾选，取消不提交；不可用的旧收录明确列出，可移出。空筛选保留作品名称，并提供加入已有、清除筛选和新建入口；新建后目前需从作品管理归类，尚未自动收录。原卡详情、手动排序和四个平级根页保留。

真实文件数据库测试新增：迁移与重开、两作品共用版本、同名不合并、移出/解散保留原件、过期写入拒绝、已删除的新目标拒绝。编译和这些安卓测试等待云端结果。尚待“用于对话”选择、四组配置中的开关与来源、使用/管理反向对话列表；不能把第一批分组视为任务 E 完成。

### B 十万字续跑的下一阻塞

缩小窗口后全部 1,429 个可读块完成，29 份笔记保留；累计已返回用量为输入 95,305、输出 63,613 词元，与保存值一致，未重复此前保存范围。但阶段综合要求最多 2,293 字符，模型实际返回 2,623 字符，按现有保护规则暂停，未截断后强行入库。语义初查也看到来源范围泛化，尚未通过质量门槛。全部规模调用到此 37 次，保守费用约 0.1570 美元，含一次状态不明的预算占用；它不是提供商账单。百万字尚未发起，继续保留十万字综合阻塞，先推进交接允许独立进行的 E。

### E 第二批：从作品进入对话配置，再从卡片找回对话

第一批 30d37e3 通过云端完整安卓检查，运行 34090069993；此前 B 的阅读区分 5a84c63 通过运行 34089445138。两次均无安装包或发布。

作品管理增加“用于已有对话”，按最近顺序搜索对话并进入其原有配置页。两个实际首页宿主都接到该入口。选中的作品只限制候选卡片范围；回答身份、背景、活动文游和管理对象依旧逐项选择并按原有保存流程采用，不自动全选，也不更改原件。对话配置内可以切换候选作品范围，卡片选择增加搜索；文游明确区分启动与只读资料挂载，后者使用既有只读管理权限，不启动、不改身份。

世界、具体角色版本和文游详情共用“使用它的对话 / 创作管理它的对话”反向入口。索引投影来自实际保存配置、仍有效的采用快照，以及已公开创作卡的来源对话；不将管理对象变成背景，也不从原卡的最新引用反推旧对话。旧会话没有新配置时沿用已保存世界和角色版本编号。作品的相关对话由成员的使用/管理关系汇总，仍可关联多个作品。

此批仍待云端编译和真实文件数据库反向索引检查；手机体验另验。当前索引表达正在采用或管理/创建的关系，并非所有历史局次全文检索。父子开关、来源合并/不同修订、根对话页按卡片查找等剩余项继续推进，任务 E 尚未完成。

十万字在同一窗口受控重试综合：完成一份阶段笔记后下一份仍未按目标缩短；累计 30 份笔记、输入 100,950、输出 65,966 词元均保存。旧记录不覆盖，未重复可读原文。由此不能把偶然一次成功视为全过程通过，百万字仍待综合链路和语义门槛处理。

### E 第三批：不同采用修订不丢失

第二批 f8bac62 已通过云端完整安卓检查，运行 34091047645。核对发现旧上下文按同一个来源编号只保留先到正文；现在同编号同正文合并用途，不同正文分别产生带完整内容校验值的来源编号，原文读取与搜索共用此规则。旧编号无指定修订且存在多个版本时拒绝猜测，按目录或精确修订读取。

文游捕获按采用路径保存直接背景和文游配套资料，避免原卡更新后启动文游覆盖先前直接采用的旧正文。图片按实际校验值区分；配置页增加实际采用来源、修订和用途的只读说明，直接移除后仍有文游来源时明示。真实数据库测试覆盖旧背景、新文游并存、重开、精确回读与只移除直接关系；此批云端编译和测试待验证。父子开关与根对话页查卡仍待执行。

十万字对已保存成果的原定 11 个查询全部返回，但语义未通过。甲本原文有辰时记载，首批通读笔记却称正文空白，错误继续进入综合和 Q04；Q02、Q05、Q09、Q10 还存在范围、程序或关系的过度推断。完整人工复核记录在开发区 B_100K_QUERY_REVIEW.md，不以覆盖率或正常结束代替质量。规模累计 86 次传输尝试，保守费用约 0.4581 美元，含一次未知结果占用；百万字尚未发起。

### E 第四批：设定与模块开关，等待验证

第三批 8083934 云端编译通过，但完整回归发现一项失败：明确刷新直接背景后，文游里保留的同一路径旧副本仍进入请求。第四批把同一路径的新采用优先于该路径的旧副本，其他独立文游来源继续保留；保留原失败检查，不删测试。

开关页面从对话配置进入设定列表，再下钻卡片模块，返回逐级回原配置。关闭项在原父级底部显示；父级关闭保留各模块选择，重新开启不重置子项。直接关系仍在配置页单独移除；开关停用本对话各背景路径中的对应对象或模块，不修改回答身份、管理权限和原卡。

新采用同时保存引用路径，文游自带路径与直接采用路径分开；读取、搜索、图片和反向使用索引共用过滤结果，关闭路径不从可变原件补回。其他根路径仍能到达同一配套对象时保留该独立用途；明确关闭对象则各背景路径都停用。旧快照缺少路径时拒绝猜测模块依赖，要求明确刷新后再处理，整项直接背景仍可关闭。规则缺失会进入本轮必要说明，不自动重新开启或结束文游。

增加真实数据库重开测试：地理模块关闭、世界关闭再开启、循环引用、另一条直接来源、独立角色身份与管理权限、关闭内容不可搜索或读取、保留图片的可见范围。沿用既有弹层、搜索列表和开关，不改输入法或聊天滚动；云端结果和手机验收另记。

两个实际对话根页增加共享“按卡片查找”入口：先选使用或创作管理，再搜索世界、具体角色版本、文游，最后搜索并打开相关对话。按需读取目录，载入中不把原卡误标为缺失；卡片已不可用但仍有关系时保留编号。现有最近列表、手动排序和分组滚动位置算法保留，对话不迁入作品目录。此入口待云端编译；实际手机返回和搜索输入由用户验收。

第四批 cbefc97 通过完整安卓云端检查 34093517208，包含原第三批刷新回归和新增重开/开关/图片范围测试；没有安装包或发布。根页入口 537bb49 单独送云端检查。

## 任务 C 第一批：存档保留原始依据，后续请求实际读取

按交接确定的修复范围保留自动本局数值，不启用第六版，也不新增模型抽取事实库。正式存档升级第二版：保留当前分支已落库消息原载荷、消息编号/角色/父子关系/时间与修订、本局编号、采用配置和缺失消息编号。正在执行的回复尚未落库时明确列为缺项；不把内存摘要冒充原话。模型摘要和补充状态标为未核验辅助整理。旧第一版原载荷保留，缺少依据不能推定已核验。

保存操作使用对话、写入分支和工具调用的稳定编号；相同操作重试读取已写对象，不更新原数值和时间，内容不一致则拒绝覆盖。同进程写入串行，通用工作区写入、编辑和受限计算不能覆盖或伪造正式存档路径。未证明跨进程同时写入和整个文件系统故障下的恢复，不声称任何情况下绝不丢失。

实际请求装配读取当前局次、当前分支的最近兼容存档，核对文件和原始消息修订，携带末尾原始消息有界摘录、未返回范围和原文件入口。模型摘要不直接作为确定前情装入请求；仍可从同一保存文件明确回查辅助整理。当前软件数值继续使用实时本局状态，不自动回滚到历史数值。无法读取的较新存档会显示缺失；没有本局编号的旧记录不自动采用其前情，即使快照编号相同也不能推定属于同一局。

对话菜单增加“存档与原始依据”，依次查看名称时间、保存时软件状态、未核验摘要/补充状态，以及具体原始消息；查看不调用模型、不推进剧情。此批是保存与后续请求的依据读取闭环，尚未新增任意历史存档的一键数值/剧情回滚，不将它写成完整恢复功能已验收。

测试增加真实文件重开、同操作重试、另一局/兄弟分支隔离、原件后改仍保留存档原话、数据库实际活动路径捕获、消息缺失、旧载荷保留和修订损坏拒绝。四组原话覆盖锁门/来客、倒水、围巾借还和傍晚；错误模型摘要不会被直接装成确定前情。核心工作区测试通过，安卓数据库/请求接入编译待云端验证，真实模型长期恢复语义测试及手机验收尚未完成。
