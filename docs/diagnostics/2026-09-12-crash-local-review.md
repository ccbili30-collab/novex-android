# 2026-09-12 15:53 崩溃日志与本地复查

## 原始证据

用户补充：输出结束本身未必触发，仔细回看回答时出现退出。原包保留在下载目录，不将原日志提交。

4 份 Java/Kotlin（应用运行层）异常日志：9 月 11 日 21:09:29（候选十二）、9 月 12 日 02:01:14（候选十三）、10:55:44 与 10:57:52（候选十五），均为 ForegroundServiceDidNotStartInTimeException（前台服务未及时登记），对象 AgentForegroundService（智能体后台服务）。这直接证明这些时间点是系统因服务超时终止进程，不是容量提示主动返回列表。设备 Redmi（红米），安卓 16。

4 份原生崩溃记录：06:57:35、07:16:38、07:44:42、15:52:55。全部是 SIGABRT（主动中止信号），由本进程发出。15:52:55 是最新反馈附近的直接崩溃证据；文件只有信号、进程与线程，未包含中止原因和原生调用栈。不能认定与服务超时同根，也不能据“回看时发生”猜定排版或内存根因。

## 已完成的本地工作

- 核对当前服务与候选十六开工基线，已经存在 onCreate（创建回调）中先登记前台、再启动附加功能的实现及对应测试。本轮没有冒充重新修复该处，也未因旧日志盲目重写服务。
- 新增 ProcessExitEvidence（进程退出证据）：主界面创建时，后台查询本应用最近 8 次系统退出原因；尽可能保留系统原始栈文件。保存原字节，每份最多 4 MiB，报告注明限制；最多保留 32 个相关文件。不上传。
- 页面导航／进入／离开、活动创建／销毁／结束、生成状态变化、服务创建／登记／命令均有专用本地记录。事件记录与系统栈读取分别使用后台线程，不把正文、密钥或工具参数写入事件日志。事件文件轮换，界面不因记录结果导航。
- 系统栈不存在或读取失败不影响业务；使用独立临时文件后改名，避免把半写入文件当完成。日志后缀与现有日志管理导出机制兼容。
- 发送链缓存补入容量和分页权限，避免同一请求修改配置后沿用旧选料结果。

系统接口依据：https://developer.android.com/reference/android/app/ApplicationExitInfo 。该接口可能不提供栈，不能承诺任何设备都能拿到完整原因。

## 本轮代码复查覆盖与边界

已回查：发送异常归属（内存与数据库）、选料恢复（正常空选择与异常分开）、真实正文预算和续读、缓存失效、正式内容／管理草稿读取边界、交换包分装与旧版读入路径、拖动坐标与模块递归展示、对话条目缓存、导航与后台服务调用关系。当前检查未证明用户手机上的所有表现均正确；尤其原生主动中止仍未定位。

新增诊断类使用本机缓存的 android.jar（安卓接口文件）在隔离构建中编译；原有内容存储 72 项与对话运行 99 项检查仍作为核心回归。应用主界面、完整服务与安卓页面没有在此隔离构建中编译，不把该结果写成整体安卓通过。

下一步必须完成整体安卓编译、设备读取／拖动／导航验收，并取得最新原生中止的系统栈。未发布候选十六，未改稳定通道。

原包 SHA-256（散列）：`5ccc0385971fd3ce7d98df0060179bd33b76b4b4ddd3a4bb4c672c446d801bbd`。
