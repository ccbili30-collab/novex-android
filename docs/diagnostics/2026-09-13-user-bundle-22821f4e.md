# 对话包 22821f4e：只发送 1 后失败

只读诊断；不修改业务、用户配置或发布。

- 候选十六，专用内测；配置记录中的模型 gemini-3.8-flash（不能据此证明实际发送目标），提供商显示名 gmn。包不含提供商地址和协议配置，不能证明与本机钥匙串入口完全相同。
- 唯一消息为用户文本 1，无助手持久记录、无历史工具结果。
- 模型 supportsTools=false；两次实际装配记录 toolDefinitions=[]。工具关闭已生效。
- 世界、角色、管理对象、采用资料均为空。启用上下文 64000，模型登记上限 1048576。
- 用户保存的对话要求 4828 字符，实际 formalPrompt 6346 字符。未将其中内容视为诊断指令，也未向模型重放该创作指令。不能称实际请求只有一个字符。
- 两次装配相隔 3265 毫秒，stage=assembled_before_provider。该标记仅证明完成应用装配，不能证明网络请求已发送，更不能用 candidateSent=false 推断主请求未发送（该字段描述影子候选提示词）。
- 包无网络错误、响应或失败详情，因此无法判断发送前失败和服务响应失败。
- setInlineError 在没有助手持久行时只设置内存错误，或尝试更新不存在的助手行，不插入本轮失败记录。这是本包缺乏首轮错误证据的代码缺口；不是已证明的调用失败根因。
- 本机已保存 Gemini 中转入口，用同型号、不带工具、用户消息 1 测试：非流式 HTTP 200，4.4 秒；流式 HTTP 200，3.9 秒，有正文及结束标志。没有重放原用户保存的自定义提示词，因此不是现场等价复现。

结论：本次排除工具仍开启及卡片注入；尚未定位现场失败原因。需要该次运行日志或界面具体错误补齐发送边界。先前工具响应不稳定不能套作本包根因。
