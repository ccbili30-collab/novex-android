# 上下文容量、超限阻塞与输入栏用量修复

用户确认范围：2026-09-11 本轮处理容量识别与超限阻塞，以及输入栏上下文圆环。使用钥匙串中登记模型进行实际请求；不公开发布，不改稳定工作树，不修改用户原卡或密钥。

## 已核实的断点

用户对话包来自开发候选十。服务端报错明确上限 1,048,576 词元，请求输入 1,069,387 词元。装配记录一个背景模块本地估算 650,432 词元。旧计数为字符数除以三；截图 3% 读取上一次成功调用的 31,126 词元。恢复器在完整错误正文搜索任意 5 开头三位数字，将上限 1048576 中的 576 错当成服务器错误，触发重复请求。

模型设置页把已拉取的模型对象变成名称列表后，保存时重新创建无容量模型；所有选择的模型又被标为自定义，后台据此跳过更新。官方深度求索列表实际返回 deepseek-flash（深度求索快速版），旧规则不认识该别名。三个问题相互独立，不能只改型号表。

## 本轮职责与变化

- 计数与发送：本地计数明确为保守估算，区分连续英文数字、分隔符与中文等文本；完整发送历史、系统资料和工具定义统一计入，在模型输出之外另预留 5% 估算余量。大卡继续通过既有共用分页器分配预算；原文不截断落盘，必带及只读超额仍明确停止。
- 请求恢复：只识别带明确状态标签的状态码。上下文超限及明确请求参数错误不原样重试，也不原样切到其他模型。错误保留原始服务说明；可解析到本次输入词元时更新实际用量。
- 模型元数据：拉取对象贯通保存，当前服务地址与凭据变化后不沿用旧接口拉取的容量。后台对自定义入口只更新匹配模型的容量，不新增模型、不改分组、不覆盖用户设置。已核实的官方别名和容量修补到基础记录，用户覆盖字段继续优先；未知容量可在已有上下文面板中校正或恢复自动识别。
- 圆环：完整细圈代表模型上限，灰色粗弧代表启用容量，黑色进度代表已用容量（深色主题使用反色保证可读）。只有圆心文字在百分比与词元数之间切换；百分比以启用容量为分母。预计用量显示约等号；未完成装配不显示旧请求数字；超过容量可以显示超过 100% 的文字。
- 界面沿用 48 逻辑像素触摸区及现有底部面板；容量校正为面板内次级对话框，取消不保存，运行中不能更改。

## 实际接口检查

见[逐模型记录](2026-09-11-context-model-probes.md)。70 个对话模型入口完成长请求，其中 27 个成功接收超过 14 万词元；其余 43 个入口补做短请求也未成功。均使用人工生成的内容，不使用用户原包。成功仅证明该长度请求可接收，不等于测出物理上限或证明模型理解能力。

不能宣称所有模型元数据都已自动取得：本次七个登记凭据返回的模型列表均没有直接容量字段。官方资料、型号估算、用户覆盖和服务端返回值必须区分；未知别名仍需校正，不能把未知当成确定 128K（12.8 万词元）。

## 检查与交付状态

首轮编译失败：模型设置保存处可空基础地址与辅助函数参数类型不匹配；已修正。

第二轮应用 1,474 项检查中有两项失败：旧容量断言未跟随已确认的自定义官方入口修正；上下文面板直接引用底层页面组件，违反现有组件体系。分别修正断言和组件引用。第三轮 1,476 项检查中仅后者仍失败，因为该轮上传发生在本地组件修正前。三份失败日志保留；不把中间生成的安装包交付。

最终包含临时模型的第四轮编译和检查成功：应用 1,479 项、内容存储 71 项、对话运行 94 项，共 1,644 项，0 失败、0 错误、3 项跳过。新增临时配置隔离检查均通过。完成后复查请求路径、配置保护入口和取数路径，没有再修改代码；本地及构建机 1,431 个源码文件逐项散列一致。

私人验收包：`3.0.0-dev.12`（开发候选十二），版本码 `300000012`，应用标识 `com.noven.player.preview`。签名证书散列 `cab4226b416183671281253b5f4000a28885cfa4f48146c0e7798d137e41c6a6`，与已有预览包一致；非可调试构建。安装包 59,929,818 字节，SHA-256（安全散列算法）`b24e79f76386d4ebe0833d17c3370e468811f4123270f6638d72877ffdea45ff`，本地与构建机一致。

交付路径：`/Users/noven/Downloads/Novex-预览测试版-3.0.0-dev.12.apk`。实际检查了临时凭据已包含于这个私人包，源码归档和构建日志均不含凭据。未公开发布；稳定工作树保持干净、未修改。未进行手机点击验收或宣称用户原始对话已实际恢复。

本地证据目录：local/qa/context-repair-20260911/。包含改动前源码、逐文件散列、接口元数据、长短请求对照及构建记录；不进入版本库。

## 用户手机验收

1. 覆盖安装后打开模型容量面板：深度求索官方快速版与专业版应可调到百万；校正一个未知型号的上限，关闭再打开仍保留，留空保存可恢复自动识别。已有用户设置不应被后台刷新覆盖。
2. 启用容量设为模型上限的一半：灰弧应为半圈。点击圆心只切换百分比与词元数，外圈不变化。预计用量有约等号，服务端返回统计后显示实际用量。
3. 继续原大卡对话：自动采用的大模块应按预算读取并保留后续分页入口；不能为了能发送而删除原卡内容。查看是否能继续正常交互和管理。
4. 如果服务端明确报超限：应停止等待并显示原因，不能继续重复同一请求；输入、原卡和已完成操作保留。手机手感与真实用户卡体验仍由用户验收。

5. 在对话顶部打开模型选择，选择“DeepSeek API（深度求索接口）”，发送一条消息；原有自建服务商和默认分组应保持不变。设置中不可编辑或导出这个测试配置。

## 独立临时免费模型

用户本轮额外授权在私人测试版加入 `DeepSeek API`（深度求索接口），实际模型标识为 `deepseek-flash`（快速版）。单独的服务商、模型与分组编号，不按名称合并，不覆盖用户已有深度求索配置，也不更改原默认分组。用户原配置和自行新增深度求索的能力保留。

密钥通过苹果钥匙串读入构建进程内存，仅注入预览渠道的私人测试构建；源码默认值为空。设置页面不展示地址、密钥、编辑和导出入口；仓库层拒绝修改、删除及导出该保留配置。模型分组和单模型编辑的直接路由同样只展示不可修改说明。对话仍可直接选择，也可调整本轮启用容量。

不以本地定时器自动删除配置；用户通知移除后停止注入，后续无密钥构建会清理这一个保留配置及其引用，保留所有用户服务商与模型。安装包内携带临时凭据属于用户本轮明确授权的测试交付方式；界面隐藏不等同于安装包无法被提取。

实际请求：新临时凭据短请求返回 200（成功），回答“连接成功”。使用大卡分页检查生成的同一份无用户内容的中文材料，实际请求 1,272,510 字节，服务端统计输入 339,250 词元，6.43 秒返回 200（成功），回答“好”。只证明该实际请求通过，不宣称完整百万输入、真实用户大卡或手机操作已经验收。
