# 3.0 执行记录

当前处理状态统一见[3.0 故障表](3.0-bug-board.md)：修完移出待处理表，新增问题补入；下文历史状态保留用于追溯。

最新授权：完成已确认更新与必要验收后直接打包发布，随后报告延期／未定案／未完成项。

## B01 历史执行回执已收尾

后续验收记录（2026-09-09）：

- B06／B07 已收尾：`stream-drag-order-green-3-build.log` 构建通过（1 分 15 秒），相关 13 项检查无失败，原件 `stream-drag-order-unit-results.tar.gz`；后续测试编号修正构建 `stream-canonical-entry-build.log` 通过（1 分 8 秒）。`stream-canonical-entry-native.log` 独立安卓 14 四项全通过（115.367 秒）：普通流式正文中途可见与结束底部跟读、消息发出后立即上翻、正文出现后再上翻、真实工具过程收拢与失败可访问。两种上翻均在最终持久化后保持历史位置。设备原件 `stream-canonical-entry-device.tar`，已查看历史和末尾截图。
- B08 排除为测试编号错误：`stream-binding-diagnostic.json` 中测试保存随机临时编号，`entry_exists=false`，实际模型为另一个旧测试项；模型仓库保存时会规范化编号，旧夹具未回读。修正后设备 `binding-diagnostic.json` 中期望、实际、持久绑定一致，`entry_exists=true` 且地址一致。没有为这一测试问题改生产模型选择逻辑，也没有把它宣称为修复用户端绑定缺陷。测试失败、连接失败和两次编译配置错误均保留；本轮只以后续规范编号的四项运行证明实际流式行为。


- B07 已加入候选修正：保存后的发送定位携带最初请求时间；如果用户其后已拖动，不再开启跟随；用户拖动取消待定位消息，下一帧执行前再次检查拖动是否发生。理由是同步生成状态早于异步保存回执，原代码无条件接受较晚到达的定位。此段尚待新构建和手机验证，不能宣布关闭。
- `stream-history-diagnostic-native.log` 中“正文已出现后手动上翻”通过，另一项运行未正常收尾，被主动停止，不能将该进程退出归为产品崩溃。单独运行 `stream-history-early-alone-native.log` 实际以 60 秒等待回复超时结束（102.589 秒），停止命令到达前已记录失败；`stream-history-sends-logcat.txt` 显示对应发送遇到本地端口连接失败。此项不是滚动成功／失败证据，新增 B08 待查配置恢复与请求地址。滚动测试补上等待自己的会话配置恢复及绑定编号核对，记录测试地址；仍保留生成刚开始与正文出现后两个拖动时机。
- 第一次定位候选编译因遗漏长整型状态函数导入失败，保留 `stream-drag-order-build-import-error.log`，改用现有状态函数后重新构建。测试取得已有状态管理对象的工厂参数尚需同步齐全，未把编译失败当作软件验收完成。

- B03：`library-theme-test-build.log` 编译成功（1 分 9 秒）；`library-dark-large-native.log` 4 项通过（76.974 秒）。独立安卓 14 采用暗色及 1.5 倍系统字体，覆盖嵌套选择、管理退出后真实底层触摸、跨目录选择与白线像素检查，以及真实输入法返回。脚本结束已确认恢复字体为 1.0。`library-dark-large-device.tar` 保存本轮指定截图，文件夹选择和键盘收起后的仓库截图已查看。此故障移到已修记录，其他页面体验仍按 T02 验收。
- B06：`stream-visible-green-build.log` 构建成功（7 分 22 秒），`stream-visible-unit-results.tar.gz` 5 组 38 项无失败。安卓 `stream-visible-native.log` 3 项中 2 项通过：普通正文生成中可见并在结束保持底部；工具过程默认折叠且失败详情可访问。手动上翻项失败，截图确实显示最终标记；新增 B07，不把整体宣布通过。`stream-visible-device.tar` 保留失败现场，继续区分拖动没发生与拖动后被覆盖。

- 旧实现将安全筛选后的回执转换为助手正文。新回归 `history-paired-receipts-red.log` 11 项中 4 项失败，原件 `history-paired-receipts-red-results.tar.gz`；没有把测试失败覆盖为通过。
- 现仅对历史中可验证、顺序正确且唯一对应的调用／结果保留配对，沿原调用编号和工具名称；参数标明因范围变化隐藏，结果仅含当前许可下的公开保存事实及成功／失败。推理签名、私有错误详情、图片和原始参数不重新发出；缺失／歧义配对不伪造新调用。原消息、数据库和导出原件不改。
- `history-paired-receipts-green.log` 构建成功（7 分 41 秒），15 项历史投影、存档隐私、迁移检查通过。结果原件 `history-paired-receipts-green-results.tar.gz`。
- `history-paired-receipts-native.log` 独立安卓 14 三项通过（68.474 秒），包含旧私有工具、旧摘要隔离及安全执行回执的真实提供商请求。已读取设备原件 `history-paired-receipts-device.tar`：玩家身份保存成功与文游启动失败均作为 tool（工具结果）发送，与原调用配对，未进入助手正文。
- 以上直接请求验证覆盖 OpenAI 兼容协议。Gemini 3（谷歌双子座第三代）原有缺少推理签名时的协议降级逻辑未改，不伪称已测试所有提供商。当前共用历史投影不再把执行回执改成助手正文；从故障表待处理区移入已修记录。

## B02 原案例操作序列补验

- `card-original-sequence.log` 构建／回归通过（1 分 59 秒），`NovexCardEditingContinuityTest` 四项通过，原件 `card-original-sequence-results.tar.gz`。
- 新增同一真实数据库流程：创建四张独立世界卡、同产物不同调用重试不重复建卡；三次越界排序不修改；同名模块新增返回原编号供更新；同模块追加及同操作重试不重复正文；合法排序；删除及原计划重试；已删模块与误用计划编号被拒绝；最后分类读取及其他卡不变。
- 此测试使用原案例的操作形态与中性正文，不包含用户私有正文；不是重新调用原模型，也不是完整重放原包全部 60 次卡片操作。软件序列证据补齐，实际界面／工具组合仍随 T02 收尾，不提前关闭 B02。

## 当前结论（后续记录覆盖历史“待验”描述）

最新用户纠正：软件准确传达和真实操作是验收目标，不再以消除模型所有创作偏差、原 86/90 语义分数或小动作零次作为发布条件。旧失败原件仍保留；主计划“最新用户纠正与恢复执行”覆盖下文历史门槛。用户已恢复执行，最终必要软件验收通过后直接发布。

- U7 最新真实安卓流式检查 `stream-viewport-history-native.log` 两项通过（82.183 秒）：底部跟随到最终末尾，流式期间主动翻看已有历史后仍保留历史位置；两张设备截图已人工查看。此前第二轮手动翻页夹具等到输出结束才尝试拖动，该失败保留但不算产品复现。普通长文本这两条路径已验证，不能外推所有工具交接与所有设备。
- 提示词放宽与滚动整合 `scroll-prompt-boundary-build.log` 构建成功，535 项中 534 通过、1 跳过；包含正式装配不再携带新增小动作禁令的检查。模型选择拆分手机用例已在 `stream-viewport-native.log` 通过；该日志同时保留当时另一条滚动夹具失败，不把整份日志改称全绿。
- U8 标题绝对居中已加入共用标题栏测量修正，左右按较宽按钮区对称留白。`header-center-red-native-2.log` 实际复现旧实现标题中心 192、屏幕中心 216；`header-center-green-native.log` 修正后安卓检查通过（13.463 秒），覆盖不对称按钮、长名称与按钮宽度改变。`header-center-green-build.log` 构建成功（5 分 55 秒），相关布局／组件 7 项通过，原件 `header-center-unit-results.tar.gz`。首次测试同步目录层级错误造成测试类未装入，`header-center-red-native.log` 仅为测试配置失败，不是居中缺陷证据。真实聊天复验 `header-center-chat-native.log` 通过（60.803 秒）；`header-center-chat.png` 已人工查看，标题和模型信息在屏幕中线，时钟／菜单位置保留，最终回答尾部仍可见。

- 新增 U7 生成结束滚动回跳，手机现象不再按旧通过结论忽略。旧已编译策略的直接回归 `transcript-completion-red.log` 两项中一项失败：流结束即取消跟随，最终持久行后续排版不再允许跟随；手动上翻保护通过。此证据证明策略缺口，不冒充用户设备完整复现。当前修改保持结束后跟随、发送时启用跟随，并观察末尾控件位置变化，尚待整合与真实流式界面测试。
- 正式提示词已撤掉小动作细化禁令，身份保存教学和工具说明同步区分既定身份与委托创作；用户提示词、资料通读任务专用说明、第六版候选均未替换。新整合断言待运行。
- 模型选择弹窗拆分构建 `model-selection-extraction-build.log` 10 分 10 秒成功；确认／取消与文字模型直接选择的新增手机检查尚未运行，不标整项完成。S5 其余输入区和总控制器继续待收尾。

- 世界／文游导出丢失作者扩展字段已有失败复现及完整包往返通过；未取得用户具体残缺导出文件，不能宣称已穷尽该投诉。
- 本轮新写文件使用正式回复编号，安卓完整检索／存档／重开流程通过。旧临时分支文件恢复核心三项、应用回归与安卓重开入口均通过。
- 只提供名称即可存档的软件链已通过。最新真实模型在前一条创建文游请求处失败，未到存档步骤；不算该真实模型门槛通过。
- 存档详情地址收拢、展开／返回及空占位隐藏已通过安卓检查，最终截图已人工查看。
- 创作／身份／启动的模型语义、小规模长资料事实质量仍未通过；不得扩大后续大规模评测并用总数掩盖失败。
- 原 S5（控制器与页面职责拆分）仍未全部完成，整体综合验收及最后有限图标尝试未完成。尚未打包发布或升级版本。

## 最新恢复与导出补验

- 空卡写后配置回归通过：`promoted-card-green.log` 构建 10 分 4 秒成功，归档 `promoted-card-green-results.tar.gz` 共 345 项，344 通过、1 跳过、无失败。包含世界／文游填写原空卡、配置失败共同回滚、其余空卡隐藏、身份切换保留公开成果地址与重试不恢复已移除管理项。真实模型重跑保存在 `creation-model-promoted-card`，仍待完整旅程结果。

- 导出修正回归已通过：`export-completeness-green.log` 构建 9 分 18 秒成功；归档 `export-completeness-green-results.tar.gz` 共 343 项，342 通过、1 跳过。新增世界／文游两项实际压缩包、完整正文、扩展字段、图片与独立数据库往返均通过。
- 对齐正式窗口后的无模型草稿真实界面验收通过：`draft-window-native.log` 1 项通过，包含新建、无模型发送提示、继续编辑、连接模型入口、系统返回、清理控制器后重新读取持久草稿。未发送消息。此前失败原件保留，不能再将测试宿主遮挡说成生产发送故障。

- 用户补充世界卡、文游卡导出残缺，纳入 R4 发布前检查。尚未取得具体残缺导出文件，不能将自行复现的缺口冒充用户原包根因。
- 已用真实归档往返复现两类卡丢失作者自填的结构化扩展字段：`export-completeness-red.log` 两项失败均为 `authorNotes` 缺失。导出器曾把渲染数据模型当作完整文档；现保留当前模块扩展字段，并由已有媒体／引用装配覆盖对应字段。新增用例覆盖各卡类允许的全部模块类型、长正文、图片字节、独立数据库导入及再次导出；修复后的回归运行中。第一次用例误放了其他卡类专属模块，已修正测试夹具并保留 `export-completeness-fixture-error.log`，不计为产品缺陷。
- 主机恢复，独立模拟器 emulator-5580 在线。学习任务协调器集成构建成功（11 分 3 秒）；`local/qa/v3-execution-20260909/learning-session-integration-results.tar.gz` 共 549 项，547 通过、2 跳过，无失败或错误。
- 最新无模型草稿界面测试失败于发送提示不可见。截图显示测试宿主软键盘遮住发送区域；正式活动配置了边到边与调整窗口尺寸，测试默认活动未配置。已对齐测试宿主窗口，待重新验证。上下文指示器与顶部时钟真实截图为同目录 `novex-indicators.png`。
- 真实模型完整请求保存在 `creation-model-user-source`：同一张文游未重复创建，但启动时仍补写玩家接替阿予等背景，并同时修改玩家身份；身份断言失败，未进入存档验收。原子启动参数修正不代表自然启动质量通过。
- 进一步核对原始回执：启动确实被校验拒绝，随后玩家身份修改成功；身份变更后的历史隔离连“启动失败”这一结果也抹掉了。已新增仅含固定操作名与完成／失败状态的公开回执，不转发旧人物资料、参数或错误正文；对应隔离回归待验。此修正解决执行事实丢失，玩家背景自行补写仍需真实模型后续核验。
- 公开操作回执整合构建 8 分 35 秒成功，`configuration-receipt-results.tar.gz` 12 项全部通过（隔离、原记录不变、配置失败状态、世界／文游导出）。包含收敛修正的新应用正在独立安卓模拟器重跑同一真实模型旅程；原件保存于 `creation-model-control-receipt`。
- `creation-model-control-receipt` 实测未通过：14 次模型请求后，玩家原句已正确保存，但文游仍未启动。公开失败回执确实进入新请求，模型没有声称启动成功；同时新证据暴露直接填写已有空卡路径只归库、不把结果加入对话管理配置。切身份后，该卡的成功保存回执仍因管理地址缺失而被滤掉，最终答复误称没有文游编号。已统一原生写卡与旧提案执行的写后配置：仅本次实际改写、已非空的对话自有卡加上新建结果；其余空卡不展示、单纯背景不授权、重试不恢复已移除管理项。新增回归同时检查配置保存失败回滚、另外两空卡隐藏、切身份后保留成果地址与重试不重新挂载。`promoted-card-red.log` 原实现 7 项有 1 项失败：填写空卡未触发配置保存；修正回归运行中。
- 小规模通读续跑 `learning-small-scoped-resume` 已保存 279/279 个可读块与 12 份笔记，阶段综合因输出未缩短而暂停；该状态累计输入 19583、输出 13780。原笔记保留，未扩大十万字／百万字。
- 阶段综合具体失败为 8306 字符源笔记缩到 4582，软件因没有达到期望的 4153 字符而拒绝，实际上已缩短约 45%。已把“期望减半”与“必须收敛”分开：仍要求模型尽量减半，但至少缩短四分之一即可继续，扩写或近乎不变仍暂停并记录费用。新增原行为失败与修复后核心回归，`learning-convergence-red.log`／`learning-convergence-green.log`；未降低原事实题门槛。相同小规模资料续跑保留原题核验，结果待检。
- 收敛修正核心回归 145 项，144 通过、1 跳过。`learning-small-convergence/live-summary.json` 显示仅新增两次综合调用，14 份笔记，原已保存范围未重复阅读，279/279 个可读块；扫描附录仍未解析，因此正确保留部分失败状态。原 11 题中 10 题正常结束，Q06 达到调用轮数上限；Q02 仍把不自动共享推成绝对不能互用，Q01 额外断言同名者之间“无任何关联”，Q09 额外断言不同路线不发生衔接。事实质量门槛未过，不扩大评测；此查询使用专用学习提示词和正式核心工具，仍不是完整安卓聊天装配，过程文字泄露不能直接归因到正式聊天界面。

- W6 新追溯确定阶段总览已污染事实：原调用 `call-0081` 把原笔记“不自动共享”写成“不共享”，并补写“两者之间无任何关联”；`call-0082` 又沿用该错误。隔离候选 `learning-qualifier-candidate` 同输入三次检查，限定词保留有所改善，但仍出现未经来源支持的跨路线禁止与编号重叠推导，未转为正式提示词；第六版继续未启用。后续需核查叶笔记的密集编号事实与真实查询路径，不能靠单次精简提示宣布解决。

- `creation-model-promoted-card` 真实安卓旅程持久化断言通过（1 项，135.668 秒，13 次接口调用）：单角色、单文游、正式主持身份、玩家原句和可列出的存档均通过。人工检查未通过：启动已成功，最终答复仍误复述早先失败；存档把所建卡的暮色改为清晨，并补造老邮差交代规矩和蓝灯位置。保留全部请求、回复及设备消息，不计创作质量成功。接下来区分历史失败表达与保存时事实增写，不能把绿色持久化断言当完整通过。

- 历史失败表达隔离对照 `creation-receipt-chronology`：同一已启动请求中，只把失败提示改为“此前这次尝试失败、较晚重试与当前设置为准”，三次均未再宣称启动失败。仍有代写触摸／移动等问题，不算旅程通过。将这一有界文案修正接回公开操作回执，避免旧失败以当前命令口吻压过后续成功；待应用回归。

- M2 存档输入改为名称即可：现有软件早已封存分支原话、采用配置与正式状态，但工具仍强制模型重写摘要和状态清单，与普通保存意图不符。保留旧摘要／结构化状态／旧文本调用兼容，常规保存不再要求生成叙事；新增名称单参数的真实文件重开检查，并将原生贯通用例改为同一路径。摘要字段保留固定软件说明，补充状态为空对象时详情不显示空面板；待编译、手机入口与真实模型复验。不因此宣称保存时的模型事实增写已全部解决。

- 历史回执时间表述应用回归已通过：`receipt-chronology-green.log` 构建 9 分 3 秒成功，结果归档 `receipt-chronology-green-results.tar.gz`；新名称单参数存档整合继续在 `name-only-checkpoint.log` 验证。
- W4 工具说明补齐连续短语匹配语义：原 Q02 把多个词以空格拼成单条查询，得到零条后转而读大量笔记。现明确单个连续短语、不同词分别查询、零命中不代表资料不存在；匹配行为本身未改。`literal-query-contract.log` 核心回归通过，原始查询与事实失败仍保留，不能据此宣布 Q02 或 W6 通过。

- Q1 已补齐[原 56 项逐项证据索引](2026-09-09-requirement-56-evidence.md)：沿用原题与编号，对照当前软件断言、真实模型失败及未覆盖范围；没有将候选模拟成绩计入安卓验收，也没有把待验条目标完成。

- 名称单参数存档编译成功（11 分 23 秒），应用结果 `name-only-checkpoint-results.tar.gz` 共 346 项，345 通过、1 跳过。手机 `name-only-checkpoint-native.log` 已通过存档原件与空辅助状态检查，但在后续仓库检索断言失败，未到达最终重开检查。原始请求 `name-only-checkpoint-requests.json` 确认文件返回写入成功，却使用界面临时 `assistant_…` 分支；同轮存档使用正式消息编号。现将工作区工具写入也统一为 `turnMessageId`（正式回复编号），保留失败样例，继续同一手机用例。此前遗留错位文件的安全恢复仍需单独核验，不按本修正自动认定已经恢复。

- 新真实模型 `creation-model-name-only` 首次运行在发送后等待 240 秒超时，桥接器未收到请求、设备仅有用户消息。检查本机进程确认此前转发连接已经退出；保留失败原件，不把连接缺失算模型能力失败。恢复独立测试转发后再跑同一旅程。

- W3 旧错位文件存在可核对线索：已捕获索引中的 `provenance.messageId`（来源消息编号）为正式回复编号，而文件分支为临时界面编号。恢复应只依据同对话、当前可见且实际持久的回复及原始校验值；保留旧文件，不覆盖目标同名内容，不把无法证明归属的兄弟分支直接开放。该恢复尚未实现，仍列发布前处理，不能把新写入修正当成旧资料已经找回。

## 基线与证据

- 活动工作树：`worktrees/tasks/preview-icon-memory`；基线 `614ec673545956ab8a007866da288dc7397c716e`。原四批修复未合并到预览线，继续沿此树工作。
- 已保存入口差异至 `local/qa/v3-execution-20260909/entry-tracked-diff.patch`；原未跟踪源码与验收材料全部保留。
- 本地 `local/` 与 `tmp/` 未有跟踪文件，现已加入忽略规则。测试源码和诊断结论仍入版本管理，用户原包不提交。
- 主计划：[完整更新计划](../plans/2026-09-09-complete-next-update-plan.md)。此前成果：[任务对账](2026-09-09-task-reconciliation.md)。
- 独立结构审查已完成；本轮先统一配置保存，再随相关功能拆分内部实现。

## 工作树登记

| 工作目录 | 分支 | 未提交条目 | 提交已在预览线历史中 |
|---|---|---:|---|
| ios | work/novex-ios-017 | 0 | 否 |
| preview | next | 5 | 是 |
| stable | release/0.2.15-announcement-history | 0 | 是 |
| announcement-history | fix/0.2.15-announcement-history | 0 | 否 |
| app-theme-colors | feature/app-theme-colors | 0 | 是 |
| card-workspace-execution | feat/card-workspace-execution | 0 | 是 |
| conversation-foundation | feat/conversation-foundation | 2 | 是 |
| conversation-settings | feature/conversation-settings | 0 | 是 |
| fast-candidate-build | fix/fast-candidate-build | 0 | 是 |
| fix-chat-media-scroll | fix/chat-media-scroll | 0 | 是 |
| fix-generated-image-blank | fix/generated-image-blank | 0 | 是 |
| fix-nova-resume-scroll | fix/nova-resume-scroll | 0 | 是 |
| mainline-next-stage | feat/mainline-next-stage | 0 | 是 |
| model-group-management | fix/model-group-management | 0 | 是 |
| preview-card-release-notes | docs/preview-card-release-notes | 0 | 是 |
| preview-icon-memory | fix/preview-icon-memory | 144 | 是 |
| release-0.2.10 | release/0.2.10 | 0 | 是 |
| release-0.2.11 | release/0.2.11 | 0 | 是 |
| release-0.2.12-theme | release/0.2.12-theme-colors | 0 | 是 |
| release-0.2.4 | release/0.2.4 | 0 | 否 |
| release-0.2.6 | release/0.2.6 | 0 | 是 |
| release-0.2.7 | release/0.2.7 | 0 | 是 |
| release-0.2.8 | release/0.2.8 | 0 | 是 |
| release-0.2.9 | release/0.2.9 | 0 | 是 |
| transcript-anchor | fix/0.2.14-transcript-anchor | 0 | 否 |
| unified-compaction-ui | fix/unified-compaction-ui | 0 | 否 |

合并状态只检查提交历史，不代表未提交成果已合并。没有清理任何工作树。

## 当前任务

- S1／S2：目录登记、材料忽略已完成；未移动或删除原包。
- S3：设置、身份、文游、世界书、状态和内容工具已共用配置保存模块；首次领域回归通过，新增保存失败／并发旧编辑拒绝三例通过。后续改动仍继续回归。
- C8：按最新用户纠正扩展到世界、角色、文风；取消强制模型转换前提。
- 其余任务沿主计划推进，未因登记而标为完成。

- W3 正式分支修正复验：`workspace-formal-branch-native.log` 安卓 14 完整流程通过（82.254 秒，一项），包含写入后搜索、仅名称存档、关闭重开与存档详情。应用编译 8 分 8 秒通过；`workspace-formal-branch-results.tar.gz` 共 346 项，345 通过、1 跳过、无失败。旧文件恢复与真实模型语义仍未据此标为通过。

- W3 旧文件恢复已实现候选：只核对同对话实际持久的当前分支回复，按原始字节数和摘要校验后复制到正式归属；保留原件和来源引用，冲突与损坏不覆盖。核心三项通过（`legacy-workspace-recovery-core.log`），覆盖重开、重复恢复、分支隔离、冲突与损坏；手机入口恢复及安卓整合待验。
- 真实模型 `creation-model-name-only-retry` 在转发恢复后实际调用六次，但第三条明确创建并启动文游的请求被继续当作角色台词，没有创建文游，因此在存档步骤之前失败。模型还在身份工具成功后多余索要确认。请求、回复与设备原件均保留；不能将此轮算作名称单参数存档的真实模型通过，也不据此改写已通过的软件存档结果。

- M2／U1 存档截图复核发现详情直接展示多行内部地址与空数值占位（`workspace-formal-branch-final.png`）。已将地址及校验收进“保存信息”，空数值不渲染，普通列表只标实际保存状态；原始依据和缺项提示保留。新增安卓检查默认无地址、主动查看可见、返回再次收起。该界面改动尚未随当前旧文件恢复构建产出，需下一次编译与截图复验。

- 旧文件恢复整合构建 `legacy-workspace-recovery-build.log` 10 分 37 秒成功，结果归档 `legacy-workspace-recovery-results.tar.gz`：应用 346 项与核心 148 项，共 492 通过、2 跳过、无失败。已安装到独立模拟器继续真实重开验收。

- W3 旧文件恢复安卓入口通过：`legacy-workspace-recovery-native.log` 一项完整流程，125.977 秒；包含创建旧临时分支文件、释放控制器后重新进入、自动找回原正文、保留原件，并继续查看正式存档。当前仍保留无法证明归属、内容损坏或目标冲突的原文件，不声称这些已自动恢复。
- R4 界面调用路径已静态核对：世界与文游卡均经 `ExportNativeSelection`（导出所选原生卡包）及同一压缩包编码器，现有完整性回归正是该命令。新增 `NovexCardExportEntryTest` 点击生成预览与分享、读取实际分享文件，核对两类完整正文、作者字段与图片字节；只拦截外部接收者选择器，尚待编译与原生执行。

- M2／U1 详情界面整合 `checkpoint-details-ui-build.log` 10 分 11 秒成功，应用 346 项中 345 通过、1 跳过；`checkpoint-details-ui-native.log` 一项贯通流程通过（101.81 秒），普通详情无内部地址／空数值占位，保存信息可展开与返回。`checkpoint-details-ui-final.png` 已实际查看，标题、日期、保存说明与依据入口可见。
- R4 实际分享入口通过：`card-export-entry-build.log` 测试编译 1 分 18 秒；`card-export-entry-native.log` 一项检查 33.531 秒通过，同时覆盖世界和文游。两份实际分享文件已归档 `card-export-entry-device.tar`，包含完整两章正文、作者扩展字段及逐字节一致的图片；分享提供器的文件授权已检查，外部接收者选择器由测试拦截，没有冒称跨所有第三方软件分享均通过。
- S5 消息行渲染已从大页面迁入 `ChatTranscriptRow`（消息行渲染模块），只接收既有行数据、四项显示状态、选择／面板状态与明确的用户操作回调，不持有控制器或仓库。列表顺序、稳定键、滚动所有权、选择清理和真正的执行仍沿宿主。页面约 5397 行；输入区域、弹层及控制器其余协调仍未完成。新增选择按钮仅填入与分支按钮边界检查，旧审批贯通用例补上已明确要求的“确认执行”，待编译和原入口回归。

## 本轮新增证据与实现（尚未整体验收）

- C1：已逐项核对 Agnes 原包，见对应诊断文档。类型目录查询缺口、越界排序误用、已有模块反复新增、重复删除和存档单文件上限各自登记，没有统称模型问题。
- C2／C3：新增原卡名称、简介、文游启动方式编辑；追加文章保留原文和模块编号；同名新增需要明确意图，重试沿用原回执；越界排序在写入前给出可操作范围。已加入真实数据库回归，三项真实数据库检查已通过；后续新增审批原文记录仍待回归。
- C8：已接世界／角色导入入口与原文采用；未知可读格式保留原文、原始字节及未知字段；原件落本卡目录。首轮世界采用断言对末尾换行的比较失败；修正该断言后，五项保存／重开／实际采用／导出检查全部通过。仍需覆盖用户导入入口与后续整理流程。
- U4：独立安卓 14 模拟器已复现“只勾选即执行”（期望 0 次，实际 1 次）。修复后的勾选、取消勾选、确认执行流程通过；新增关闭／重复点击用例需等待弹窗出现后重跑。原始失败日志保留。
- C6／U2：已有编辑器使用全部四种启动方式，原包只写入两模块不代表软件只能创建两个模板。移除创作快捷语句中强制先规划的措辞，替换错误的创作／游玩模式提示与仅限文游欢迎语。

日志根目录：`local/qa/v3-execution-20260909/`。尚无本轮正式发布；整体质量、长期验收和其余计划条目继续推进。

- 第二轮领域回归：384 项中 381 通过、1 跳过、2 失败。新建的编辑连续性 3 项与原文导入 5 项均通过。失败之一是旧教学断言仍禁止整块原文，按最新用户要求更新；另一项暴露审批原文展示混用校验指纹，已改为独立保存原正文，待复验。失败原日志保留，不改写成绿色结果。
- M2 队列新增大存档复现：存档自动封存原始消息与采用配置，可能超过普通模型文本写入的 1 MiB 上限。已加入超过该上限的原文保存、重开、重试和分支隔离用例，先跑出确定失败再修。
- I3 待核验：存档保留的采用配置与工具原文，在换身份后通过仓库读取路径是否遵守私有范围。当前只有候选装配／直接模块读取隔离证据，不能推定存档回查路径也通过。

- U4 原生复验已通过：`approval-green-2.log` 为同一轮 5 项通过，包含勾选／取消不执行、确认后一次执行、重复点击不重复、新操作重新选择、关闭不执行，以及原有执行折叠／世界详情入口。两次编译缺少测试依赖和首次未等待弹窗的失败保留，现使用已有系统按键注入并等待真实弹窗出现。设备为独立安卓 14 模拟器，未触碰用户模拟器。

- 第三轮领域回归通过：`complete-edit-checkpoint-domain.log`，应用 385 项、核心 135 项，合计 518 通过、2 跳过、0 失败。结果原件保存为 `complete-edit-checkpoint-domain-results.tar.gz`。覆盖修正后的审批原文、卡片头部修改、原文导入和大存档；不等于真实模型质量门槛通过。
- M2 大存档已先复现再修复：`checkpoint-capacity-red.log` 明确在普通文本 1 MiB 上限拒绝；新增软件专用存档写入通道，沿既有 64 MiB 附件容量写入并登记，普通模型文本限制不变。大原文保存、重开、同操作重试、分支隔离检查通过。超过 64 MiB 仍明确拒绝，未声明无限容量。
- C8 继续补手机入口：原文卡显示“帮我整理”，打开挂载同一张卡的对话草稿，用户可修改要求后发送；导入不调用模型，不自动开始整理。新增同名独立卡／整理同卡／重试不重复／旧快照不变测试和安卓导入预览交互，等待复验。

- C8 入口回归：`import-entry-ui-domain.log` 109 项通过；`import-native.log` 安卓 14 上两项通过，覆盖世界／角色的真实文件读取、取消不保存、确认入库、打开原模块与整理入口。系统选择器结果由测试登记器提供，不冒称实测了所有手机文件提供器。测试没有使用模型。
- C8 后续整理发现新缺口：`checkpoint-privacy-red.log` 中，同卡整理回读失败，事务回滚；保存层自动保留导入来源元数据，而工具核验仍要求与未带来源的正文完全相等。已让写入与核验共用来源保留规则，继续精确检查正文，等待绿色复验。
- I3 存档路径两项明确失败：同一局次切换身份后，自动续接仍包含历史工具私有文本；工作区直接读取亦包含原始环境。不是错分支或身份没有切换。新增面向模型的公开续接视图，存储／用户查看／导出仍保留原件；读取、搜索、受限计算共用过滤，自动续接沿同一规则，待复验。普通聊天历史、压缩摘要及其他回查路径仍需继续核验，不据此将 I3 全部标为完成。

- 第四轮领域回归通过：`import-edit-privacy-domain.log`，523 项中 521 通过、2 跳过、0 失败；原件 `import-edit-privacy-results.tar.gz`。同卡整理、旧快照不变以及存档读取／搜索／计算隔离均通过。
- 新真实模型旅程：`creation-model/native.log` 在安卓 14 上一项通过，四次用户请求共 11 次模型请求。创建单角色三模块、正式选角色、正式启动主持人文游、保存并列出存档已通过；原请求与回答、设备消息原件均保存。人工语义检查仍发现“你回头看”等代写动作，以及“刚到镇上”被扩写为“入镇数日”，**不通过创作质量门槛**。没有将持久化通过冒充整体成功。
- I3 普通历史已真实复现：`history-privacy-native-red-2.log` 捕获实际提供商请求，旧身份的工具结果／推理仍被发送，明确断言失败。首次夹具绑定了不同回环地址导致连接失败，保留 `history-privacy-native-red.log`，不计作产品复现。当前新增执行历史访问范围记录和只读投影；同范围沿用，改变身份／玩家身份／管理范围后保留公开对话、停止沿用旧执行内容。压缩摘要增加持久范围记录和版本 33→34 数据库迁移；旧摘要不补造范围，重新从可见原文整理。编译、迁移、真实入口回归尚在进行。

- I3 历史与摘要补验：`history-export-build.log` 编译成功，`history-export-unit-results.tar.gz` 中 8 项全部通过（历史投影 4、升级迁移 1、存档／摘要回查 3）。`history-export-native.log` 中两项真实界面请求检查通过：旧身份工具结果／推理与旧压缩摘要不再泄漏，公开任务和正式答复仍存在。管理范围扩大保留旧读取，范围撤回停止重用；原始数据库与导出不删改。
- 同轮对话包界面检查没有等弹窗出现即点击，失败在找不到“保存到文件”，没有进入保存操作；已补等待，继续复验。当前不能将 U6 标为完成。
- U6 新入口复用同一导出包，直接调用系统创建文件；取消不报成功、失败不覆盖成功提示、写入成功后才反馈。新增取消／原件失效／逐字节一致测试。
- U2 新增无模型可进入对话与输入草稿持久化，数据库 34→35 新增未发送文字列，不写成聊天消息。发送前检查可用模型并保留输入与附件；返回列表不将含草稿的对话删除。正在验证迁移、无模型实际入口、设置引导和重新打开；尚未宣布通过。
- S4 已拆出卡片导入、导出和共用字段处理。导出只接收快照及媒体读取／模块引用读取接口；导入使用原有存储端口，仍在原工作空间事务内执行。文件指针与失败回滚未拆成独立提交。工作空间由本轮拆分前约 2,159 行减至约 1,472 行；需要下一轮往返与失败回滚回归，未因行数减少宣称架构已全部整理完成。
- 玩家自主性候选没有达标：`agency-candidate` 六次请求中，三个开场仍有错误前情／代写回应；`agency-reasoning-baseline` 三个有效请求仍有未指定感受、错误时间或未获得物品。开启思考的最初请求缺历史推理字段遭拒，夹具修正后重测，失败原件保留。两个方案均未加入正式提示词，第六版仍未启用。新对照 `agency-task-anchor` 仅在实际请求末尾保留本轮用户要求，尚在观察，未作为软件实现。

- 追加两组玩家自主性诊断：`agency-task-anchor` 三次在末尾重复当前任务，`agency-background-role` 三次把背景资料移到参考消息。两组仍出现代写动作／错误前情，均拒绝转正。正式请求装配未采用这些试验。错误时间已能追溯到模型先前写入的文游缘起，重复用户要求或更换消息层级不足以修复；后续质量工作需覆盖“创作资料本身忠实”及“游玩续接”，不能只检查最后一段回答。

- U2／U6／I3 组合回归：`draft-export-build.log` 成功，`draft-export-unit-results.tar.gz` 共 357 项中 356 通过、1 跳过、0 失败，含草稿 34→35 升级、原字节恢复、清空草稿与对话隔离。`draft-export-native.log` 共 4 项，导出 1 与历史隔离 2 通过；无模型草稿在第二次点击发送后的连接按钮查找失败，输入保留与草稿写入断言已过，补等待后需重跑，不提前判定失败原因。
- U6 已有原生证据：取消不生成文件、保存字节与原包完全一致、原件失效不报成功均通过。截图 `conversation-export-save.png` 已检查；随后将长技术说明收入“导出范围与缺项”，重要缺项数量仍常显，视觉变更等待最终组合回归。
- P5 条件评估已记录到主计划：基础图文模板延期，理由是完整模板缺结构化冻结／展示／导出连贯路径；用户允许复杂时后延。已有卡内图片与剧情插图不受影响，不把普通网址面板声称为本卡附件模板完成。

- S4 拆分回归：`card-transfer-build.log` 编译成功；卡片导入导出仍沿同一事务与文件回滚链，领域结果已归档为 `card-transfer-unit-results.tar.gz`。未以拆分代替全部页面／控制器整理。
- U2 无模型手机检查已通过提示、保留输入和设置入口；下一次返回断言失败，位置在系统返回键，继续检查输入法先消费返回的情况。`draft-wait-native.log` 保留失败，不标为产品全部通过。
- I3 新发现并补齐“自由描述我的身份”的标准工具入口，沿已有配置保存模块；已有身份不同需明确替换，启动文游可明确沿用当前身份，不能拼接。新增真实数据库关闭重开、配套冲突、重复启动和结束恢复用例，正在编译回归。
- 玩家自主性的精简教学候选 `agency-concise-candidate` 三例仍有错误时间或代写动作，未纳入正式提示词；不能以一例有所改善转正。

- I3 玩家身份工具领域用例已通过，`player-native-context-red-results.tar.gz` 同轮 391 项有 389 通过、1 跳过、1 预期容量旧实现失败；身份保存两项无失败。
- V2 容量旧路径已真实复现为 128K，见 `player-native-context-red.log`；官方规格、目录解析、模型组与界面安排见 `2026-09-09-model-capacity-and-peak-clock.md`。V3／V4 与容量修正编译成功，但首轮手机测试因测量接口写法编译失败，尚未完成手机验收；测试代码已修正。
- 新真实模型复验 `creation-model-player`：四条用户请求共 18 次接口调用，持久化链检查通过，语义检查发现切换玩家身份后丢失已保存卡回执，随后重复创建文游并发生活动局冲突，仍不通过。原始请求、回复和设备消息已保存。新增仅保留当前管理范围内公开成果编号／名称的历史投影，私有正文、工具参数与推理仍隔离；正在先运行该断言的旧实现失败检查。
- U2 输入法收起后系统返回仍未触发页面退出，`draft-keyboard-native.log` 保留；继续用系统全局返回动作和失败时窗口截图区分残留弹层与测试按键路径，未放宽草稿重开标准。

- I3 保存回执旧实现复现：`receipt-history-red.log` 共 403 项，401 通过、1 跳过、1 预期失败；失败正是切换玩家后保存编号丢失。结果原件为 `receipt-history-red-results.tar.gz`。同轮容量目录、模型组、接口元数据及官方峰谷边界测试通过。
- U2 `draft-global-back-native.log` 一项通过：独立安卓 14 上无模型进入、发送引导、保留草稿、设置入口、真实系统全局返回、释放控制器后重新进入、草稿不变且没有发送消息。先前按键注入失败与全局系统返回结果分开保留，不再据按键失败宣称产品返回失效。首张自动截图为空白，不算视觉验收；待用已绘制页面重新采集。
- 独立结构增量复核已完成，见 `2026-09-09-structure-follow-up.md`。发现旧计划和卡片工具回执结构不一致，已抽取共用成果回执与按当前管理范围过滤的公开续作视图；覆盖模块写入、排序和关联的稳定编号，不重放正文、参数或推理。旧计划仍注明未回读，不伪装为核验成功。正在完整编译和回归。
- 结构复核另有两项跟进：来源准备仍被宿主外层事务包住；缺失历史原行的扁平文本需要明确来源边界。两项未标完成，后续处理；总控制器拆分仍是进行中。

- I3／S3 后续回归：`receipt-history-green-results.tar.gz` 共 405 项，404 通过、1 跳过；`prepared-player-tests-results.tar.gz` 共 408 项，407 通过、1 跳过。第二组覆盖来源读取先于提交、撤回来源权限后拒绝写入、配置失败时卡片共同回滚，以及缺失原行时仅保留明确公开原话。两组均无失败，不把重复回归相加成独立用例数量。
- 真实模型两次续验仍未达标：`creation-model-receipt` 为 8 次请求，文游未重复创建，但启动时错误沿用不存在的玩家身份；`creation-model-atomic` 为 9 次请求，直接保存玩家身份后启动，却加入了原角色的蓝灯和信袋。原请求、回答、设备记录保留，不算创作质量通过。当前启动身份说明限定引用用户原话，独立的身份编辑仍支持用户委托创作；待模型与手机复验。
- U2 最新手机复验失败在重新进入后的草稿尚未恢复便立即断言。测试改为等待原草稿实际出现后再逐字比较；不会仅凭输入框出现判定恢复。最新截图代码尚未随测试安装包成功产出，旧空白截图仍不算视觉验收。
- W4 原文依据回读：新增笔记旁的有界原文片段，严格采用已记录来源版本与区间，记录实际返回范围；不将笔记标为事实核验成功。核心 138 项中 137 通过、1 跳过，见 `learning-evidence-core-results.tar.gz`。首次失败是测试构造违反既有来源约束，随后测试编辑有括号语法错误，失败日志均保留。
- W6 原小规模笔记查询因旧来源修订不齐，被当前校验拒绝且未调用模型。改用相同 1.8 万字符资料、原 11 题重新完整通读并查询；沿现行正式通读提示词，不启用第六版。结果待人工检查，不据领域测试宣布事实污染已解决。

- W4／W6 第一次新完整小规模运行在密集长章节输出截断，软件暂停且未计该批为已整理；输入／输出用量和已存笔记一致保留。第二次将源片段同时限制在输出容量内，仍出现重复生成多套总计导致截断。针对直接证据，通读请求明确“本批结束不等于文档结束”，要求按原文顺序记录事实一次；不是第六版人格提示词。`learning-small-live`、`learning-small-output-bound` 失败保留，`learning-small-scoped` 待验。
- S5 调度职责已迁出：新增核心 `NovexLearningSession`，统一启动提交、单运行实例、暂停、恢复、重新打开后的孤立任务处理；完成等待也迁入核心。宿主保留提供商适配与页面状态，未重新实现通读、预算、审批或重试规则。独立复核发现两处竞态：撤回来源后应仍能取消自己的运行、同资料新计划不能复用旧任务实例；已补先暂停再切分支，以及资料集／计划／任务实例共同识别。`learning-plan-owner-red.log` 先稳定复现新计划未启动，绿色核心回归 144 项中 143 通过、1 跳过，原件 `learning-plan-owner-green-results.tar.gz`。安卓整合待验，总控制器和页面的其余职责尚未全部拆分。

## 消息行职责拆分复核

- 将单行正文、执行过程、选择项、成果入口与分支控件移入 `ChatTranscriptRow`，消息列表稳定编号、滚动、选择状态及实际修改动作仍由宿主持有。独立结构审查未发现有依据的行为变化；发现材料设计控件通配导入违反现有页面结构约束，已改显式导入。
- 首次构建 `transcript-row-extraction-build.log` 10 分 16 秒成功，但筛选范围未包含 `ui.novex` 的结构测试，不能作为该规则通过证据。修正导入后补上 `NovexComponentArchitectureTest` 继续检查；手机交互仍待验。
- 新增消息行隔离交互检查：选择只填输入、分支动作保持原消息编号与边界。原对话贯通检查中的两处批准恢复点击补上正式“确认执行”，与已确认三档权限一致，不恢复旧勾选即执行行为。

- 创作失败隔离诊断 `creation-role-boundary-probe` 保留四份实际请求／回复：原请求重放、只限定身份说明作用范围、只移除上一条错误答复、只开启模型思考。四者本次均开始调用查看工具；原始失败具有采样波动，不能据此确证角色说明是根因。两个文字候选仍有内部流程说明泄露，思考候选消耗增加且只测到首批工具，不计整条旅程成功。没有改正式提示词、没有过滤用户原始对话，也未启用第六版。

- 显式导入后的整合构建在结构测试发现另一个既有新文件 `NovexChatIndicators` 仍有通配导入，原失败归档 `transcript-row-imports-results.tar.gz`，不算全通过。应用／手机测试程序编译已完成，开始实际交互复验。指示器改成同四个控件的显式导入，不改行为。
- 结构规则通过直接运行原已编译、未修改的 `NovexComponentArchitectureTest` 检查当前源码，`source-architecture-direct.log` 四项通过（2.138 秒）。此前尝试跳过编译运行构建任务遇到运行时包任务依赖限制，`source-architecture.log` 保留，不算产品失败或通过。最后的指示器导入清理仍须随下一次正常整合编译核验，不用源码检查冒充新安装包已编译。

- 消息行拆分的安卓验证 `transcript-row-native.log` 五项全部通过（216.456 秒）：选择仅填入、分支边界；三档权限、批准后丢失运行环境再恢复、成果打开、模块排序与删除；完整角色／文游／状态／插图／存档／旧文件恢复和重开路径。设备材料 `transcript-row-device.tar`。这证明此拆分保留对应操作链，不等于真实模型创作质量通过，也不把 S5 其余输入区域／弹层宿主标为完成。

- `creation-model-row-baseline` 真实模型四回合持久化通过（111.608 秒、11 次调用），仍未通过内容质量：开场替玩家推门、给玩家挪用角色蓝灯，保存辅助状态包含错字且先传数组后纠正；不能以持久化通过取消这些失败。
- 进一步从实际 `request-08.json` 找到教学同步遗漏：`NovexProductToolGuide` 仍要求“提交可读摘要与结构化状态”，与现行存档工具和回复规则的“通常只提供名称”矛盾。此前记录的“已对齐”仅覆盖工具／回复规则，不能继续泛称全部教学已同步。
- 隔离 `checkpoint-teaching-alignment` 仅替换这一条旧教学，同一真实请求三次都以名称调用存档；第三次另外调用本局状态更新，因此仅证明此次存档参数简化，不证明整体事实质量通过。已修正该旧教学，并在正式提示词整合测试加入矛盾回归断言；不修改用户补充提示词、不删除旧参数读取兼容、不启用第六版。尚待正常整合编译和手机重验。

- 存档教学对齐正常整合构建 `checkpoint-teaching-alignment-build.log` 8 分 29 秒成功，535 项（534 通过、1 跳过）；包括正式教学、消息交互逻辑、结构规则和之前的导出／存档回归。指示器显式导入已经随该构建编译。
- `creation-model-checkpoint-aligned` 安卓真实模型完整持久化路径通过（107.521 秒、10 次调用）：此次存档只传名称，没有状态格式错误重试。内容质量仍失败：开场代写踏入、接灯、持信和内心，卡片开场与正文／最终位置清单还有物品位置冲突。未宣称整轮创作质量通过，原失败保留。
- 新增 `NovexRealModelSourceQueryTest`（仅显式启用才计费）走真实 `ChatScreen` 与仓库工具，采用原已解析文本、原 14 份笔记、完整解析状态清单和未改的 Q01／Q02／Q09，各题独立对话；不把扫描附录替代文字算已识别图片。种子文件保存原状态校验值。这里只补正式聊天的查询证据，不替代原通读／笔记失真检查，也不计大规模评测通过。测试入口构建 1 分 13 秒成功，手机查询进行中。

## 正式聊天来源查询与仓库修复

- `source-query-native` 实际安卓查询运行结束（166.868 秒、17 次模型请求），仅证明三题取得回复，不能当作语义验收通过。Q01 的人物、出生地与保管物正确；Q02 最后仍输出大量内部筹划、把“不自动共享”推成互不共享；Q09 虽正确判断无法计算航程，仍把“未设定”升级为不存在，并额外推导地理体系互相独立。源笔记失真与正式聊天质量问题均保留，不缩减原题或扩大长资料评测。
- 新发现的两处软件缺口已有实际请求证据：普通中文引用被读取器拒绝，模型必须重写百分号编码；分页返回的两个游标被原样传回却判失效。`request-03` 保留中文引用错误；`request-07`／`response-07` 核对两个分页标记原样沿用，`request-08` 保留无效游标回执。
- 中文引用核心回归先红：`readable-reference-red.log` 三项失败，包含原用户路径及畸形字符编码会误接替代字符文件的检查。现允许普通中文／空格／表情和百分号编码混合输入，统一规范化；只解码一次，继续拒绝路径逃逸、空字符及畸形字符编码，不按名称跨对话查找。
- 分页回归先红：`cursor-visibility-red.log` 两项中一项失败，新增中间工具回复使未变化目录的游标失效；新增真实可见文件的失效检查保持通过。现签名以当前实际可见文件及修订为依据，不被没有文件的中间消息编号打断；改变真正可见资料仍拒绝旧游标。
- 两项修正后 `workspace-reference-cursor-green.log` 核心回归通过，结果归档同名前缀的 `-results.tar.gz`。新增真实聊天贯通测试 `NovexWorkspacePagingPipelineTest`，依次读取第一页、保存工具回复后续第二页、读取普通中文与表情路径；应用整合编译进行中，不能先标手机验证通过。

- 仓库中文引用／分页整合构建 `workspace-readability-build.log` 9 分 21 秒成功，`workspace-readability-results.tar.gz` 535 项（534 通过、1 跳过）。真实聊天贯通 `workspace-readability-native.log` 一项通过（49.568 秒），实际请求原件为 `workspace-paging-requests.json`。最初材料提取使用错误应用包名，错误保留 `workspace-paging-package-error.txt`；重新按已安装包名提取，不计为产品失败。
- 消息回执对照 `control-receipt-protocol-probe` 只将最后一条软件操作文字改为配对的工具调用／工具结果，三次均产生正文，但仍有玩家代写、旧角色物品／能力混入和内部编号外露；仅支持继续检查历史消息类型，不证明创作质量问题已解决，没有直接修改隐私投影。对照调用参数为空对象，只用于隔离诊断，不能原样充当正式历史重放方案。
- 思考开关对照 `agency-thinking-comparison` 首次关闭思考只回复“软件操作记录：开”；随后开启思考的提供商请求失败，本组中断，不能据此比较开关优劣。失败日志保留，不把请求失败当作思考模式语义失败。
- 原失败题 Q02 通过修正后的正式安卓聊天复验，`source-query-readable-run/native.log` 一项完成（71.657 秒、3 次模型调用）。两条普通中文路径直接读成功，不再需要编码重试；模型引用了实际来源，结束为完整答复。首次测试命令误用了应用包名，尚未进入测试，单独保留 `native-package-error.log`。新设备原件为 `device-evidence.tar`，其中旧 Q01／Q09 缓存不是本次新跑证据，只认 Q02 与本次三份请求。
- Q02 仍不算语义通过：正文把不同版本的当前经历边界概括成绝对互不相通；更明确的无依据推导是用“谁保管钥匙”证明另一版本缺乏相应知识，物品保管事实不能证明知识有无。本次没有读取含“不自动共享”完整限定的第三份来源。读取修复有效与事实判断仍需修复分开记录，保持原门槛。


## 创作库补测与流式正文误折叠（2026-09-09，后续校正）

- `library-modal-integration-native.log`：8 项中 7 项通过，包括嵌套选择／管理弹窗返回后恢复底层点击、白线像素检查、分组设置和文游入口。文件列表项失败保留，未将整组宣称通过。
- 文件补测确认是新增行处于保留滚动位置上方，先前测试假定立即进入可见区域。保留失败截图和树后，以三次有界真实下滑查看顶部，新增文件显示并可打开。`library-visibility-bounded-native.log` 1 项通过（38.458 秒），`library-visibility-bounded-device.tar` 保存截图及状态树；已查看 `readable-file-library.png`，未改生产刷新逻辑。此前无界测试滚动被主动停止，`library-visibility-native.log` 的进程退出不能解释成产品自行崩溃。亮暗、大字、键盘组合尚未全部验收。
- 普通正文误折叠 B06 已在原实现新增用例复现：`library-stream-red-result.xml` 共 6 项、1 项失败，失败为正文在实际工具调用前即标为执行文字。构建守护进程日志确认 15:51:36 已结束，旧输出连接未收尾，不能据连接仍在等待认定编译正在运行。修正改为实际工具调用时才收拢执行文字，待整合验证。

- 输入编辑职责迁至 `ComposerTextEdit.kt`，保留原发送后输入法迟到提交、回车偏好、多行粘贴、选择及组合区行为；没有修改创作提示词。`composer-edit-extraction-build.log` 整合构建 8 分 29 秒成功，`composer-edit-unit-results.tar.gz` 7 组共 34 项通过。输入区整体拆分仍未全部完成。
- `composer-pipeline-native.log` 的审批中断恢复路径在已保存后找不到成果打开节点，先列 B09 取证，不直接推断为测试问题或产品回归。首次安装脚本的路径转换错误发生在安装前，保留 `composer-pipeline-install-path-error.log`，与产品故障分开。

- 长资料软件控制基线重新运行当前核心：`learning-software-18000`、`learning-software-100000`、`learning-software-1000000` 各 12 项全通过，正文分别 17,630／99,653／999,642 字符，279／1,429／13,839 可读块，12／33／255 阅读批。没有真实模型调用；验证发送前、返回后保存前、保存后界面更新前中断恢复、用量记录不丢、已读范围不重复、来源／解析修订拒用旧游标。扫描附录仍明确未解析，不宣称全部原件可读。原真实模型语义失败记录保留，未用本轮离线结果冒充理解能力通过。
- `composer-pipeline-native.log` 最终为 4 项、3 通过、1 失败，失败仅为审批恢复后找不到成果打开节点；自由／只读和无模型草稿通过。新增取证未改变生产行为，等待同一审批路径的消息与界面现场。

- B09 同路径取证用例 `pipeline-open-diagnostic-native.log` 单独通过（81.676 秒），未进入缺失分支，故无缺失现场，不能据此判根因。测试原先只等待正文可见与持久化，现增加停止按钮消失的回合完成等待；等待后的组合复验尚未执行。生产成果逻辑未修改。

- `pipeline-completion-boundary-native.log` 4 项全部通过（213.696 秒）；已查看 `pipeline-completion-boundary-device/cache/foundation-ui/chat-sort-reopened.png`，成果入口在正文中可见。原 B09 移到已排除的测试问题，原失败保留；未改生产成果处理。
- 截图发现降级渲染提示为英文实现细节，列 B10。仅改资源文字为生成结束后恢复排版的说明，增加简繁中文；三份资源解析与唯一名称检查通过，待随整合构建进入安装包。
- 新增多世界卡串联界面用例，读取独立仓库资料、四卡创建与重复请求、同名模块新增拒绝后按原编号追加、排序及重开。第一次测试编译错误是误把世界卡包装对象当作世界实体；保留 `multi-card-native-build.log`，已修测试访问路径，未改生产数据结构。三个既有界面用例同步回读保存后的模型编号，避免临时编号污染测试结果。

- `multi-card-native-fixed-build.log` 测试构建通过（1 分 4 秒）；`multi-card-native.log` 真实安卓聊天串联通过（133.554 秒）。9 个提供商请求、14 项工具操作，仓库正文完整返回，四卡创建重试不新增，重复模块新增返回错误，随后按原编号追加、有效排序、成果打开及重开通过；四卡分别保留两模块。`multi-card-native-device.tar` 存实际请求、原始消息、界面树与截图，已查看四个成果入口。原 B02 软件操作路径关闭，模型组织偏好不作为强约束。期间一次远程读取连接超时，重读同一日志确认测试已成功完成，没有重启测试或当作产品崩溃。

- 输入区域继续按责任拆分：`ChatComposerTextField` 拥有输入框外观、占位提示轮换、焦点与输入法动作连接；父页面继续拥有草稿、发送和提及选择。`ChatThinkingControls` 拥有思考等级按钮及选择弹层，不访问数据库或总控制器。`ChatScreen` 当前 5,044 行，不能以此次拆分宣称所有历史大文件问题消失；构建与沿原入口的交互验证正在进行。
- 发布基线只读核对：当前最新公开预览仍是 `v0.2.16-beta.38`，未发布 3.0。快速发布脚本限定 `next`（预览分支）、已提交且推送一致的源码、签名和产物校验；本轮仍先完成整合／升级验收，不改稳定分支。


### 输入组件收尾与资料快照验收续接

`composer-controls-integration-build.log` 构建成功（8 分 28 秒）；27 项相关单元、`composer-controls-native.log` 5 项安卓检查全部通过（159.371 秒）。B10 文案修正随此次构建验证，移入已修表。

`learning-ui-native.log` 首次失败发生在进入界面前：完整状态含采用对象身份比较的台账类，测试错误地直接比较重建对象。改为比较完整持久化表示；不改生产存储行为。原失败保留，界面验证尚未通过。结束文游后恢复身份的扩展测试构建成功，正在安卓验证。

结束恢复扩展的首次安卓检查失败：测试模拟提供商发出空参数，实际工具在执行前正确拒绝（缺必填本局编号），并非已证明的结束状态保存故障。`journey-end-requests.json` 保留拒绝回执；测试改从实际系统设定读取本局编号传入，生产代码不改。

资料快照界面复验 `learning-ui-equality-native.log`：1 项通过（34.43 秒）。比较完整保存表示后，真实资料弹窗的范围、未解析标识、笔记正文与来源、系统返回、文件回调和续接回调均可达，查看不修改快照。测试使用实际离线生产运行器快照，标签中性化；不代替真实百万字模型理解成绩。截图 `learning-restored-progress.png`。

### 发布候选准备

`journey-end-args-native.log` 1 项通过（114.737 秒）：从实际设定读取本局编号，重开后结束文游，恢复原角色及其原范围记忆。首次测试漏参的失败原件保留。

图标有限检查：已有别名切换服务未连接应用实际主题。预览默认唯一白鸽素材为 `novex_preview_dove.png`，SHA-256（文件校验值）为 `41fdfd026b0df6d4dfa1e53b06f4994648ee23ab686b6730d3036c39ac41a859`；旧主资源亮／暗前景完全相同，校验值均为 `c53b217ee5665ac6a4c55e9dbd04953898c06bb269b227d6edcd17f66ee4802f`。资源、原实施记录与本地证据目录中未定位两版可直接复用的白鸽。按用户有限尝试的范围延期自动跟随，保持固定原图，不将同图切换冒称成功。

预览候选基础版本设为 3.0.0，更新说明已起草；稳定工作树不动。版本编辑不是发布完成。快速通道首次在处理已迁走的旧文件时校验失败，未开始构建；`release-candidate-deleted-path-failure.log` 保留证据。同步清单现排除不存在的本地输入，远端旧文件清理继续沿清单差异进行；`release-runner-tests.log` 已通过。完整检查及 3.0.0-beta.1 签名候选构建运行中。

升级前先备份独立模拟器的数据库、文件与设置，待传输结束后校验完整性；正在下载已发布 beta.38 作为真实升级起点。未验证完备份前不替换安装。下载、备份和构建均独立记录，不以进程启动代替完成。

后续结果：原测试环境备份 `pre-upgrade-emulator-data.tar` 传输结束，3,846 个条目完整可读，包含数据库与设置；beta.38 安装文件校验值与正式发布的 `SHA256SUMS.txt` 一致。独立模拟器 5580 清理原测试安装后，实际安装并启动 beta.38，界面创建 `UpgradeWorld`，概述 `Preserve-this-world-across-3.0-upgrade` 已保存并在详情中显示，证据 `upgrade-beta38-world-saved.xml`。尚未覆盖升级；下一步备份这个旧版本样本，再安装签名候选验证保留。

完整候选检查首次执行到 1,441 项测试：1 项旧契约断言失败、3 项跳过。旧断言仍要求存档必须携带摘要／状态；按已确认的名称存档契约改为仅名称必填，并继续检查辅助字段和原始记录捕获说明。`release-candidate-old-checkpoint-test-failure.log` 保留失败，未改变生产存档实现。完整快速检查及候选构建已重新启动，结果仍待确认。

### 首个签名候选与升级发现

`release-candidate-fast-check.log` 完整构建通过（17 分 25 秒），1,441 项单元检查无失败、3 项环境相关用例跳过，归档 `release-stable-unit-results.tar.gz`。预览包版本 3.0.0-beta.1，签名校验通过；首个候选校验值 `a9e7b6868c9a44d1cfdaec2a703b70fbf51d7c68c1c475a924a8814a3f50e911`。代码固定到 `2825100` 并快进本地预览线，尚未推送／发布；该提交还有一个无行为变化的文件尾空行整理，最终发布仍须沿正式脚本重新核对源码。

真实 beta.38 覆盖安装首个签名候选成功，库中可见原卡和概述。`upgrade-from-beta38/upgrade-database-checks.json` 七项通过：32→35、原世界行及修订行保留、草稿字段、历史范围字段和卡片目录表。升级前后数据库备份均留在忽略目录。安装过渡期间的一次界面抓取报空根节点，旧路径文件不能作为新界面证据；后续独立路径 `upgrade-candidate-worlds-ready-ui.xml` 为安装完成后的实际库页面。

升级同时发现原 U2 的实际入口遗漏（B13）：无可用模型时首页无条件显示引导，既有草稿被挡；空首页的开始按钮仍锁定。现改为仅空首页显示引导，普通新建回调可直接打开草稿，欢迎语和无标题对话名称采用对话／创作表达。扩展既有无模型安卓用例：返回真实列表、找到保存草稿并通过列表打开，不再用测试文字替代列表。`onboarding-draft-candidate-build.log` 正在运行；最新回归用例及无标题文案改动尚待后续同步验证，不能把首个签名候选当作已经含有 B13 修复。

B13 继续核对：实际 `AppNavigation` 仍在无模型时将 CHAT 路由退回首页，已移除该回退，保留配置加载等待和发送前连接检查。先前页面级无模型用例未经过真实导航，不能据其通过覆盖此遗漏。`onboarding-draft-candidate-build.log` 中间候选构建通过，但不含后补导航和无标题修正；最终增量候选 `onboarding-navigation-candidate-build.log` 运行中。

最终导航候选 `onboarding-navigation-candidate-build.log` 13 分 26 秒构建通过，版本 3.0.0-beta.1，签名符合预览证书；校验值 `d4de2821811f9630e408b7fde3f4ce8826c26ae45c61ed451ae2586eb68535fc`。实际覆盖安装后，经首页新建菜单进入，输入草稿、返回列表、列表打开、强制停止重启再打开，以及发送时连接提示均通过。8 项界面原件检查见 `b13-signed-entry-checks.json`，重启草稿截图已查看。两次过渡期空根节点原件不纳入通过依据。新增自动回归构建 2 分 37 秒通过，执行进行中。

`onboarding-navigation-native.log` 扩展安卓回归通过：1 项、50.179 秒；从无模型草稿返回真实列表并点击恢复，发送连接提示和草稿保留同时通过。B13 已移出待处理区。准备把最终修复整合到预览线并执行授权发布，稳定工作树保持原提交。
