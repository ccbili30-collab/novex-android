# 嵌套模块、世界结构与编辑交互复查

执行位置：原应用接回工作树。依据 [用户确认的决策](../plans/2026-09-11-nested-card-editor.md)。不修改稳定分支，不公开发布。用户负责手机点击验收。

## 当前状态

第 04 轮完整集成编译通过（6 分 39 秒），编译后再次检查最终源码中的树变换、角色归属、保存/放弃及有效编辑目标，未发现新的代码级阻断。最终源码与构建清单逐文件一致，安装包身份、版本、签名与非调试标志检查通过，下载后文件校验值一致。

仅完成代码语义复查、编译与安装包检查；没有本轮手机、模型调用或操作验收通过结论，未运行自动化测试。不公开发布，不修改稳定工作树。

私有安装包：[下载开发候选三](/Users/noven/Downloads/Novex-嵌套模块-3.0.0-dev.3-20260911.apk)。版本 3.0.0-dev.3，版本号 300000003；包名 `com.noven.player.preview`，可按签名与版本规则覆盖预览版，尚未代用户执行安装。

SHA-256（文件校验值）：`d5e27eff22b2a941ed176927f35d6e1d0b50408ac46dec4efd756880bfe74eca`。

点击验收：[逐步验收清单](2026-09-11-nested-card-manual-acceptance.md)。B75、B77、B78 保留为待用户验收；只关闭已取得编译成功证据的 B76。

## 代码责任与语义检查

- 内容核心：模块有正文、子模块与子项排列；共同变换处理插入、移除、同级排序、跨父级移动和循环检查。布局不改变采用或管理。
- 存储：结构编码递归读写子模块，新字段可缺省以接收旧包。内容引用清单、复制、图片用途检查、编辑位置校验均递归；世界内角色依然是完整对象，复制后独立。
- 普通编辑：名称行与 Markdown（标记文本）正文，长文按完整临时内容引用分段编辑；图片编辑标记在保存时还原为自有资源和稳定图片块，导出复制不依赖编辑标记里的旧路径。代码围栏中的图片标记保留为文字。
- 对话运行：候选模块按树展开，每个稳定编号每轮只一次；仍独立选择模块，不把排版当注入。工具目录包含父级、子项顺序及排列；人工和模型经过共同编辑器、版本及占用规则。
- 页面：世界和角色共用图片头部、编辑模块树与渲染。插入线/容器高亮区分移动意图，菜单提供对应操作。预览读取草稿，退出比较真实结构/字节；编辑位置不造成内容变化提示。原应用对话等宿主保留。

## 首轮失败

`local/qa/nested-card-editor-20260911/nested-build-01.log`：页面调用内部 TextRangeEdits（文字范围编辑器）不可访问。修正为 ModuleMarkdown（模块标记文本）公共编辑入口适配，没有扩大内部工具可见性。第二轮完整编译已通过。

源码基线为同目录 before-source.tar.gz（开始前源码快照）；source-01.tar.gz（第一轮源码）和后续差量、哈希清单均保留。本地忽略材料不提交。

## 编译后仍需用户验收的重点

空卡头部；点击模块进入编辑；图片加入/删除原文件后显示；预览返回光标与滚动位置；无修改退出不询问；有修改退出保存/不保存；横向末尾新增、竖向末尾新增；同级排序和跨层级拖动；世界内部角色复制后独立；导出导入包含嵌套顺序、图片与内部角色；真实模型读写及采用。

## 后续语义复查修正

- 世界模块保存角色展示编号，角色仍独立属于世界。复制重映射展示编号；移除展示/模块不删角色，移除角色才清理其展示关系。
- 草稿已移除角色而正式角色仍可查看时，切换编辑前检查目标存在，防止页面进入无效目标（B77）。
- 删除模块仅清理其自身子树中的编辑位置，不清除其他角色的位置（B78）。
- 编辑中的图片标记按卡片自有资源解析；复制同一标记分配稳定的新展示编号。旧多文字块的可见正文等同，不因内部重新分块触发无意义的保存询问。
- 空容器不加入自动选料候选；选料目录说明父模块与子模块分别选择。显示布局不决定本轮注入。

## 最终构建与收口证据

| 构建 | 源码清单 | 结果 |
|---|---|---|
| 01 | source-manifest-01.json | 编译失败，原日志保留 |
| 02 | source-manifest-03.json | 完整构建通过，5 分 57 秒 |
| 03 | source-manifest-06.json | 完整构建通过，7 分 15 秒 |
| 04 | source-manifest-07.json | 完整构建通过，6 分 39 秒；最终安装包 |

全部日志、版本与签名输出、构建输入校验、源码基线和最终快照位于 `local/qa/nested-card-editor-20260911/`。交付元数据为 `delivery.json`。没有重新打公开版本、推送发布或操作用户设备；稳定工作树仍为 `06f60b6b7e7b1218ed2cf449b4237eb2373f65bd`。
