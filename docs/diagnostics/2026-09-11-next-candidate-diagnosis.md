# 下一候选包范围与病因核对

2026-09-11。初始诊断如下；当前已进入授权实施与构建，进度见文末。未发布。版本号体系暂不调整；稳定工作树不动。实际手机验收仍由用户进行。

## 已定位问题与下一候选范围

1. **B96 附件读取失败**：用户对话包中的直播mod.docx 为 9809 字节，已解析为 34 块、状态 ready（就绪）。首次用户消息没有手输文字。ChatViewModel.kt:6343 保存真实消息编号，但附件回执只进入 contentParts（内容块）；:7220 请求选择只接受非空 content（文字）或 imageParts（图片），漏掉纯文档附件。:8856 保存阅读记录要求请求编号非空，成功读取结果被改写为失败。修复须统一真实用户轮次识别，覆盖仅文件、文字加文件、队列、继续及历史重载；不能找不到本轮时误挂到上一条用户消息，不能把工具结果当用户请求，不能绕过阅读记录来掩盖故障。
2. **B97 无进展循环未停止**：原包 216.161 秒内 122 次调用；85 次 document_inspect（文档检查）完全相同错误，28 次 workspace_inspect（工作区检查）与9次 learning_prepare（学习准备）。文档检查参数有5种；任意最近30调用中文档检查最多24次。ToolLoopDetector.kt 的普通工具硬停止依赖同工具同参数无进展达到30次，而窗口仅30；穿插工具或换参数躲过阈值。ChatViewModel.kt:8039 即使工具被拦，也仅返回工具错误继续模型循环。修复应区分不可重试的软件前置错误与真正有进度的分页读取；无进展时终止当前自动循环、给出具体失败、保留已完成结果。不能对正常分页或剧情按总工具次数粗暴截断。
3. **B95 圆环验收未通过**：NovexChatIndicators.kt:57–76 使用自绘圆环，粗弧 Stroke（描边）未指定圆头端点；38dp画布再内缩3dp，视觉直径约32dp；中心9sp，并自定义万／千单位。恢复原组件圆润比例与圆头，恢复K / M，固定外圈、灰启用弧、黑使用进度及两种中心读数保持；深色模式对应反色。用户未提供本次圆环截图，具体手机观感仍待验收，不能声称视觉已复现。
4. **B98 首页空状态**：NovexIntegratedLibraryRoot.kt:99 的空状态 Text（文字）没有指定主题颜色；NovexLibraryFrame 仅设置背景，未提供相配套的内容颜色。截图黑底黑字与此代码缺口一致；同一路径的加载、导出、错误文字也有遗漏。空状态直接堆叠两个文字按钮及24dp空隙，未统一排版。修复世界与角色共享空状态、加载和错误呈现，并核对两种实际首页入口。目录仍读 LibraryModel 新存储，暂无证据表明截图用户丢卡；加载失败不得冒充空库。附件对话包导出时新卡索引为空，也不能据此推断另一截图设备的数据。
5. **候选更新分流**：当前 UpdateChannel 只有稳定与预览，免费模型包仍同属预览。UpdateReleasePolicy 仅按预发布状态与固定附件名筛选。TemporaryPreviewModel.install 在无内置凭据的包中会移除临时配置，所以装普通更新会失去免费模型。下一候选新增明确分流标记及各自更新附件/目标匹配；普通只取普通，免费模型测试只取专用，无匹配则无更新。检查、下载、待安装记录及安装前校验必须一致；免费密钥不进入普通产物。保持原应用标识、签名及用户配置；首次专用候选手动覆盖安装，后续跟随专用更新。现有临时凭据有效期不会因升级延长。不公开发布、不改版本号管理。

## 验证边界

已直接读取用户归档并统计执行顺序、失败结果、参数变化、文档状态；已对照当前实际代码定位上述调用链。未在设备重演、未运行完整应用回归，不称已修复。实施阶段先建立覆盖真实附件入口及原调用序列的失败检查，再修复及编译；视觉验收由用户点击。内部原包与凭据不提交。

## 收口条件

附件可真实读取并关联当前用户消息；同一前置错误不循环；正常分页仍继续；圆环外观单位符合约定；亮暗首页状态可读且加载错误不伪装空库；两类候选更新不会互相匹配，临时模型只出现在专用产物。此前容量计算与估算/实测区分保持，本轮不重新扩大到旧全量计划。

## 追加范围与实现进度

用户已确认将 21:19 新反馈一起加入本候选包。

- 分类存在确定的接线错误：IntegratedCatalog 提供新存储目录，但 RoomNovexWorkGroups.replaceMembers 通过旧表 targetExists 校验对象。纯新世界不在旧表，会被拒绝；纯新角色也有同样风险，不能因用户旧角色成功就宣称角色分类全正常。现改为生产入口注入新目录对象校验，旧数据兼容地址仍映射新卡，文件继续查文件表；删除对象与错误类型仍拒绝。已补充新世界、新角色、子目录、重新读取及删除对象检查。
- 新归档为候选十二，只有两条用户消息，无装配与用量记录。对话启用 128000；导出时模型配置声明 1048576，声明不等于服务商实测。单个空名模块正文 192060 字节、117900 字符，本地保守估算 133627 词元。原始导入文件另存 246318 字节。自由模式应能分段，不应因全文大于本轮预算就全部阻断。
- 已用实际原卡的选料摘录请求临时模型，返回正常模块选择：520 输入、30 输出词元，约 0.6 秒。该检查仅证明此次选料请求成功，不冒充历史失败复现或整卡游玩通过。原卡已接入独立的预算与请求装配检查，等待构建结果。原文保留在忽略目录，不提交。
- 原定五项已实现；完整构建首次发现停止变量作用域与颜色别名问题，第二次发现稳定版历史记录误受预览附件过滤，均已修正。第三轮两种包分别通过 1655 项检查，0 失败、3 环境跳过。原 DOCX 真实解析与阅读记账通过，34 块。随后补充持久化系统提醒不得抢占请求归属，再收到上述追加范围；中间构建不作为最终交付。
- 分流复查发现三处预览诊断入口仍只认普通渠道，已让免费模型渠道同样保留诊断与对话包入口。

最终检查、签名、产物摘要及手机验收边界在交付时补录。K / M 按用户要求原样显示，不附单位翻译。

## 原卡实测与候选十三免费包

- 原卡全文直接容量请求成功，服务端实际输入 51269、输出 1 词元，约 2.4 秒。只做容量检查，不作为剧情质量验收。本地 133627 是保守估算，不能把它等同于服务商实际用量。
- 原卡在实际 IntegratedCards 与 NovexContextComposer 的联合检查中，以 100000、64000、8192 剩余资料预算均通过：分段内容不超预算，后续读取入口存在，装配保留所选内容；原始卡片不改。检查明确打印 private-original（用户原件），未用合成卡冒充。
- 本次未复现用户候选十二的历史阻断，原归档没有保存完整报错；已请求补充当时提示，B89 不关闭。没有把所有异常改称“卡太大了”，也没有取消容量保护。
- 免费模型候选十三最终检查 1658 项：存储 71、运行层 94、应用 1493；0 失败、3 环境跳过。新增分类检查通过。追加检查首次有一处测试接口类型参数编译错误，已修正；原失败保留在 candidate13-fifth-free.log。
- 免费包 SHA-256：`5d865318c7519707227ae6a7c9b4a077b9f7d087803e618eb507acb92627f320`，59936494 字节；预览应用标识、300000013 版本序号、原预览签名及 preview-free（免费模型预览渠道）标记已核对。未公开发布。普通包仍在最终构建，手机验收由用户进行。

## 最终交付

普通包亦已构建通过，相同 1658 项、0 失败、3 环境跳过。普通包 SHA-256：`2cb1b8a879b37eb0966c6b48b280a1885f1bc14b606f171fac776525ba6a2de4`，59936414 字节，渠道为 preview（普通预览）。两种包均为 `com.noven.player.preview`、`3.0.0-dev.13`、`300000013`，原预览签名摘要 `cab4226b416183671281253b5f4000a28885cfa4f48146c0e7798d137e41c6a6`。

1439 个源码文件的本地／远端构建摘要全部一致。已核对普通包不含临时凭据、专用包含临时凭据；源码归档和构建日志不含该凭据。自动检查报告、所有本轮失败与最终构建日志保存于忽略目录 `local/qa/candidate13/evidence.tar.gz`，产物检查为 `artifact-check.json`。稳定工作树仍是 `06f60b6b7e7b1218ed2cf449b4237eb2373f65bd`，无改动。

- [免费模型专用安装包](/Users/noven/Downloads/Novex-免费模型测试版-3.0.0-dev.13.apk)
- [普通预览安装包](/Users/noven/Downloads/Novex-普通预览版-3.0.0-dev.13.apk)
- [手机验收步骤](../../local/qa/candidate13/acceptance.md)

没有公开发布，没有安装或清除用户设备数据。手机外观与真实操作由用户验收；线上双渠道更新仍待实际发布后验收。B89 的本次历史阻断未复现，保持待核实，不能宣称所有问题已经排除。
