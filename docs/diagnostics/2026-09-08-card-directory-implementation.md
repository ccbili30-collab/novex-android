# 卡片专属目录实施记录（进行中）

第三批主链路已接入，尚未全部验收或发布。第二批世界书检查见独立记录；剧情插图已接入并在第四批验收，用户追加的模型创作反馈与上下文容量问题仍排队尾。

## 已验证协议

`local/qa/card-directories-20260908/fast-protocol-2.log` 通过领域快速检查，包含目录写入失败、准备后未发布、独立复制、内容损坏及越界路径检查。`native-protocol.log` 安卓两项通过：真实目录同步／重新读取，以及世界书多模块范围选择。

第一次协议构建因安卓未公开对应目录常量失败，改用只读方式打开已确认的目录后通过。这个失败是构建问题，不是发布应用中的数据故障。

## 正在接入的部分

- 数据库新增目录指针，与卡片保存位于同一事务；完整正文和附件在发布指针之前落盘。外层对话事务回退不发布新目录。
- 新卡、模块修改、图片、引用、角色版本、卡片复制与导入接到统一保存命令；旧卡按单卡事务迁移，个别损坏图片不阻塞应用启动。新目录不进入用户卡库选择列表。
- 原生卡片文件供交换；完整本地记录另存，引用保存稳定编号，不递归复制其他卡全文。角色本体与其他版本仍归属于同一人物。
- 模块代表图及已有集合条目的图片复用一个选择流程：设备、创作库、图片直链 → 预览 → 保存。新条目先正常保存再放图。来源信息随本卡保存，不依赖原创作库继续存在。
- 目录附件改用文件流复制与校验，避免普通改字把全部图片读进内存；同卡不可变旧修订可复用未变化的附件文件，不跨卡共用这一目录内的文件。需要最终增量验证。
- 清理仅针对超过保留期且没有已提交目录指针引用的修订；旧局图片继续由现有快照保存体系保护，不依赖源卡目录。

## 仍待验收

数据库迁移和外层回退、三类卡实际创建与复制删除、旧卡重复迁移、图片直链失败／取消／保存、集合条目图片、原生截图、旧局保护、最新源码领域回归。当前构建日志和失败均保留；不将早期协议通过表述为全部接入通过。

## 本轮接入检查进展

- `integration-3-results.tar.gz` 保存 369 项应用测试结果，368 项无失败（其中 1 项跳过）；唯一失败为图片解码测试切换安卓版本后，电脑测试环境未能下载对应运行依赖。未将它算成通过。真实命令全过程、外层回退、独立目录与附件流式复制均已通过该次检查。
- 发现并补齐实际遗漏：普通文字模块的代表图原先没有随原生卡包导出，解析端也只在图片模块接受代表图。现在各模块形态统一收录代表图并在导入时还原。
- 安卓 `native-images.log` 三项通过：真实图片解码／分享页与过大响应拒绝、真实目录同步、模块直链预览并保存。首次界面测试的输入定位器错误已保留在 `native-images-locator-failure.log`；修正定位器后通过，不是应用输入框缺失。
- 最新迁移检查增加了版本 32 升级到 33、正文不变及重复迁移稳定；第一次构建因测试调用语法错误失败，已修正，待最终回归结果。
- 快速检查脚本为远端构建关闭标准输入，避免已完成的构建仍被远端进程占用连接。此前发生的输出通道滞留不作为测试通过证据，以保存的结果文件为准。
- 回退说明：本次数据库从 32 升至 33。不能承诺直接安装旧包可降级读取；发布前需保留完整数据备份，出现问题优先前向修复，必要时恢复升级前备份。

图片取消／失败后重试、可读图片截图和剧情条件接入后的完整回归还在补验。

`local/qa/story-illustrations-20260908/fast-story-2.log` 最新领域快速检查通过（8 分 2 秒），包括本批版本迁移、重复迁移与卡片目录测试。图片实际入口加强验收与最新截图继续随下一次安卓检查完成。

## 旧创作图片归属补齐（已通过最新定向检查）

旧版生成图通过创作成果关联显示，不能只接通新图片按钮而留下借用路径。当前增加同一保存命令的迁移桥接：仅迁移原来实际显示的模块图片，复制进入本卡；保留来源关系，移除来源关系不删本卡副本。显式移除图片会持久保存已管理标记，旧关联投影及重复迁移不再把它恢复。随后以纯标记文本修改正文也保留该标记及图片条件。新增覆盖迁移、重复运行、源成果删除、换图、移图和修改正文的回归样例，尚待最新运行结果。

2026-09-09 收尾：上述新增样例已在 fast-final-domain-3.log 通过。原生图片失败／取消／重试、条目图移除保留主图及正文、条件草稿与保存，在 native-targeted.log 的三项定向检查中通过；同组完整聊天还核对了原卡移图后旧消息图片仍显示、存档仍可打开。先前失败和测试准备修正见 NATIVE_STALL.md，不覆盖用户所有历史数据与手机型号。当前未发布。
