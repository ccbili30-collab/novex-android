# 剧情插图接入（软件定向验收完成，未发布）

- 图片条件编辑放在已有模块图片下；默认按需选择，可填关键词与间隔，并以短剧情试命中。完成仅更新当前草稿，仍由模块“保存”写入原卡。
- 图片名称、说明、展示条件、资料使用条件与附件共同进入采用快照；共享原卡之后修改不替换旧局。
- 候选复用本局实际启用的图片范围，管理挂载不等于采用。只返回分页名称与说明，不发送图片像素。通过标准工具显式选图仍沿用对话的三种权限。
- 自动展示只在正文正常完成后判断，跳过工具返回与思考，按作者顺序选择一张，默认不连续自动展示同图。显式选择优先。
- 所选图片写入当前助手消息的额外内容块。模型历史忽略这个展示块；界面恢复时读取已保存选择，不重新匹配；对话包既有媒体扫描会收录相应受保护附件。
- 第一轮编译发现请求编号变量使用错误，已修正；当前仍在编译和回归，尚未宣称实际对话链路通过。

待验收：领域选择与停用隔离、原生条件入口、真实消息流程和重开、分支间隔、终止不展示、对话包包含原图。Agnes（模型）创作反馈和 DeepSeek（深度求索）上下文容量仍排在本批完成之后。

`fast-story-2.log` 通过领域快速检查，包含自动顺序、同图间隔、显式选择优先、停用世界书隔离、采用图片与条件冻结、原卡删除、卡包交换、消息展示块保存及模型内容不包含图片块。尚未以此代替真实请求流程和界面验收。

## 最终回归进度

测试主机于 23:00 后短暂离线，已恢复。离线期间那次界面结果已取回：109 项中 108 项通过、1 项失败，原因为图片失败提示的重试按钮直接使用底层控件，违反项目共用视觉组件约束。已改回统一按钮，等待重跑；原始结果留在 `local/qa/story-illustrations-20260908/ui-final-results.tar.gz`。

插图选择和消息恢复协调已从大型聊天状态文件抽出独立模块；聊天状态只提供本局配置、当前分支和完成事件。新增对话包两条回复分支分别保留原图字节的检查，尚待运行。

`fast-final-domain-3.log` 最终领域检查通过；保存的结果包 `final-domain-results.tar.gz` 包含应用 373 项（1 项跳过）和核心 135 项（1 项跳过），零失败和错误。新增双分支图片导出检查通过；上一轮该样例遗漏必需的会话环境，失败原件保存在 `bundle-fixture-failure.xml`，补齐样例后重跑。原生操作及真实模型完整中文旅程尚在验收中。

最终安卓检查：完整 12 项中最初 10 项通过、2 项失败；失败均保留。两处自动滚动等待、界面刷新竞态和损坏图片样例经定位修正后，相关 3 项定向重跑全部通过（native-targeted.log），其余 9 项此前已通过，生产代码未再变更。新增诊断捕获一度因存档弹窗有两棵节点树而失败，修正捕获后通过，没有删掉业务断言。

实际聊天软件链路经过 12 次可控模型请求：身份、记忆准备／写入／回读、文游启动、仓库写入、正式存档、状态展示、本局世界书停用、自动插图、主动查看和选图。重建对话运行实例前移除原卡图片，恢复后的两条消息仍各自显示保留图片；存档详情仍可打开。截图见 local/qa/story-illustrations-20260908/persisted-story-image.png。实际卡片目录保存、条目图片移除保留主图与正文、条件草稿／保存分别通过。真实模型理解验收仍单列，不把可控回复当作自然理解。
