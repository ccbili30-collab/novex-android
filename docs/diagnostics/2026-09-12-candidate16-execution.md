# 候选十六：实施中，未发布

用户授权合并修复并发布专用内测。保留普通预览/稳定通道，不修改稳定工作树。

## 已写入的修改

- 模块拖动浮层使用横纵坐标；目录预览补空标题回退、摘要、文件夹/正文图标、递归缩进，缩减条目额外间距；摘要过滤基础标记语法。
- 对话展示条目与滚动状态由同一个会话模型保留，返回页面不先重建空列表。
- 资料缓存核对请求、关联、正式修订及选择输入；仅采用的对象工具读取正式版本，管理编辑仍可读取草稿。
- 辅助选料失败且有分页能力时，对候选正文分配预算并读取实际初始页，剩余正文由正式回答模型按需读取；无分页能力保留阶段错误。资料准备错误不再一概描述成网络错误，失败保留已有用量读数。
- 为发送前容量判断和异步准备补异常边界；此项不是用户首次闪退已经复现/解决的证明。
- 用户卡片导出使用第二版交换包，将正文、图片、扩展分目录且带名称/扩展名；结构编解码及长度/散列校验保留。读取器兼容旧第一版编号包；内部存档默认仍输出第一版。旧版本应用未支持第二版，不能宣称向旧应用反向兼容。

## 已执行检查

本机现有 Java 17（运行环境）与隔离构建缓存运行真实核心源码：内容存储 72 项、对话运行 95 项，合计 167 项，零失败、零错误、零跳过。新增正式版/草稿读取隔离及可读分装导入还原检查。完整日志和隔离源码在 `local/qa/candidate16/core-check.log`、`core-check/`，结果为 `core-results.json`。

代码复查修正了异常保护误放位置，当前保护在真实 sendMessage（发送）准备阶段。`git diff --check`（差异空白检查）通过。应用层未编译，不能把核心层编译当安卓界面通过。

## 剩余与阻塞

`ssh win-zhz` 连续三次连接超时。当前本机找到核心构建环境，但没有找到可用安卓开发工具包；既有云构建工作流的签名日常包要求 next 分支且是普通预览通道，未为赶发布推送 next 或改动通道约束。

还必须完成应用层编译与针对性检查、界面/拖动及导航运行核验、首次闪退追查、签名和包内容核验，再发布专用内测。现在没有候选十六安装包，没有发布或移动任何更新通道。

开工前源码已在忽略目录保存 `before.sha256` 与 `before.tar.gz`；本次源码/测试文件的差量、全源码校验及远程构建脚本已准备在 `local/qa/candidate16/`。旧候选十五原始用例保留，临时测试模型凭据继续仅经钥匙串/标准输入，未写入源码或文档。

## 最新对话包发送链修复

针对 `8ac4894c` 对话包证明的空返回、截断结构、越界编号等选料失败，新增统一恢复解析。无效辅助结果不再等同于“没有相关资料”：具备分页能力时，在实际预算内分配各正文块初始页，保留续读位置和原文；明确返回空选择仍保留。记录使用恢复读取原因，不冒充模型选择。必带内容与只读边界未放宽。

准备失败尚未产生本轮回答时，界面使用本次发送错误并恢复待发内容；数据库兜底更新也要求回答位于最新用户消息之后，避免把错误附到上轮已完成回答。

重新运行核心检查：内容存储 72 项、对话运行 99 项，总计 171 项，零失败、零错误、零跳过。新增 4 项覆盖上述失败形式、明确空选择及多块实际正文预算读取。应用层错误归属完成代码复查，仍待安卓编译和运行核验。原用户包无闪退堆栈，不能由这些检查宣称首次闪退已解决。

最新源码差量和校验清单已重建。此次重试远程连接仍超时，未生成或发布候选十六安装包。

## 崩溃日志后的本地工作

补系统退出证据、页面／生成／服务事件诊断，及资料缓存容量／分页权限失效条件。服务早登记已在本轮基线存在，不冒充本轮新增修复。最新原生主动中止仍未定位。详见 `2026-09-12-crash-local-review.md`；独立诊断类编译记录为 `local/qa/candidate16/diagnostics-compile.log`，不等同于整个安卓应用通过。

## 远程恢复后的完整构建

用户告知远程恢复后，连接已实际成功。候选十六源码差量与全源码校验已传至既有隔离构建目录；开始专用内测与普通渠道对照构建，临时模型凭据仅经标准输入传递。未改预览／稳定工作树，普通渠道对照包不发布。最终结果待构建回执。

故障编号去重统计：115 项，63 项已有验证记录、52 项未关闭；52 项中，本候选 7 个编号、另 33 项已改待验、12 项部分未修或证据未齐。统计是故障表口径，不将历史局部验证冒充最新整产品验收。

### 专用内测完整构建通过

远程专用内测构建成功，用时 12 分 52 秒。内容存储 72 项、对话运行 99 项、应用 1503 项，总计 1674 项，0 失败、0 错误、3 项环境跳过。源码校验通过，编译后复查保留原生中止未定位、手机交互未验边界。

安装包 `Novex-免费模型测试版-3.0.0-dev.16.apk` 已下载至用户下载目录；包名 `com.noven.player.preview`，版本码 `300000016`，签名 SHA-256 为 `cab4226b416183671281253b5f4000a28885cfa4f48146c0e7798d137e41c6a6`，与候选十五相同。文件 SHA-256 为 `185a0ed905f71eef572dc7681451fbbd14b684678490e3f2169f7b11f730f624`，59948378 字节。已确认临时测试模型存在。普通渠道对照构建尚在执行，未发布更新。

### 双渠道最终核验

普通对照构建也成功，用时 8 分 46 秒；同为 1674 项、0 失败、0 错误、3 项环境跳过。双包临时凭据隔离通过：仅专用内测包含；源码差量及本地构建日志未含该凭据。构建后 1446 个源码文件校验全部通过。

普通对照包留在本地忽略目录，未发布。专用内测包交付用户覆盖安装。新版不将原生中止列为已修；验收步骤在 local/qa/candidate16/acceptance.md。
