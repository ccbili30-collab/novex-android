# c7892c9d 对话包诊断

本次仅补充诊断证据，未新增业务修改。

环境版本为 3.0.0-beta.1，版本码 300000001，模型 glm-5.2，容量记录 128000。共 123 条消息。

按文件从零编号：第 116 条包含 1587 字符剧情，标记 execution=true（执行过程），随后调用 update_playthrough_state（更新本局状态），成功后第 118 条只有 10 字符收尾。用户投诉后，第 120 条再次生成 3234 字符剧情，仍标为执行过程；状态更新成功后第 122 条只有 20 字符收尾。原剧情完整保存在包内，不是未生成或压缩删除。

与 B06 已定位的工具触发正文误折叠机制吻合。当前集成代码 AssistantStreamTurn.kt 不再因工具将正文标为执行过程；ChatViewModel.kt 工具循环保留正文，旧消息载入明确忽略旧隐藏标记。候选十已有修复记录；本包不能证明新候选复发，仍需手机重开原会话验收。

另有一条 HTTP 404（模型路由不存在），其后仍有多轮回复，不能据此断言永久阻塞。原包保留在用户下载目录，不提交原始内容。
