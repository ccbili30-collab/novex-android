# 2026-09-06 已确认修复执行记录

用户已在问题盘点与修复方案之后明确要求“开始修复”。本文件承接问题清单，保留全部范围；盘点文档中的暂停状态仅描述盘点当时。

当前基线：`129580c`，预览工作树 `next`（下一预览版）。仅修改预览线；正式分支与稳定工作树不变。移动端安装、启动与点击由用户负责。自动测试完成后使用日常快速通道构建、推送和发布。

## 完成判据

- [x] A01 卡片创建意图与应用内成品默认行为（共享协议与实际提示词链自动测试，真实模型表现待用户验证）。
- [x] A02 来源结构到内容模块的主动组织协议（同上，不以字符串测试冒充模型行为验证）。
- [x] A03 回答人格与实际工具授权独立（实际工具集合与人格组合自动测试）。
- [ ] A04 新对象、模块、顺序与引用形成完整可验证成品。
- [ ] T01 内容契约可查询，写入、渲染、编辑、上下文与导出一致。
- [x] T02 模块局部修改保留其余字段，参数说明与实现一致（自动接口测试）。
- [x] T03 文游启动方式公开稳定值与可执行错误说明（自动接口测试）。
- [ ] T04 多轮创建意图持续有效，真实执行确认不被绕过。
- [x] D01 文档读取按字符预算聚合小块（学习执行器分批另属 D05—D07）。
- [x] D02 保留原始标题；无标题样式时以明确中文章节标题提供可追溯索引回退（接口测试）。
- [x] D03 续读与冲突参数有明确、可靠的恢复方式（接口测试）。
- [x] D04 支持从明确内容位置开始读取（接口测试）。
- [x] D05 预检与执行共用真实分批计划（含保存进度后的剩余批次；核心与应用链自动测试）。
- [x] D06 词元估算、输入输出预留与执行预算一致（保守预留与提供商计数明确区分，不冒充精确账单）。
- [x] D07 超长条目二次切分及分层笔记综合（含来源覆盖、部分完成、笔记回读及恢复；模型内容质量待用户验证）。
- [ ] U01 主题选择、取消、保存、恢复和重启链路。
- [x] U02 共享内容渲染遵循应用字号设置（组件边界测试、实际源码检查及编译；实际布局待用户验证）。
- [ ] S01 前台服务超时崩溃诊断与修复。
- [ ] 历史返回、菜单、公告和模块类型回归自动检查。
- [ ] 真实任务的跨层自动验证、独立提交与预览快速发布。

## 测试边界

沿用用户已确认的共享领域命令、内容读写、渲染投影、文档工具和运行时接口；先失败测试后修复。实际模型行为与移动端点击不冒充已被单元测试证明。

### 第一条回归

新增 `NovexContentReadbackTest`（内容回读测试），通过真实内容编码/解码与页面、上下文共用的纯文本投影，验证集合条目的完整说明不会消失。

- 红：`129580c7c518-dirty-20260906T010758Z-48389-novex-domain.log`，95 项中仅新回归失败，确认集合正文被漏读。
- 绿：`129580c7c518-dirty-20260906T011213Z-48652-novex-domain.log`，领域增量测试通过，完整说明与无名称条目均保留。
- 此项只是 T01 的回读子项，不能标记整个 T01 完成；未构建安装包。
- 下一组接口回归：局部编辑保留未指定字段，以及文游启动方式的可查询、可执行稳定值。

### 第二组回归

- 红：`129580c7c518-dirty-20260906T011803Z-49034-novex-domain.log`，97 项中两项新增用例失败：仅重命名会把正文写成 `{}`；查询没有启动方式表。
- 绿：`129580c7c518-dirty-20260906T012012Z-49181-novex-domain.log`，领域测试通过。局部编辑在执行事务中读取当前模块，不补空默认值；文游启动方式提供四个稳定值、中文名称和包含合法值的错误。
- 未进行移动端操作或真实模型调用；检查项完成仅表示相应接口实现和自动测试完成。
- 独立提交：`63bf68b`。

### 内容契约与共享编辑

- 红：`129580c7c518-dirty-20260906T012607Z-49514-novex-domain.log`，四项新增用例失败，分别是缺少可用示例、接纳不可渲染对象、编辑丢扩展字段、未知旧对象被当成空文本。
- 修复后领域测试本体通过（`63bf68be2b9c-dirty-20260906T012917Z-49699-novex-domain.log`）。当时改动正在运行的脚本，使它结束时读取偏移出错；不是产品测试失败。后续不再修改执行中的脚本。
- 新一轮完整快速检查：`63bf68be2b9c-dirty-20260906T013536Z-50082-novex-domain.log`，119 项中仅下一组人格/协议的两项新增测试失败；117 项通过，含内容契约、原有内容编解码、共享编辑及展示投影回归。
- 独立模块页复用 `SharedModuleDocumentFields`（共享结构化字段编辑），不再将模块降级为纯文本；图片/引用刷新保留正文草稿，返回采用已有草稿保护。图片/引用仍是即时保存操作，页面已明确说明。
- 仍需随整批复验原生卡导出链路；移动点击和真实模型行为由用户验证。

### 人格与产品操作协议

- 红：`63bf68be2b9c-dirty-20260906T013536Z-50082-novex-domain.log` 的两项新增测试证明角色人格误过滤标准工具，两条人格路径也未共用完整创作协议。
- 绿：`bd7070cd29a9-dirty-20260906T014000Z-50298-novex-domain.log`，领域增量测试通过。协议明确应用内成品、按来源组织模块、真实用户确认、完成后回读以及资料不等于授权。
- 能力测试使用实际工具工厂的输出，确认角色不再删掉合法管理/工作区/文游工具，也不能添加模型或配置未提供的生图、记忆、原始命令工具。
- 旧角色编辑器去掉误导性的“角色工具默认关闭”开关；历史交换数据字段保留。旧角色提示词回归随文档链测试继续运行。
- 这不是 A04 的完整成品创建修复；新增对象携带模块的接口仍单独实现，不宣称已经修完全部人工智能问题。

### 紧凑通读与可靠定位

- 红：`bd7070cd29a9-dirty-20260906T014617Z-50583-novex-core.log`，88 项中 3 项新增用例失败：1154 短行无法直接通读、指定块位置未生效、游标错误缺恢复说明。
- 绿：`bd7070cd29a9-dirty-20260906T015234Z-51015-novex-core.log`，89 项通过。默认紧凑通读以 24000 字符控制本次正文，连续短行合成带来源范围的段落组；5000 是扫描防护上限，不再把 20 行当作阅读预算。原始块与锚点不改写。
- 覆盖 1154 条短行一次读完、正文预算内跨块/跨游标无丢失、块范围续读、非法游标与互斥定位参数说明。`view=blocks`（逐块详情）保留原有详情能力。
- `first_block/last_block`（来源块范围）从 1 计数；续读返回原样游标及明确的下一块位置，禁止模型自造游标。
- `bd7070cd29a9-dirty-20260906T015317Z-51013-novex-document.log` 的应用层附件、文档工具、原有角色提示词及请求链测试通过。
- D02 另有失败用例：`f5c8b61709be-dirty-20260906T020009Z-51458-novex-core.log`，90 项中仅无样式中文章节导航失败；正在实现只读派生索引，不更改源文件。
- D02 转绿：`f5c8b61709be-dirty-20260906T020758Z-52114-novex-core.log`，核心测试全部通过。检查、标题定位和附件回执复用同一派生目录；原始样式优先，普通叙述及流程编号不擅自猜成章节。索引明确标注推断来源和块范围。

### 全局字号与历史回归

- 新增组件边界测试先失败，规则接入后实际源码检查列出共享内容渲染器 21 处固定字号/行高。
- 已全部接入现有 `novexScaledSp`（应用字号缩放函数）；6 项边界测试与整个页面源码边界检查通过，未进行设备点击。
- 对话返回、菜单与公告动作的原有静态探针再次通过；快速通道脚本及测试计划回归通过。
- 主题恢复与前台服务超时仍未宣称修复；不能把字号修复、静态返回检查当作这些现象的根因证据。
- `f5c8b61709be-dirty-20260906T020059Z-51456-novex-domain.log` 已编译字号修复，121 项中 119 项通过；仅 A04 的两项新建成品回归按预期失败，确认原实现忽略初始模块且不验证其格式。

### 带完整初始模块的新建成品

- 红：`f5c8b61709be-dirty-20260906T020059Z-51456-novex-domain.log` 的两项回归分别证明创建丢弃初始模块，以及错误初始正文仍能进入提案。
- 绿：`7215eea7d3fa-dirty-20260906T020917Z-52111-novex-domain.log`，121 项领域与共享内容回归通过。
- 世界、角色与文游创建参数均支持有序 `modules`（模块数组），复用已有完整页面保存命令。接口测试分别提交 25 个模块，核对顺序、完整正文、独立编号、确认前零写入及单一事务入口。
- 所有初始模块在提案前检查作用域、重复规则、名称与可渲染正文；错误不再等创建容器后才暴露。空模块仍允许；没有提供初始模块时，提案明确告知。
- 这组测试验证管理服务到真实领域命令的完整载荷，不是设备数据库事务回滚实测。跨卡引用仍需要返回真实编号后另行确认添加；整卡回读、原生导出与跨层验收继续属于 A04/T01 未结清部分，因此未把这两项整体勾掉。

## 本轮继续执行记录

### 服务生命周期回归（S01 子项）

- 主机端运行真实 `AgentForegroundService`（人工智能前台服务）的生命周期，通过 `Robolectric`（安卓主机测试运行环境）观察通知登记，而非用源码字符串断言冒充运行。新增依赖仅用于测试，不进入安装包；未使用设备。
- 红：`c53589da2f61-dirty-20260906T022001Z-52639-novex-domain.log`，123 项中仅两项新增服务回归失败：创建阶段没有前台通知；首命令是停止时也未登记通知。
- 绿：`c53589da2f61-dirty-20260906T022405Z-52948-novex-domain.log`，123 项领域与服务回归通过。最小通知在创建入口登记，早于唤醒锁、悬浮窗及仓库访问；安全模式只清理已登记通知。
- 证据边界：修复了可复现的“登记晚于初始化／停止提前返回”路径。用户日志没有服务创建入口的时序证据，尚不能断言这就是那两次真实超时的全部原因；主线程调度和系统重启延迟仍未排除。因此 S01 不整体勾选，也不以主机测试冒充手机闪退消失。
- 初次登记保留一条无正文、无凭据的耗时日志，帮助区分服务已进入但登记晚，与根本未及时调度的情况。
- 主机测试配置依据：[官方配置说明](https://robolectric.org/getting-started/)、[服务通知观测接口](https://robolectric.org/javadoc/4.16/org/robolectric/shadows/ShadowService.html)。

### 学习短行与超长块（D05/D07 子项）

- 红：`c53589da2f61-dirty-20260906T022244Z-52813-novex-core.log`，93 项中两项新回归失败：1154 短行被拆成许多批次；单个长块突破正文预算。
- 绿：`7e568d6acbc2-dirty-20260906T022929Z-53309-novex-core.log`，93 项核心测试通过，含真实状态编解码后的中断续读。
- 片段范围以来源文档、块编号和字符偏移持久化；源块内容不改写。分段优先选择附近换行边界，不能切断表情符号。只有全部片段成功通读，才将整块计为已完成。5000 块只是扫描防护，正常批次由正文预算决定。
- 学习状态格式升至第 5 版，兼容读取第 1—4 版；旧整块进度继续有效。预检接入同一计划、输入输出预算统一及分层综合仍未完成，不整体勾选 D05—D07。

### 分层综合与付费进度恢复（D07 子项）

- 红：`5b6931ec223b-dirty-20260906T023227Z-53522-novex-core.log`，94 项中新增综合用例失败，原实现一次送出全部笔记。
- 第一轮修复后综合及续跑断言已通过，但测试发现 800 字符短块也被拆散来凑满批次（预期 6 个完整段落批次，得到 5 个碎片批次）。已收紧为：能放进一个新批次的段落保持完整，只有超长块才二次切分。
- 绿：`5b6931ec223b-dirty-20260906T023712Z-53820-novex-core.log`，94 项核心测试通过。分层综合保留全部叶子笔记，通过持久化来源关系只选择尚未综合的笔记；总结阶段退出后可以恢复，不重复已完成的小结调用。
- 单条过长笔记可以只读切片；阶段输出不按约定缩短时，保存本次用量并暂停，不继续自动付费重试。应用发送给模型的请求包含相同阶段长度约束。
- 边界转绿：`5b6931ec223b-dirty-20260906T024459Z-53983-novex-core.log`，96 项核心测试通过，含巨大单条笔记和拒绝缩短时的用量记账。应用侧 `5b6931ec223b-dirty-20260906T023916Z-53985-novex-document.log` 编译及附件、文档工具、学习工具、工作区和原角色提示词链回归通过。
- 6 项组件边界测试与整个页面边界检查再次通过。这一组没有安装包、没有真实模型账单、没有移动端点击证据。
- 状态格式第 6 版增加笔记之间的来源关系，兼容第 1—5 版，不删除原笔记或文档。
- 额外发现的链路缺口：当前应用只有保存学习状态与原生进度控制，没有把所存笔记接到对话的工具回读入口。检查范围包含 `novexLearningRepository`（学习仓库）的全部应用调用点以及笔记引用的工具注册；这必须随学习链补齐，不能仅凭任务状态“完成”宣称模型已经可以使用笔记。

### 接下来沿现有范围修复的接口问题

- D05/D06：应用预检按每三字符约一词元估算，执行按每字符四词元加预留预算计算；必须共用真实请求与分批计划。模型窗口、输出预留、未知窗口、来源缺失及大数溢出统一处理。
- D06：继续学习时当前提供商来自当前对话，而任务记录绑定原模型；恢复前也需要校验模型及预算边界，不能只在首次确认时校验。
- D07：补齐笔记的可定位回读，让对话能使用阶段成果。只提供当前分支已授权资料集的笔记，不开放应用私有文件系统。

### 实际请求、分批与预算（D05/D06 子项）

- 模型窗口回归先失败：`293df562997e-dirty-20260906T024827Z-54601-novex-core.log`；首次转绿：`293df562997e-dirty-20260906T025016Z-54715-novex-core.log`，97 项通过。预检现在保存模型窗口与输出上限，状态格式第 7 版兼容读取旧格式；旧任务缺少窗口时明确拒绝静默运行。
- 长标题回归先失败：`293df562997e-dirty-20260906T040446Z-56060-novex-core.log`，98 项中仅新增测试失败。该次构建已结束，但安全外壳连接未退出，后续关闭了这条残留本地连接，没有重跑或停止仍在执行的构建。完整请求预算转绿：`293df562997e-dirty-20260906T040806Z-56256-novex-core.log`。
- 不再只数正文：共享请求对象包含实际发送的系统指令、资料/笔记标题、章节上下文及正文；应用适配器发送完全相同的字符串。连续短行不再逐行重复长编号；来源片段范围仍完整存入笔记。
- 输入预留采用完整消息的 UTF-8（统一字符编码）字节数与消息封装余量，是保守预算，不是精确词元计数或账单。输出与输入共同受模型窗口限制，未知窗口不能默认为安全。提供商返回的用量另行记录。
- 预检轮数回归先失败：`293df562997e-dirty-20260906T040929Z-56370-novex-core.log`，99 项中仅新增用例失败；转绿：`293df562997e-dirty-20260906T041136Z-56496-novex-core.log`。预检与执行共用真实通读请求计划，提供明确通读批次和输入预留量；综合次数/时间仍标为估算，不冒充精确耗时。
- 应用侧预检接入实际解析快照，不再以正文除以三估算一套、执行再采用另一套。确认编号绑定解析版本、实际文字、模型窗口和输出上限；来源快照缺失会明确列为不可读取。
- 提供商超出预留用量回归先失败：`293df562997e-dirty-20260906T041328Z-56639-novex-core.log`；转绿：`293df562997e-dirty-20260906T041447Z-56748-novex-core.log`，100 项通过。先保存已完成笔记、阅读进度和实际用量，再暂停后续付费调用；状态重新编解码后不丢失超过预算的已发生用量。累加比较使用宽整数，防止溢出被误判为尚有预算。
- 最后综合已完成但因用量异常暂停的恢复回归先失败：`293df562997e-dirty-20260906T041657Z-56909-novex-core.log`；转绿：`293df562997e-dirty-20260906T041808Z-57025-novex-core.log`，101 项通过。恢复时复用已保存总览，不再发起同一次综合请求。
- 来源块缺失回归先失败：`293df562997e-dirty-20260906T041946Z-57142-novex-core.log`，102 项中仅新增用例失败；已加读取前范围核对，并要求最终覆盖完整。转绿：`293df562997e-dirty-20260906T042100Z-57261-novex-core.log`，102 项核心回归通过。
- 应用侧 `293df562997e-dirty-20260906T042211Z-57259-novex-document.log` 编译与附件、文档、学习工具、工作区和原角色提示词请求链测试通过，耗时 338 秒。6 项组件边界测试及全页面边界检查通过。没有打包、推送、发布或移动端操作。
- 待结清边界：恢复/运行期间切换模型的校验、预算扩展预检扣除已读片段、提供商缺少用量时的估算标识、解析本身截断时的部分完成标记、笔记工具回读。上述测试没有真实模型账单或移动端点击证据，不据此整体勾选 D05—D07。

## 学习接口继续执行记录

### 学习成果回读、模型一致性与解析缺口（D06/D07 子项）

- 笔记回读先失败：`9ee8d4e0ce45-dirty-20260906T043146Z-57850-novex-core.log`，新增用例证明没有模型可调用的笔记入口。实现后该用例通过，`043441Z-58024` 仅原工具目录测试仍限定一个预检入口；更新为预检与只读笔记后，`9ee8d4e0ce45-dirty-20260906T043923Z-58246-novex-core.log` 全部 103 项通过。
- 新增 `learning_read`（读取整理笔记），复用文档工具的字符预算、定位与游标，不另写一套分页。真实文件仓库保存后重新打开，通过多次读取拼回完整笔记；正文不超本次预算、来源锚点保留、越权资料集拒绝。默认先返回总览，来源元数据过多时明确标记截断；笔记变化使旧游标失效，错误指引回到笔记位置，而非虚构文档入口。
- 应用的工具目录、调用分发及两种人格共用协议已接入；读取限于当前分支可访问资料集。工具不能确认或开始学习，不调用模型，也不开放原始文件系统。
- 模型一致性回归先失败：`9ee8d4e0ce45-dirty-20260906T044028Z-58370-novex-core.log`，104 项中新增用例失败，证明扩大预算只核对模型编号。转绿：`9ee8d4e0ce45-dirty-20260906T044145Z-58488-novex-core.log`。共享校验核对提供商、模型编号、窗口与输出上限；应用恢复任务、通读及综合请求前调用相同校验，同时检查连接与当前分支资料范围。变化时拒绝下一次调用，保留进度。接口测试不等于真实提供商或设备切换实测。
- 解析截断回归先失败：`9ee8d4e0ce45-dirty-20260906T044343Z-58628-novex-core.log`；转绿：`9ee8d4e0ce45-dirty-20260906T044509Z-58747-novex-core.log`，105 项全部通过。新资料集保留“解析截断”状态；旧资料集在运行时再次检查解析快照。可读部分照常整理，但缺失来源保留到持久化阅读记录，最终只能部分完成；预检也提示此风险。
- 6 项组件边界测试与全页面边界检查通过。`9ee8d4e0ce45-dirty-20260906T044709Z-58924-novex-document.log` 的应用编译及附件、文档工具、学习工具、工作区、角色提示词回归通过，耗时 338 秒；没有安装、启动或操作设备。
- 仍需结清：预算扩展预检扣除已读片段；提供商缺用量时的估算标识。额外读到旧进度提示写死“没有超出确认上限”，与已发生超额用量保留规则冲突，必须一起改为按真实账本说明，不能继续给绝对保证。D05—D07 暂不整体勾选。

## 下一段工作（不重新扩展范围）

1. D05—D07：核心与云端应用链自动验证已完成；保留真实长文语义质量、实际账单与移动交互由用户验证的边界，不再扩大整理模块重构。
2. T04：真实用户的多轮创建意图延续，取消/换题失效；执行仍必须独立确认。
3. U01/S01：主题选择与恢复的真实失效路径、前台服务超时的可复现启动边界；没有证据时不把猜测写成根因。
4. A04/T01：完整成品、引用、回读、导出链路补齐，然后整批自动回归与日常快速发布。移动端安装、启动、点击继续全部由用户负责。

未推送、未发布、未改动稳定工作树；长目标保持进行中。

### 剩余阅读计划、估算标识与确认说明（D05—D07 收尾）

- 恢复预检先失败：`43e7ab743977-dirty-20260906T045450Z-59358-novex-core.log`；转绿：`43e7ab743977-dirty-20260906T045648Z-59528-novex-core.log`。保存并重新解码进度后，预检与执行都排除已完成片段。五万字符来源已读 24000 字符时，只预留剩余 26000 字符的两批；恢复不能因为剩余内容变少就绕过预算确认。
- 估算标识先失败：`43e7ab743977-dirty-20260906T050030Z-59794-novex-core.log`；转绿：`43e7ab743977-dirty-20260906T050145Z-59892-novex-core.log`。早一次 `045842Z-59657` 是测试数据意外超过预留导致的失败，不当成估算标识缺陷的证据。
- 提供商缺少用量回归先失败：`43e7ab743977-dirty-20260906T050343Z-60039-novex-core.log`；转绿：`43e7ab743977-dirty-20260906T050607Z-60234-novex-core.log`。应用与测试使用同一响应转换入口；缺少部分按该次请求预留计入预算并标为估算，不再用返回文字除以三制造精确计数。
- 状态格式第 8 版保留估算标记，兼容第 1—7 版。旧计数没有来源证明时明确标为“来源未记录”；后续实际计数、扩大预算或重启均不能抹去历史标识。原资料、笔记及已发生用量不清零。
- 超额提示先失败：`43e7ab743977-dirty-20260906T050805Z-60352-novex-core.log`；转绿：`43e7ab743977-dirty-20260906T051049Z-60531-novex-core.log`。进度说明读取实际账本，区分预留不足与已发生超限；不再固定宣称“没有超出上限”。页面复用这份可测试状态投影，未重做控件或布局。
- 已存任务入口先失败：`43e7ab743977-dirty-20260906T051211Z-60650-novex-core.log`；转绿：`43e7ab743977-dirty-20260906T051317Z-60756-novex-core.log`。已完成、部分失败、取消及预算暂停均返回已有记录与笔记入口，不让模型重新建立付费任务。应用保留旧任务授权；新的预算确认不能直接覆盖其已存预检。模型窗口校验失败转成可见错误并保留进度。
- 确认说明先失败：`43e7ab743977-dirty-20260906T051508Z-60908-novex-core.log`；转绿：`43e7ab743977-dirty-20260906T051700Z-61056-novex-core.log`，共 111 项核心测试通过。确认弹窗展示剩余阅读批次、保守输入预留、综合轮数估算与任务累计上限，不许诺绝对账单上限。
- 移动交互技能只用于诚实状态、任务保留与恢复说明；本轮没有安装、点击或启动设备，没有真实模型调用。6 项组件边界测试与全页面边界检查通过。应用层回归结果待下面补记。
- 应用层检查 `43e7ab743977-dirty-20260906T051902Z-61215-novex-document.log` 未拿到终态。其后安全外壳连接连续超时，专用网络连通检查也无响应；不能将其认作产品测试失败或通过，亦未声称远端构建已经停止。
- 按用户已允许的“视窗环境异常则转云端”方案，调整本批顺序为先独立提交源码、推送预览分支用于云端检查。只调度不打包的文档链增量测试，不发布半成品、不触碰正式分支；此前“整批后才推送”的执行安排因此调整，整体修复与最终发布范围不变。
- 修复提交 `f9b76c0` 已正常快进推送到预览分支；[云端文档链检查](https://github.com/ccbili30-collab/novex-android/actions/runs/34015175366) 明确使用 `novex-document`（文档链检查）与 `build_preview=false`（不打包），当前尚未取得最终结论。自动触发的另一个检查由同组手动精确范围检查接替，不代表发布。

### 云端快速检查范围对齐

- 切换环境时确认云端选择性检查缺少本地已纳入的外观、标准工具详情、共享内容、提示词、前台服务、工作区和词表回归，可能出现未测相关代码却报告通过。
- 新增三种选择性模式逐项比对测试清单的脚本回归。`bash scripts/tests/android_cloud_fast_workflow_test.sh`（云端工作流契约检查）先失败于文档模式不一致；补齐云端各组过滤条件后通过。以后两侧清单漂移会直接失败，不靠人工记忆。
- 同时通过检查范围规划、日常预览构建类型、6 项组件边界与全页面边界检查。不新增构建步骤、不启用压缩、不申请签名或发布权限，也没有让移动设备参与自动验证。
- 此处是构建通道独立提交；不把通道检查通过当成应用层测试已通过。后续云端外观与领域回归必须使用补齐后的清单。

### 云端验证结果

- `34015175366` 已完成且成功；来源严格匹配 `f9b76c020c26e01db04483cce6f06f1ec5f973f2`。日志证实实际执行了预览应用编译与文档链单元测试，构建耗时 3 分 9 秒，整个任务 4 分 12 秒。不是读取旧版本结果。
- 签名恢复、打包、安装物上传均未执行；没有新预览发布。D05—D07 的实现与自动验证至此勾选，真实提供商行为与手机操作不包含在此结论中。
- `45731d6` 单独保存云端/视窗测试范围对齐，目前尚未推送；后续源代码检查使用这个较完整的测试清单。视窗失联任务仍无终态证据，不重复调度该镜像；当前继续使用已可用的云端快速检查。

### 多轮创建与真实消息来源（T04）

- 多轮创建回归先失败：[34016002277](https://github.com/ccbili30-collab/novex-android/actions/runs/34016002277)，来源 `58cd57e`，124 项仅新增用例失败。保留前面的明确创建请求后，“按刚才的方案做”能够提出计划，不能代替写入确认。
- 取消、换目标、换题和先前确认作为任务边界；后续“继续”不能重新激活已经结束的创建请求。世界、角色、文游共用同一策略，分身沿用同一策略并仍要求来源版本编辑授权。
- 消息来源回归先失败：[34016282627](https://github.com/ccbili30-collab/novex-android/actions/runs/34016282627)，来源 `d81808f`，127 项仅两条新增来源用例失败；原多轮创建及取消回归已通过。原逻辑会跳过空用户消息，且把附件收据、内部提醒混入授权文字。
- 接入 `1860f74`：从仓库读取当前活动分支的原始消息，复用附件元数据和内部提醒清理；不使用模型压缩历史，不读取图片字节。工具结果、助手文字及附件正文不授权，空用户消息不能沿用旧确认，无法解析的用户行中断旧请求。内容写入与长期记忆写入共用真实确认来源。
- 工具不能传入此前用户请求；该信息只由宿主读取。三类卡片携带 25 个有序模块的服务回归改为多轮请求，并检查模糊确认不会产生写入。分身、原始消息投影及异常消息边界另有接口回归。
- 整组回归 [34016742955](https://github.com/ccbili30-collab/novex-android/actions/runs/34016742955) 已成功，来源 `1860f74`，实际执行预览应用编译与领域增量测试，任务总耗时 3 分 50 秒。原失败用例以及新增来源、异常消息、分身和三类完整卡片服务用例均在本次检查范围内。
- T04 暂不整体勾选：静态复核发现旧匹配器仍允许以卡片名称作为类型匹配回退；当角色与世界重名时，“创建角色雾海”可能被错误用于提出“雾海世界”的计划。当前没有绕过执行确认，但该类型边界仍需补一条失败回归并收紧，再完成 T04 收尾。不把已通过的常规取消/换目标用例扩大为所有目标匹配都已验证。
- 本节只运行云端 `novex-domain`（领域增量检查），不签名、不打包、不发布；`45731d6` 的两侧测试清单对齐现已随源码正常快进推送。三项快速通道脚本检查、6 项组件边界测试及全页面边界检查通过。没有操作设备或调用真实模型。
- 仍有 U01、S01、A04/T01 与最后整批回归、发布工作；长目标未完成，不以本节代替全部修复。
