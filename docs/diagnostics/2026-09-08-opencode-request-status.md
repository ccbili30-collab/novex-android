# 模型服务请求问题核对状态

本项仍未确认用户个案根因，不标记“修复完成”。

- 官方文档要求 OpenCode Go（订阅模型服务）的每段对话使用稳定会话请求头，并使用客户端自身标识，且说明适用于典型编程代理流量。来源：https://opencode.ai/docs/go/#where-can-i-use-it 。2026-09-08 再次核对。
- 当前应用的通用兼容服务构建入口没有会话参数，不能仅从这点判断截图中的请求一定来自本应用，也不能宣布补一个固定请求头就全面兼容。
- 用户提供的 `conversation-252947aa-e90c-4dc8-9c7f-4cafaf9660b0.zip` 中，当前环境记录的供应商名称为 mimo（米模），模型为 mimo-v2.5-pro（米模第二点五代专业模型）。该环境记录没有实际连接地址和请求头，不能判断其背后是官方米模、OpenCode（模型服务）还是其他中转。
- 未调用外部付费服务，未读取或输出密钥。未伪造其他客户端身份、未向所有供应商附加专属会话头。

后续有可核实的实际服务入口时，在独立本地接收端覆盖主请求、流式请求、工具后续、重试、辅助请求和连接测试，再对允许使用的实际服务做有限验证。没有服务入口证据不阻塞其他独立维修与新功能，也不把此项列入“用户升级后已恢复可用”。
