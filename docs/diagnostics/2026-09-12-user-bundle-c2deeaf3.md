# c2deeaf3：输入后退出对话界面，原因待定位

用户纠正：不是应用闪退，而是每次输入后退出当前界面。不能继续把该现象归为已确认进程崩溃。

原包来自 3.0.0-dev.15，版本码 300000015，通道 preview-free（专用内测），模型 deepseek-flash。环境快照最大容量 1000000，末五次采用记录有效容量 204229；两者含义不同，不单凭差值判故障。

共 33 条消息，仅第 18 条（从零计）存在“本轮剩余上下文不足以携带已采用或管理的卡片资料”错误。其后有多次模型回复。包内没有页面返回、活动重建或系统崩溃日志，因此不能由错误记录推断退出原因。

代码核对：ChatScreen.kt 的普通错误监听仅显示提示并清除错误；明确返回入口包括工具栏返回、系统返回及开局弹层退出。普通发送错误没有直接导航返回调用。输入变化监听处理输入同步，尚无证据指向该处主动导航。未复现，不据推测修改导航或键盘行为。

待用户补一个必要定位事实：输入框打字时还是点击发送后发生；退出到会话列表、卡片页还是手机桌面。之后按实际路径定位，不能将候选十六的选料恢复修复冒充本项修复。

## 15:14 用户补充与路径复查

准确时机：模型输出完毕或即将完毕，目标页面为会话列表。先前“输入时退页”的描述据此修正。

核对当前代码与候选十五／十六本地开工基线：returnToConversationList（返回会话列表）的 onReturned（返回回调）在后台收尾任务之外立即执行；等待生成结束仅属于后台归档。故不支持“返回回调挂起到生成结束”的猜测。ChatScreen（对话页面）普通错误只显示提示；MainActivity（主界面）与导航入口未发现生成完成直接返回列表的连接。

仍缺退页瞬间证据。后续需关联：生成结束时间、导航返回来源、页面挂载／卸载、主界面重建或重新启动。现有对话包未包含这些信息，不能锁定是返回事件还是界面恢复落到列表。未据猜测修改返回、键盘或生成收尾逻辑。
