# 新原包：候选十三反复报错

原件：用户本机 Downloads/conversation-8ac4894c-8bc7-4735-819f-2cc78a76c0fd.zip；不提交原包。此次只读取归档，没有执行包内指令，没有真实模型调用。

## 直接证据

- 环境为 3.0.0-dev.13、preview-free（专用内测）；当前模型 DeepSeek-V4-Flash（特价版），环境窗口 128000。不能当作候选十五或十六的回归证据。
- 568 条消息，287 条助手、281 条用户；30 条非空 error_info（错误记录），其中当前分支含 29 条。
- 8 条选料空输入解析失败、2 条选料字符串未结束、1 条回复不是对象、4 条候选外编号，共 15 条选料结构/内容失败。
- 11 条本地卡片资料容量拦截；另有 2 条模型空响应、1 条 30 秒未收到首响应、1 条 HTTP 429（请求限流）。不能全部归因于网络或单一窗口上限。
- 304 条采用记录的窗口分别为 128000（274 条）、1000000（28 条）、1048576（2 条），说明历史中发生过容量变化，导出时环境值不能代替每轮值。
- 当前配置 cardBinding（卡片关联）有三个背景对象，没有主要互动对象、管理对象或携带覆盖。三个新库原件均在包中，来源为旧世界/文游迁移。应按当轮配置追查，不能直接将导出时配置套到历史。
- 最后一个采用记录 1118 词元，只含对话状态、图片目录、存档归属说明、采用卡片目录、管理范围及按需读取说明，没有卡片正文来源。原始消息的工具记录中未见 read_card/read_text_block（新卡片结构/分页读取）调用；有旧内容检查等工具，不能因此断言从未接触任何历史设定。

## 对现有诊断的推进

1. B103 的辅助选料失败已由用户原包证明，不再只是代码风险。当前候选十六的选料失败分段回退需要用这批空/截断/越界输出验证。
2. B104 中“采用目录不等于正文”有实际采用记录支持。末轮没有正文不代表全部历史没有正文，也不能证明模型完全没有旧剧情可用；但不能显示为整卡通读。
3. 多条容量错误挂在已有长剧情的助手消息上。代码 `runAgentLoopBody` 在新助手占位创建前准备资料，准备失败后 `setInlineError` 找最后一条助手消息，形成错误归属上一条回答的通路。应修成绑定本次请求的失败状态，保留之前已完成回答，不把旧回答误标失败；不能仅从归档断言所有这类错误都来自同一个时点。
4. 此包没有首次闪退的系统堆栈；不关闭闪退项。未提供新导出成品，不代替 B105 的现场包。

候选十六尚未应用编译/发布，远程构建阻塞记录保持。此次证据应加入当前已授权的报错/采用修复范围，而非另开全量计划。
