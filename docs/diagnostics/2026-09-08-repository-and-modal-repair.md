# 对话仓库与创作库弹层修复进行记录

用户已确认执行《世界书复用与未完成更新计划书》。本记录只覆盖第一批正在处理的仓库和弹层；后续世界书、目录图片与剧情插图仍待实施。不发布、不改暂缓审批、第六版提示词不启用。

## 取得的失败证据（采用修正后的同一测试）

独立安卓 14 模拟器，串口 `emulator-5580`（测试设备）。新增原生检查 `NovexRepositoryEntryInteractionTest.kt`，通过真实导航、工作区导入和登记、实际工具搜索，以及系统窗口触摸注入检查。

- 新建草稿提升为真实对话后，把三份中文文件导入真实对话工作区并登记；工具搜索成功，但从对话菜单进入仓库后找不到对应文件。
- 多选库浏览器进入子文件夹后按系统返回，底层第一次点击计数为零。
- 真实创作库管理控件进入子文件夹后按系统返回，底层第一次点击计数也为零。
- 日志 `local/qa/worldbook-update-20260908/native-faithful-before.log`：3 项运行、3 项失败。分别是文件列表等待超时、两个底层触摸断言失败。

初次冷启动有安卓系统界面无响应弹窗，选择等待恢复后重新运行；初版测试未等待导航就点击菜单，另有未等待管理页初始化的问题，均修正测试准备后才取上述失败证据。不把系统弹窗和测试时序问题计为产品修复。

## 已修改并通过定向验证

- 准备仓库先保存对话，通过回调返回实际对话编号；聊天页面把这个编号交给导航，不再捕获旧草稿地址。准备失败说明仓库暂不可用、已有资料保留。
- 库选择器的关闭回调结束当前选择；目录内显式“返回上一级”负责目录导航，二者不再混用。
- 创作库管理各选择弹层关闭时退出流程；打开卡片前结束管理弹层。避免弹层已隐藏但父组件仍保留窗口。未改公用组件样式和三档权限。

现有错误归属的旧文件不按名称或时间猜测迁移，原件保留。当前失败用例验证“新建对话入口与实际仓库一致”的修复，不冒充用户那三份原件已自动找回。

## 验证状态

快速界面检查已通过，日志 `local/qa/worldbook-update-20260908/fast-maintenance.log`。修复版原生 3 项通过，日志 `local/qa/worldbook-update-20260908/native-fixture.log`。

此前 `native-final-red.log` 与 `native-maintenance-green.log` 的失败不能单独作为产品证据：测试在界面重组时重新生成草稿入口编号，另有返回键命令尚未执行完即关闭输出管道的时序问题。已将路由固定在界面创建前、等待系统命令完成，并保留真实屏幕截图。

随后在隔离构建目录恢复旧仓库导航目标（仍捕获草稿编号）、旧库选择器和旧管理弹层逻辑，其他既有修复保留，使用完全相同的修正测试重跑。`native-faithful-before.log` 再次复现三项失败；修复源文件已恢复。该对照只证明上述源码断点及回归，不证明用户历史三份文件的原始归属。

本轮尚未处理模型服务适配及后续新功能。上一批已完成的成果打开、存档、记忆刷新、无模型退出等修改继续保留；其范围见已有修复报告。
