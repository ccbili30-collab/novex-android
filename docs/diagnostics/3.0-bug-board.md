# 3.0 故障表

更新时间：2026-09-09。当前版本尚未发布。

这是当前故障状态的唯一入口。新增问题补入第一张表；修复并验证后从待处理表移到已修记录，保留证据，不再反复列作未修复。复发时重新打开原编号。历史执行日志只作追溯，不用其中过期的“待验”覆盖本表。

## 待处理故障与待核实反馈

| 编号 | 问题 | 当前状态 | 下一步／关闭条件 |
|---|---|---|---|
| B04 | 用户反馈世界／文游导出残缺 | 已复现并修复扩展字段丢失；用户具体残缺导出原件未取得 | 本地完整导出与再导入已通过；取得原件后核对是否另有残缺。缺原件不能宣称穷尽，也不撤销已验证修复 |
| B05 | OpenCode Go（模型服务）疑似不能调用 | 缺实际配置／入口证据，未验收 | 获得对应请求后核对官方会话头、工具续调及重试；不向所有提供商统一加专属请求头 |
| B13 | 无模型首页隐藏已有草稿，真实导航又把新对话退回首页；欢迎语偏故事模式 | 签名候选实际入口复现。此前 F01 只覆盖页面级进入，未覆盖导航。现仅空首页显示引导，移除导航的无模型回退，保留发送时连接检查 | 待最终签名候选从真实首页新建、保存草稿、列表打开及重开；页面级测试不能单独关闭本项 |

## 已修复并验证

证据根目录：`local/qa/v3-execution-20260909/`。以下关闭的是具体故障路径，不代表所有设备和组合均覆盖；没有重新发布到用户设备的项目仍须随 3.0 交付。

| 编号 | 已关闭的具体问题 | 验证依据／范围 |
|---|---|---|
| B11 | 快速检查清单包含已迁走的旧文件 | 清单过滤不存在的输入，远端清理保持原有清单差异机制；脚本检查通过，真实同步后成功编译并进入 1,441 项测试。后续旧测试断言失败另记，不是同步失败 |
| B10 | 流式降级提示暴露英文实现细节 | 改为生成／排版提示，补齐简繁中文；三份资源检查通过，`composer-controls-integration-build.log` 整合构建成功；27 项单元及 5 项安卓检查通过。仅修文案，性能保护不变 |
| B02 | 原建卡反馈中的多卡、重复请求及模块编辑衔接 | 原包操作形态的 4 项数据库检查通过；`multi-card-native.log` 实际聊天链路通过（133.554 秒），仓库读取→四卡及重复创建→同名新增拒绝→按原编号追加→排序→成果打开→重开。14 项操作仅保留预期的同名新增失败，四卡各两模块，其他卡正文不变；截图已查看。不把模型分卡偏好或语义服从判作已修软件故障 |
| F01 | 无模型新建对话退出 | `draft-window-native.log`：进入、编辑、发送引导、返回与持久草稿 |
| F02 | 回答中的“打开卡片”进入空白创建页 | 成果路由检查及真实创建旅程；详见[执行记录](2026-09-09-v3-execution.md)与[旅程诊断](2026-09-09-real-model-journey.md) |
| F03 | 填写空卡后归库与管理范围不同步 | `promoted-card-green.log`：写后配置、隐藏其他空卡、失败回滚及重复请求 |
| F04 | 勾选审批框立即执行 | `approval-green-2.log`：选择不执行、确认一次、拒绝／关闭及重复点击 |
| F05 | 记忆保存与读取衔接 | [记忆与审批诊断](2026-09-08-memory-and-approval.md)及[执行记录](2026-09-09-v3-execution.md)：保存、读回、重开及范围检查 |
| F06 | 存档成功但列表找不到；普通存档要求模型重写剧情 | `workspace-formal-branch-native.log`、`creation-model-checkpoint-aligned/native.log`、`checkpoint-details-ui-native.log`：正式分支、名称存档、重开和详情 |
| F07 | 仓库文件可见但工具找不到 | `workspace-formal-branch-native.log`、`legacy-workspace-recovery-native.log`：新文件正式分支及可证明来源的旧文件恢复 |
| F08 | 中文路径失败、翻页标记无故失效 | `workspace-readability-native.log`及`source-query-readable-run/native.log`：真实安卓目录、分页和中文路径读取 |
| F09 | 世界／文游导出丢失作者扩展字段 | `export-completeness-green.log`、`card-export-entry-native.log`：完整正文、扩展字段、图片、分享文件及独立库往返；用户原件另见 B04 |
| F10 | 对话包不能直接保存文件 | `draft-export-native.log`：直接保存、取消及字节一致性 |
| F11 | 流式生成结束后底部跟读回跳；早期手动上翻续验见 B07 | `stream-viewport-history-native.log`：底部保持末尾，主动上翻保留历史；`header-center-chat-native.log`再次验证长回复末尾。其他组合纳入下表验收，不将已通过路径继续列为未修 |
| F12 | 正式提示词新增过细的小动作禁令 | `scroll-prompt-boundary-build.log`：撤掉新增限制，允许用户委托设计身份；用户提示词不改，第六版不启用 |
| F13 | 时钟挤偏标题 | `header-center-green-native.log`：短／长标题及两侧宽度变化；`header-center-chat.png`实际截图已查看，相关 7 项布局／组件检查通过 |
| B01 | 切换身份后后台回执被改成助手正文 | `history-paired-receipts-green.log`：15 项通过；`history-paired-receipts-native.log`：安卓实际请求 3 项通过，保留成功／失败与原调用配对，私有参数／推理／图片不重放；设备请求原件 `history-paired-receipts-device.tar`。原始数据库与导出不改。 |
| B03 | 嵌套创作库弹窗白线、返回后触摸阻塞及仓库键盘返回 | `library-modal-integration-native.log` 中对应弹层与白线检查通过；暗色、1.5 倍系统字体的 `library-dark-large-native.log` 4 项全通过，含真实窗口点击和真实输入法显示／返回；亮暗截图已查看。其他页面综合组合仍列 T02 |
| B06 | 有工具可用时，普通正文完成前被误折叠 | `stream-visible-unit-results.tar.gz` 38 项通过；`stream-canonical-entry-native.log` 4 项通过，正文生成中可见、真实工具过程仍默认折叠，失败详情可打开 |
| B07 | 发送后的迟到定位可能覆盖手动阅读 | 发送定位携带请求时间，用户拖动取消待定位，下一帧再检查；`stream-drag-order-unit-results.tar.gz` 13 项通过；`stream-canonical-entry-native.log` 4 项通过（115.367 秒），涵盖立即上翻和正文出现后上翻、完成后保持历史及底部跟读。`stream-canonical-entry-device.tar` 的历史／末尾截图已查看，实际绑定已核对 |

## 已排除的测试问题

- B12：完整候选检查 1,441 项中 1 项失败、3 项跳过。唯一失败是旧 `InteractiveFictionAgentToolsTest` 仍要求存档必填摘要和状态，与已确认且已有安卓验证的名称存档契约冲突。更新为名称必填、辅助摘要与结构化状态可选；原失败 `release-candidate-old-checkpoint-test-failure.log` 保留；`release-candidate-fast-check.log` 完整复验通过，1,441 项无失败、3 项环境相关用例跳过。

- B09：原组合检查未等待流式回合完成，曾在正文可见时未找到成果入口。单独复验通过；按回合完成状态等待后的 `pipeline-completion-boundary-native.log` 4 项全部通过（213.696 秒），含成果打开、审批恢复、排序、重开与删除。原失败保留；未改生产成果路由，不冒充生产代码修复。

- B08：`stream-binding-diagnostic.json` 确认测试写入保存前的临时模型编号，该编号在规范化后的模型库中不存在，因而选择了旧测试接口；并非已证明的用户端绑定缺陷。测试现从模型库回读保存后的编号。原连接失败及绑定等待失败保留，不作为滚动失败或通过依据。

## 非故障的交付验收跟踪

已通过的具体范围与剩余发布步骤分别记录，结构债务不冒充新故障，尚未完成的发布也不隐藏。

| 编号 | 工作 | 剩余范围 |
|---|---|---|
| T01 | 控制器与页面职责拆分 | 资料任务调度、消息行、模型选择弹窗已迁出；模型选择手机用例在 `stream-viewport-native.log` 中通过。单轮流式执行已迁出并通过相关检查；输入文本编辑逻辑已迁出，整合编译及 34 项相关单元检查通过；实际三档权限及草稿复验 4 项通过（`pipeline-completion-boundary-native.log`），输入框显示／键盘接入和思考等级弹层已迁出独立组件；`composer-controls-integration-build.log` 构建成功，27 项相关单元及 `composer-controls-native.log` 5 项安卓检查通过。本轮渐进拆分已核对到执行／恢复、资料协调、消息行、输入与选择弹层，独立组件不反向依赖页面／总控制器；宿主语音、附件与路由等历史结构债务保留，不宣称全库重写完成 |
| T02 | 综合用户旅程与界面验收 | 分段已验证；最新 `journey-end-args-native.log` 114.737 秒通过，覆盖身份、记忆、文游、世界书、状态存档、原件移除后图片保留、重开和结束恢复；多卡见 B02，带图导出见 F09，滚动／窗口组合见 F11／B03／B07。继续检查安装候选是否保持这些成果 |
| T03 | 长资料软件验收 | 小规模、十万字、百万字离线恢复各 12 项已通过（`learning-software-*`），含来源修订和重复范围；`learning-ui-equality-native.log` 安卓实际资料弹窗通过：持久快照重建、279/279 可读范围与 1 项未解析分开、笔记来源、返回和续接入口；续接不改旧快照。综合使用原真实模型记录与本轮离线恢复证据，不冒称重跑百万字真实模型；模型语义偏差仅诊断 |
| T04 | 升级与发布 | 固定源码及证据、升级前备份、迁移／重开、整合预览线、直接打包发布；稳定线不改 |

外部资料直接导入、多世界书启停、本卡图片与剧情插图、容量识别、输入框占比和峰谷时钟均已有实现；随 T02／T04 验收交付，不重复开发。

## 明确延期／保持候选

- 基础图文模板已延期。
- 直观挂载关系图、地图等特殊模块后续讨论。
- 第六版提示词保留候选，不直接替换正式版本。
- 亮暗桌面图标自动跟随：有限检查后延期。当前确认的白鸽素材为一张黑底原图，旧亮／暗前景文件校验值相同，未定位到可直接复用的两版白鸽素材；不将同图切换冒称成功，不另造图。3.0 保留固定白鸽图标，无新增开关。

完整范围见[主计划](../plans/2026-09-09-complete-next-update-plan.md)，详细证据与历史失败见[执行记录](2026-09-09-v3-execution.md)。每完成一项同步本表，再继续下一个未完成项。
