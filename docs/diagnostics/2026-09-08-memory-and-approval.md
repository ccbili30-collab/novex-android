# 记忆与确认弹窗反馈核对

历史决定：只保留诊断结果，暂不修改确认弹窗行为。2026-09-09 用户已明确要求纳入修复，暂缓已解除；最新执行范围见 [完整更新计划 U4](../plans/2026-09-09-complete-next-update-plan.md)。以下诊断证据保留，尚未因本次登记修改生产行为。

## 证据与结论

- 用户提供的对话包导出环境为预览版 0.2.16-beta.38。包内同时保留了更新前后的历史。
- 9 月 8 日凌晨历史中确有“发送确认短语”拒绝、反复重试，以及写入返回成功后查询为空的记录。不能因导出版本较新，就把所有历史记录当成该版本执行。
- 同一包内当天 16:07:46 的记忆准备回执已经不要求用户发送确认短语；16:08:09 返回写入 4 条；16:10:07 查询实际返回同 4 条记忆。此链路与用户补充“今天更新后好像好了”一致。这里只确认该次实际成功，不推断所有记忆场景均已验收。
- 新反馈中的勾选即执行，与当前 `NovexToolApprovalDialog.kt:44` 直接一致：勾选回调既设置 checked，也立即调用 onApprove；底部另有执行按钮，也直接调用 onApprove。并非已证实的点击穿透或模型擅自跳过批准。
- 既有 `NovexFoundationInteractionTest.approvalHidesRawArgumentsAndOneCheckboxResolvesTheDecision` 还断言勾选后立即批准一次。因此原测试是在验证旧交互规则，未覆盖本次用户要求的“先勾选，再点击确认执行”。

## 待用户重新授权的修复范围

勾选／取消勾选只改变选择状态；勾选后才能点击确认执行；提交后一次性消费；拒绝、关闭以及切换待审批操作不执行。届时需要以真实弹窗点击测试验证，而非仅调整文案。当前未修改相关生产代码或测试，未宣称修复通过。
