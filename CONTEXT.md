# Novex 对话领域

Novex 使用统一的对话模型。提示词、世界、身份和呈现方式是对话上可以独立组合的配置，不构成互斥的对话类型。

## 对话

**对话（Conversation）**：
承载连续消息历史和当前对话配置的唯一会话概念。通用助手、世界助手和角色聊天都是对话的不同配置。
对话创建后仍可加入、替换或移除世界、角色版本和玩家身份；这些变化不改变对话的身份，也不丢弃已有消息。
_Avoid_：把通用对话、世界对话、角色对话或创作对话当成不可转换的数据类型

**对话上下文（Conversation Context）**：
对话当前采用的回答身份、背景设定、玩家身份和活动文游。本概念只决定人工智能如何理解与回应，不授予任何内容编辑权限。

**回答身份（Answer Identity）**：
一段对话中负责回答的唯一身份，只能是 Nova 助手预设或一个具体角色版本。角色作为回答身份与角色作为背景资料是两种独立关系。

**背景设定（Background Setting）**：
对话可以参考的零个或多个世界与角色版本；多个世界保持各自边界，可以共同构成跨世界叙事。背景设定默认只读，不因被采用而成为管理对象。

**管理对象（Managed Subject）**：
用户明确挂载到对话、允许人工智能定位和管理的世界、角色版本、文游或创作成果。管理对象不自动成为背景设定，其内容也不自动完整进入每轮模型上下文。

**挂载（Mount）**：
将内容加入一段对话的管理对象集合并授予明确访问范围。同一内容可以同时是背景设定和管理对象，但两种关系必须分别表达。

**创作工具（Creation Tool）**：
任何对话都可以调用的内置创作能力。“帮我创作”是打开该能力的快捷入口，不创建受限的创作对话类型。

**对话提示词（Conversation Prompt）**：
只属于一段对话的可编辑行为提示词快照。新对话从其创建来源取得初始内容，之后的编辑不修改全局预设、世界或角色卡原件。

**对话呈现（Conversation Presentation）**：
对话的可选视觉身份，包括气泡模式、人工智能头像、玩家头像和背景。呈现方式不决定模型提示词内容。

**图片风格提示词（Image Style Prompt）**：
只属于一段对话的可选生图约束，在执行生成或编辑图片时与本次画面描述合并。

**快捷操作（Conversation Control）**：
由文游预设、人工智能或用户注册到一段对话的可见操作。查看型操作不推进剧情，行动型操作可以创建新回合；对话中的动态操作不会自动改写共享文游。

## 内容来源

**世界（World）**：
有自己名称的世界观实体，默认名称为“我的世界”；它拥有默认概述和可选世界模块，并可关联角色版本。世界本身不是一段对话。

**世界模块（World Module）**：
世界中按需添加的结构化设定单元，例如时间线、时代事件、地图、地区、势力和种族。

**玩家身份（Player Persona）**：
可关联到对话的用户身份资料。除展示回退值外各字段均可留空；它只描述用户是谁，不替用户决定行为。

**角色（Character）**：
角色库中的根对象，统一拥有一个角色本体和零个或多个分身。

**角色版本（Character Version）**：
角色本体和分身的统称；世界和对话关联的是具体角色版本，而不是整个角色容器。

**角色本体（Original Character Version）**：
角色的默认版本，可直接关联到一个或多个世界，也是创建分身时的来源。

**分身（Character Variant）**：
归属于同一角色、但可以独立编辑和复用的角色版本。分身不被单一世界占有，可关联到一个或多个世界。
_Avoid_：世界覆盖、角色副本

**角色卡（Character Card）**：
属于某个角色版本的人工智能身份与角色设定内容。它包含基本信息、可选角色模块和视觉资源。

**角色模块（Character Module）**：
角色版本中按需添加的结构化设定单元，例如多形态语录、属性面板、装备、天赋技能、外貌性格和兴趣爱好。

**世界角色关联（World Character Membership）**：
世界与具体角色版本之间的多对多关系。同一本体或分身可加入多个世界，一个世界也可包含多个角色版本。
_Avoid_：合并角色卡

**Nova 助手预设（Nova Assistant Preset）**：
新建通用对话时用于生成初始对话提示词的全局模板。已创建对话持有自己的快照，不随模板修改而改变。
_Avoid_：Novax 助手预设

## 文游

**文游（Interactive Fiction Project）**：
可复用、可分享的模块化游玩项目，定义启动方式、玩法规则、关联内容和预设操作。启动文游会创建或配置一段统一对话，不产生另一种对话类型。

**活动文游（Active Interactive Fiction）**：
一段对话当前运行的零个或一个文游快照。多个玩法需要先组合为一个文游，避免一段对话同时运行互相冲突的状态规则。

**本局状态（Playthrough State）**：
一次文游过程中产生的可变事实，例如当前位置、血量、物品、任务和动态操作。本局状态属于具体对话分支，不会因剧情推进自动写回共享世界、角色或文游。

## 创作成果

**创作成果（Creative Artifact）**：
对话或工具产生并需要长期保留的文档、图片、地图、卡片包或其他文件。创作成果保留来源对话、消息与分支，并可被多个内容对象引用。

**创作库（Creative Library）**：
用户在 Novex 内统一查看和管理创作成果的空间，可按对话、项目和内容类型组织同一批成果。创作库不是对话类型，也不等于应用的临时附件目录。

## 资料整理

**资料集（Source Collection）**：
用户导入并交给人工智能整理、通读和理解的一组原始文件及其组织索引。资料集不是世界、角色、文游或创作成果，导入资料也不授权自动创建这些内容对象。
_Avoid_：卡片原料、背景设定包

**资料通读（Source Review）**：
人工智能按文件和结构化内容块完整阅读资料集、记录来源与剩余范围的可恢复过程。检索命中和概括不能冒充完整通读；无法读取的内容必须明确列出。
_Avoid_：文档检索、全文注入

## 上下文连续性

**上下文引用记录（Context Usage Record）**：
一轮模型请求实际采用的身份、设定模块、记忆、摘要和工具结果的只读记录。它属于产生该回复的消息分支，不能在切换分支时重新推测或执行。

**对话蒸馏（Conversation Distillation）**：
将旧对话中仍影响后续的决定、事实、关系、叙事约束和状态提炼为可续接内容的过程。原始消息与正式创作成果仍然保留，蒸馏结果不能取代作品存储。

## 记忆

**角色记忆（Role Memory）**：
只属于“世界＋玩家身份＋角色卡”组合的记忆空间，不与通用 Nova 记忆或其他角色组合混用。
